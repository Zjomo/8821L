# SpotZoom v65 综合检测报告（2026-05-18）

## 1. 前沿同类项目调研（本轮复核）

1. CoTracker3（ICCV 2025）
- 参考: https://openaccess.thecvf.com/content/ICCV2025/html/Karaev_CoTracker3_Simpler_and_Better_Point_Tracking_by_Pseudo-Labelling_Real_Videos_ICCV_2025_paper.html
- 可模仿模块: 可见性（visibility）信号驱动的点轨迹稳定性约束、长时序一致性校正。

2. Norfair Tracker
- 参考: https://tryolabs.github.io/norfair/2.0/reference/tracker/
- 可模仿模块: hit counter / initialization delay / pointwise counter 机制用于“接受-拒绝”平衡。

3. trackpy
- 参考: https://soft-matter.github.io/trackpy/dev/tutorial/adaptive-search.html
- 可模仿模块: adaptive search（逐步收紧/放宽预算）与诊断指标可观测性。

4. HCIPy / AOtools / waveprop / slmsuite
- 参考: https://docs.hcipy.org/
- 参考: https://aotools.readthedocs.io/
- 参考: https://github.com/ebezzam/waveprop
- 参考: https://github.com/slmsuite/slmsuite
- 可模仿模块: 光学仿真与控制流程的“可组合模块化设计”、更清晰的 pipeline 分层。

## 2. 对 SpotZoom.py 的本轮补充

目标文件: `E:\jupyter file\2_Optics\8821L\SpotZoom.py`

新增 `frontier v65 trajectory guard`（轨迹守卫，CoTracker3/Norfair/trackpy 风格）：
- 新增配置默认项、`AlignmentConfig` 字段、CLI 参数、`check_environment` 规则校验。
- 新增运行指标:
  - `frontier_v65_guard_checks`
  - `frontier_v65_guard_refines`
  - `frontier_v65_guard_rejects`
  - `frontier_v65_guard_soft_accepts`
  - `frontier_v65_guard_failures`
- 新增控制器状态:
  - `self._frontier_v65_center_history`
  - `self._frontier_v65_reject_streak`
- 新增方法:
  - `_apply_frontier_v65_trajectory_guard(...)`
- 主链路更新:
  - `v57 -> v58 -> v59 -> v63 -> v64 -> v65`
- Open-source bundle 预设联动:
  - `microscopy_tracking`
  - `resilient_association`
  - `frontier_2026_tracking_stack`
  均默认启用 `frontier_v65_trajectory_guard_enabled`。

## 3. 自动化检测结果

### 3.1 构建/语法/环境
- `python -m py_compile 8821L/SpotZoom.py`: PASS
- `python -m py_compile XPS/xps_admin_console/server.py`: PASS
- `node --check XPS/xps_admin_console/static/app.js`: PASS
- `python SpotZoom.py --check-env ... --frontier-v65-trajectory-guard`: PASS

### 3.2 SpotZoom 核心流程回放

1) `spotzoom_run_report_v65_smoke.json`
- 状态: success
- 指标: `detect_success=5/5`
- v64: `checks=5, rejects=0, soft_accepts=5`
- v65: `checks=5, refines=0, rejects=0, soft_accepts=5`

2) `spotzoom_run_report_v65_offcenter_noisy.json`
- 状态: success
- 指标: `detect_success=5/5`
- v64: `checks=5, rejects=0, soft_accepts=5`
- v65: `checks=5, refines=0, rejects=0, soft_accepts=5`

3) `spotzoom_run_report_v65_probe.json`（高扰动+紧门控）
- 状态: success
- 指标: `detect_success=5/5`
- v64: `checks=5, rejects=0, soft_accepts=5`
- v65: `checks=5, refines=2, rejects=1, soft_accepts=2`
- 说明: v65 的 refine/reject 分支已被实际触发（事件见 `spotzoom_events_v65_probe.jsonl`）。

4) 故障注入
- `spotzoom_run_report_v65_missing_image.json`: failed（符合预期，缺失图像）
- `spotzoom_run_report_v65_xps_fail.json`: failed（符合预期，XPS 连接失败）

### 3.3 页面访问/静态资源/接口请求/网络异常
- 文件: `8821L/artifacts/xps_http_check_v65.json`
- GET:
  - `/`, `/static/app.js`, `/static/styles.css`, `/api/features`, `/api/session` -> 200
  - `/api/unknown`, `/static/not_found.js` -> 404（预期）
- POST:
  - `/api/connect`（不可达 127.0.0.1:1）-> 400
  - `/api/controller/raw`（未连接）-> 400
  - `/api/files/upload`（invalid base64）-> 400
  - `/api/files/upload`（超 2MB）-> 413
  - `/api/disconnect` -> 200
- 服务端日志: `8821L/artifacts/xps_server_v65_stdout.log`

### 3.4 数据库读写
- 在 `SpotZoom.py` 与 `XPS/xps_admin_console/server.py` 未检出 sqlite/mysql/postgres/redis/mongodb 访问关键词。
- 当前链路主要是图像处理 + 设备控制 + HTTP，不是独立业务数据库架构。

### 3.5 前端控制台日志
- `playwright` 依赖缺失（`Cannot find module 'playwright'`），本轮未完成真实浏览器 console runtime 抓取。
- 当前仅覆盖静态语法检查（`node --check`）和 HTTP 行为检查。

### 3.6 代码质量与模块结构
- `SpotZoom.py`: 20367 行，`except Exception` 129 处，`_apply_frontier_v*` 方法 37 个。
- `SpotZoomController.__init__`: 约 2167 行（4407 -> 6574），耦合度高。
- `XPS/xps_admin_console/server.py`: 1002 行，`except Exception` 13 处。

## 4. 问题分级（优先级 / 影响模块 / 建议）

### P1
1) 单文件耦合持续偏高
- 影响模块: `8821L/SpotZoom.py`
- 现象: `SpotZoomController` 构造和 frontier 链过长，回归风险高。
- 建议: 先拆 `frontier_post_assoc_chain.py`（v58~v65）并保留统一 `apply(...)` 接口。

2) 接口异常语义仍粗粒度
- 影响模块: `XPS/xps_admin_console/server.py`
- 现象: timeout/未连接/参数错误多映射 400，诊断成本高。
- 建议: 细化 408/409/422/500 + `error_code`，前端按码提示与重试。

### P2
3) 广谱异常捕获密度高
- 影响模块: `SpotZoom.py`, `server.py`
- 现象: `except Exception` 较多，根因定位依赖日志文本。
- 建议: 逐步替换为 typed exceptions 并规范错误事件字段。

4) 前端 runtime console 覆盖缺口
- 影响模块: 自动化链路
- 现象: 无 Playwright，无法持续抓取 JS 运行时错误。
- 建议: 安装并固化 Playwright 检查步骤（含 console/error/network failed request）。

### P3
5) v65 仅在高扰动样本触发调节
- 影响模块: `frontier_v65`
- 现象: 常规样本以 soft_accept 为主，probe 样本触发 refine/reject。
- 建议: 增加“遮挡/突跳/漂移”回放组并做分布统计后再收敛参数。

## 5. 下一步优化方案

1. 把 `v58~v65` 守卫链抽离成独立模块，降低 `SpotZoom.py` 主文件复杂度。
2. 建立 v64/v65 A/B 基线回放（同一输入序列、固定随机种子、统一迭代预算）。
3. 在 `xps_admin_console/server.py` 完成错误码细化与 `error_code` 字段规范。
4. 补齐 Playwright 浏览器层检查，覆盖 console、network failed、资源 404 监控。
