# debug-closed-loop-drift

状态: [OPEN]

## 问题描述
闭环校准/标定过程中，光斑并未收敛到目标点，反而出现逐步远离目标点的现象。

## 主要假设
1. 标定得到的“步数/像素”符号方向反了，导致纠偏方向与实际光斑运动方向相反。
2. 闭环校准中使用的映射轴或探测器轴对应关系不正确，造成 X/Y 纠偏映射到错误的电机轴。
3. 标定步长过大，单次纠偏超调，随后在反向误差下持续发散。
4. 标定/闭环过程中帧更新存在延迟或使用了过期质心，导致纠偏基于旧数据执行。
5. 闭环与标定状态切换时，目标点或基准质心被错误重置，导致后续误差计算基准偏移。

## 待确认信息
- 标定函数中如何计算步数/像素比值。
- 闭环纠偏函数中是否对符号、轴映射、限幅进行处理。
- 是否存在步长、间隔时间、平滑系数等可调参数。
