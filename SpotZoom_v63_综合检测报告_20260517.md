# SpotZoom v63 综合检测报告（2026-05-17）

## 1. 本轮互联网前沿项目调研（同类/近似）

本轮聚焦“光点跟踪 + 连续关联 + 光学闭环”的开源项目与文档：

1) CoTracker（Meta）
- 参考: https://github.com/facebookresearch/co-tracker
- 可模仿点: 轨迹可见度（visibility）输出、在线分块跟踪（online chunking）与长时序一致性。

2) Norfair
- 参考: https://tryolabs.github.io/norfair/dev/reference/tracker/
- 可模仿点: `hit_counter_max`、`initialization_delay`、`pointwise_hit_counter_max` 等惯性/寿命控制参数。

3) trackpy
- 参考: https://soft-matter.github.io/trackpy/v0.3.0/tutorial/linking-diagnostics.html
- 可模仿点: `memory`（目标短暂丢失后重连）与 linking diagnostics（如 remembered/子网络复杂度诊断）。

4) DeepTrack2
- 参考: https://github.com/DeepTrackAI/DeepTrack2
- 可模仿点: 模块化图像管线（生成/增强/分析）与实验快速拼装。

5) slmsuite
- 参考: https://slmsuite.readthedocs.io/
- 可模仿点: 光学/全息校准与可复用工作流组织方式。

6) HCIPy
- 参考: https://hcipy.org/
- 可模仿点: 模块化 AO/波前仿真组件化结构。

7) OpenWFS
- 参考: https://openwfs.readthedocs.io/en/latest/api_algorithms.html
- 可模仿点: 算法对象化（stepwise/dual-reference等）和 `troubleshoot(...)` 风格的诊断入口。

## 2. 本轮 SpotZoom.py 补充内容

已在 `SpotZoom.py` 新增 `frontier v63`：

- 名称: inertia-memory guard（惯性+短时记忆守卫）
- 位置: `SpotZoomController._apply_frontier_v63_inertia_guard(...)`
- 核心思想:
  - CoTracker 风格可见度分数参与阈值松弛；
  - Norfair 风格 hit counter/初始化延迟控制收敛稳定度；
  - trackpy 风格 memory frames 做短时救援（memory rescue）；
- 接入方式:
  - 追加在 v57→v58→v59 之后执行；
  - 已接入默认配置、bundle 预设、CLI 参数、参数校验、运行指标。

## 3. 自动化检测范围与结果

### 3.1 构建/语法检查
- `python -m py_compile 8821L/SpotZoom.py`: PASS
- `python -m py_compile XPS/xps_admin_console/server.py`: PASS
- `node --check XPS/xps_admin_console/static/app.js`: PASS

### 3.2 SpotZoom 核心流程

1) check-env
- 退出码: `0`
- 结论: PASS

2) smoke（中心光点）
- 报告: `spotzoom_run_report_v63_smoke.json`
- 状态: success
- 检测: 5/5 成功
- v63 指标: checks=5, refines=0, rejects=0, rescues=0, failures=0

3) offcenter+noisy（偏心噪声）
- 报告: `spotzoom_run_report_v63_offcenter_noisy.json`
- 状态: success
- 检测: 5/5 成功
- v63 指标: checks=5, refines=0, rejects=0, rescues=0, failures=0

4) 缺失资源注入
- 报告: `spotzoom_run_report_v63_missing_image.json`
- 状态: failed（符合预期）
- 错误: `Simulation image not found`

5) 网络异常注入（XPS不可达）
- 报告: `spotzoom_run_report_v63_xps_fail.json`
- 状态: failed（符合预期）
- 错误: `XPS controller connection failed`

### 3.3 页面访问 / 静态资源 / API 请求

- 采样文件: `artifacts/xps_http_check_v63.json`
- GET `/`: 200
- GET `/app.js`: 200
- GET `/styles.css`: 200
- GET `/api/features`: 200
- GET `/api/session`: 200
- GET `/api/unknown`: 404（预期）
- POST `/api/connect`（不可达 host）: 400（预期失败）
- POST `/api/controller/raw`（未连接）: 400（预期失败）
- POST `/api/disconnect`: 200

### 3.4 控制台日志 / 网络异常日志
- 服务端日志: `artifacts/xps_server_v63_stdout.log`
- 已观测到 timeout 与未连接错误被记录。

### 3.5 数据库读写
- 未发现项目内业务数据库（sqlite/db）；仅有若干 `Thumbs.db`。
- 本轮数据库读写项判定为 `N/A`（当前项目结构下无独立业务 DB 层）。

### 3.6 代码质量与结构
- `SpotZoom.py` 行数: 18665
- `SpotZoomController` 方法数: 79
- `SpotZoomController.__init__` 长度: 2131 行
- `except Exception` 次数: 127
- 结论: 模块耦合偏高、异常粒度偏粗，长期维护风险高。

## 4. 问题清单（优先级 / 影响模块 / 建议）

### P1
1) 统一 400 返回掩盖错误语义
- 模块: `XPS/xps_admin_console/server.py`
- 现象: `POST` 路径在广义 `except Exception` 下统一映射为 400。
- 影响: 前端与自动化难区分 timeout / 未连接 / 参数错误 / 内部异常。
- 建议: 细分为 408/409/422/500，并返回可机器解析 `error_code`。

2) SpotZoom 主文件过大且耦合高
- 模块: `8821L/SpotZoom.py`
- 现象: 单文件 18k+ 行，控制器构造函数 2k+ 行。
- 影响: 回归范围大、定位慢、增量功能容易相互影响。
- 建议: 先拆 detection pipeline / frontier gates / io drivers 三层。

### P2
3) 异常处理过宽（`except Exception` 高频）
- 模块: `8821L/SpotZoom.py`
- 影响: 真正缺陷易被吞并为“可继续”，降低可诊断性。
- 建议: 针对 I/O、数值、设备通信分层捕获并附错误码。

4) 前端静态页面存在乱码与标签破损
- 模块: `XPS/xps_admin_console/static/index.html`
- 现象: 中文乱码、`session-pill` 行闭合异常。
- 影响: 可读性与后续前端维护成本。
- 建议: 统一 UTF-8 文本修复，并做最小 HTML 校验。

5) PyTorch 可选依赖警告每次触发
- 模块: `SpotZoom_Machine_Learning/innovation_frontier_v17.py`
- 影响: 日志噪声偏大。
- 建议: 仅在首次加载打印一次，或增加 `--quiet-optional-deps`。

### P3
6) v63 覆盖场景不足
- 模块: `frontier v63`
- 现象: 当前样本中仅触发 accept，未触发 reject/rescue 分支。
- 建议: 增加极端漂移/遮挡回放样例，验证拒绝和救援路径稳定性。

7) 前端浏览器 console 自动采集未接入
- 模块: 自动化测试链
- 现象: 本轮主要依赖服务端日志与 HTTP 检查。
- 建议: 增加 Playwright 控制台监听，纳入固定 artifact。

## 5. 下一步优化方案（建议执行顺序）

1) API 错误语义化（P1）
- 目标: `server.py` POST 错误拆分 + `error_code` 标准化。

2) v63 压力回放用例（P3）
- 目标: 构造 occlusion/jump/stale-track 样本，覆盖 reject/rescue 分支。

3) SpotZoom 分层拆分（P1/P2）
- 目标: 先拆 `frontier_chain.py`，再拆 `controller_runtime.py`。

4) 前端编码和标记修复（P2）
- 目标: 清理乱码与异常标签，补基本静态检查。

5) 浏览器 console 自动采集（P3）
- 目标: 让页面级 JS 错误进入每轮报告。
