# AutoZoom — 自动光学变焦闭环测量系统

自动光学变焦（AutoZoom）实验的自动化闭环测量系统，通过 Python GUI 集成控制信号发生器、照明继电器、屏幕视觉角度检测、光谱数据采集与自动补焦，实现完整的循环测量流程。

## 系统架构

```
AutoZoom/
├── config_conditional_second_detect.py   # 默认参数配置
├── measurement_autofocus_shg_closed_loop.py  # 主程序：含自动补焦 + SHG 闭环的测量 GUI
├── measurement_with_focus_roi_metrics.py     # 简化版：含聚焦 ROI 指标的测量 GUI
│
├── control/                        # 硬件控制层
│   ├── illumination_relay.py       # 串口继电器 → 照明光开关
│   ├── signal_generator_rigol.py   # Rigol DG4062 信号发生器 (PyVISA)
│   ├── labview_tcp_server.py       # TCP Server ↔ LabVIEW 通信
│   ├── labview_tcp_server_1.py     # TCP Server 变体
│   ├── labview_tcp_server_binary.py# TCP Server 二进制协议版
│   └── labview_tcp.py              # TCP 客户端
│
├── vision/                         # 视觉检测层
│   ├── screen_capture.py           # 固定区域屏幕截图 (pyautogui)
│   ├── newpro_1.py                 # YOLO 角度检测 (ultralytics)
│   └── newpro_wan.py               # YOLO 角度检测变体
│
├── logic/                          # 业务逻辑层
│   └── angle_detect.py             # ScreenAngleDetector (截图+角度检测封装)
│
└── outputs/                        # 输出目录
    └── captured_frames/            # 循环测量截图保存
```

## 核心工作流程

每轮测量包含 12 个步骤的闭环流程：

| 步骤          | 操作                  | 说明                         |
| ----------- | ------------------- | -------------------------- |
| **Step 01** | 关闭照明光 → 等待稳定        | 消除照明光对基线采集的干扰              |
| **Step 02** | 发送 TCP `MEASURE` 命令 | LabVIEW 采集基线光谱，返回原始数据      |
| **Step 03** | 截屏 + YOLO 角度检测      | 检测"测量前角度"(angle\_before)   |
| **Step 04** | 打开照明光               | 为后续视觉检测提供照明                |
| **Step 05** | 打开信号发生器 CH1 (脉冲光)   | 脉冲时长可调，控制光学变焦量             |
| **Step 06** | 关闭照明光               | 准备光谱采集                     |
| **Step 07** | 等待 LabVIEW 就绪       | 等待 LabVIEW 准备采集激发后光谱       |
| **Step 08** | 发送 TCP `MEASURE` 命令 | LabVIEW 采集激发后光谱            |
| **Step 09** | 截屏 + YOLO 角度检测      | 检测"测量后角度"(angle\_after)    |
| **Step 10** | 光谱数据处理              | 高点滤除 → 中值滤波 → 峰值拟合 → Δw 计算 |
| **Step 11** | 自适应调节脉冲时长           | 根据 Δangle 调整下一轮 CH1 ON 时间  |
| **Step 12** | 保存数据                | 每轮 CSV + Excel 汇总          |

## 关键特性

### 1. 角度自适应闭环调节

- 根据当前角度与上一轮角度的差值（Δangle），自动调整信号发生器 CH1 的脉冲时长
- 目标 Δangle 范围可配置（默认 1° \~ 4°）
- Δangle 偏小 → 增大脉冲时长；Δangle 偏大 → 减小脉冲时长

### 2. 条件二次角度检测

- 当 YOLO 检测角度落在特定范围（如 0°\~20° 或 90°\~110°）时，自动启用二次检测
- 避免角度边界附近因噪声产生错误读数

### 3. 自动补焦 (Autofocus)

- 基于多种清晰度指标（Tenengrad、Laplacian、Brenner、高频比等）加权计算 FocusScore
- 低于阈值时自动触发 Newport Picomotor Z 轴闭环寻焦
- 支持 SHG（二次谐波）信号强度作为补焦触发条件

### 4. 光谱补焦循环 (Spectrum Autofocus Loop)

在 `0_measurement_workflow_real_virtual_same_detection_6.25.py` 的 GUI 左侧新增“9. 光谱补焦循环”控制区，实现光谱测量与被动补焦的长时间自动循环：

1. **手动框选 ROI**：点击“选择 ROI”，在弹出的 OpenCV 窗口中拖拽矩形选择关注区域；该截图同时作为后续补焦的基准图，并建立 Focus 参考基线。
2. **测光谱**：自动执行 `关照明 → LabVIEW 采集光谱 → 开照明 → 保存数据`。
3. **等待 2 分钟**。
4. **被动补焦检测**：按默认参数启动被动补焦（触发阈值 0.95、目标阈值 0.95、连续触发次数 3、hill_climb 策略、Z 轴号 1、间隔 1 秒、最大连续补焦 10 次、连续达标 5 次）。
   - 若 FocusScore 未达标，自动调用 Z 轴闭环搜索恢复聚焦；
   - 若 FocusScore 已达标，仅监测并在连续达标后进入下一轮；
   - 可随时点击“停止光谱补焦循环”优雅退出。
5. **循环**：补焦检测完成后自动回到第 2 步继续测光谱。

输出目录默认 `focus_output/`，每轮保存光谱摘要、FocusScore 历史 CSV 与 FocusScore 曲线 PNG。

核心逻辑封装在 [`spectrum_autofocus_loop.py`](spectrum_autofocus_loop.py)，主 GUI 仅做最小集成；对应测试见 [`test_spectrum_autofocus_loop.py`](test_spectrum_autofocus_loop.py)。

### 5. 光谱数据处理

- 从 LabVIEW 经 TCP 接收原始光谱数据
- 高点滤除（剔除高于阈值的数据点）
- 中值滤波平滑
- 峰值拟合与 Δw（半高宽变化量）计算

## 硬件配置

| 设备    | 型号                     | 通信方式                | 用途        |
| ----- | ---------------------- | ------------------- | --------- |
| 照明继电器 | -                      | 串口 (RS232, 9600bps) | 照明光开关控制   |
| 信号发生器 | Rigol DG4062           | USB (PyVISA)        | 双通道脉冲信号输出 |
| 光谱仪   | -                      | LabVIEW → TCP       | 光谱数据采集    |
| 运动控制器 | Newport 8742 Picomotor | USB (pylablib)      | Z 轴自动对焦   |
| 屏幕    | -                      | pyautogui 截图        | 角度读数视觉检测  |

## 配置参数

所有默认参数集中在 [`config_conditional_second_detect.py`](config_conditional_second_detect.py) 中，包括：

- **测量参数**：最大循环次数、脉冲时长、角度目标范围
- **照明光**：串口号
- **信号发生器**：VISA 地址、通道电压/频率/占空比/延时
- **角度检测**：YOLO 模型路径、截图区域、二次检测范围
- **TCP 通信**：IP 地址、端口号
- **光谱处理**：滤波参数、波长横坐标来源
- **自动补焦**：Z 轴参数、触发阈值、权重系数

## 快速开始

### 环境要求

- Python 3.9+
- Windows（硬件驱动依赖）

### 安装依赖

```bash
pip install ultralytics pyautogui pyvisa pylablib pyserial numpy matplotlib pillow openpyxl opencv-python PySide6==6.6.0
```

### 运行

```bash
# 完整版（含自动补焦 + SHG 闭环）
python measurement_autofocus_shg_closed_loop.py

# 简化版（含聚焦 ROI 指标）
python measurement_with_focus_roi_metrics.py
```

### 使用步骤

1. 启动程序，在 GUI 中配置参数（或直接使用默认参数）
2. 点击 **连接设备** — 自动连接照明继电器、信号发生器、加载 YOLO 模型
3. 点击 **启动 TCP Server** — 等待 LabVIEW 连接就绪
4. （可选）点击 **建立聚焦参考** — 采集多次 ROI 指标建立基线
5. 点击 **运行完整循环测量** — 开始自动闭环测量
6. 实时观察 GUI 日志、角度变化曲线和峰值变化曲线
7. 测量完成后，数据自动保存至 `measurement_output/save/MM.DD/` 目录

# TODO🎯

### 2026-7-25

在笔记本更新sam-main环境【环境：python3.12、torch2.5、固定prompt】	✔

```
那我能否通过修改sam2_env\pyvenv.cfg 来使用该环境呢？
我的系统的中也有python3.11 的版本，因为我安装了anaconda，位置在：E:\Anaconda
python -m pip install --upgrade pip --force-reinstall

```

将项目的输出日志实时保存至"./Log文件夹中"	✔

日志报错1：[前后两次，针对卐的面积识别超过了报错阈值，0.35-2.8 -- MeasurementConfig（line 381-382）]	✔

```
[2026-07-24 22:01:59] [ABC特征跟踪] 首帧 original_capture 失败 attempt=5/5: B mask 面积突变: old=18263.0, new=284400.0, ratio=15.57；等待下一帧重试。

```

找到设备型号的设置，功能 map 设备	✔

```
1、激光控制器为 12116：
    0_measurement_workflow_real_virtual_same_detection_7_25.py/MeasurementWorkflow/connect_laser_controller 函数内设置
        self.log("[激光开关] 正在连接 Newport 8743-CL / Picomotor")
        self.laser_stage = Newport.Picomotor8742(conn=r'8743-CL-12116', backend="network")

2、补焦控制器为 100100 
    Focus/config.py/AutofocusConfig
        # Picomotor 控制器连接参数（解决 conn=0 硬编码导致的连接失败）
        #z_picomotor_conn: int = 0
        z_picomotor_conn: str = r"8742-100100"
        """控制器索引（对应 Newport.Picomotor8742 的 conn 参数）"""

        # z_picomotor_backend: str = "auto"
        z_picomotor_backend: str = "network"
        """连接后端：auto / pyusb / serial"""

```

调整补焦参考图的逻辑	✔

```
1、要求"补焦基准图"的ROI区域选择在整个屏幕进行；
2、补焦画面范围是独立参考的，不影响"标定abc"、"角度检测"等画面的指定区域；
3、在 RuleAB 初始化路径中兼容无 B 点的情况；
4、要求"补焦基准图"的ROI区域选择之后，再进行下一步，不然容易卡死；
5、解决"补焦基准图"之后的第二个参考窗口问题，直接去除该预览图，有点多余；
6、每次点击"运行完整循环测量"后，都要重新进行一次"补焦基准图"的ROI选取,并为后续的补焦流程进行参考更新

```





### 2026-7-23

1、检测305 新设备环境，并验证测量光谱仪【光谱仪 + 电机 + 材料】	✔

2、等师姐来商量，然后去测试环境，主要还是一个sam-main 环境补充的问题【基于该环境直接安装即可】	✔

3、更新环境到另一台电脑	✔

4、在单次测量光谱的过程中，补充了1.5秒的延迟	✔

5、完成空间光调制器的操作使用、原理学习和代码调试，完成SLM软件的成像	✔

6、将"autofocus_qt_ui"已有的"补焦参数"，补充至"7.16版本"总ui界面，使得"光谱补焦循环"方便进行参数配置	✔





### 2026-7-18

1、在305电脑上同步后，先测试JY光谱仪原有的功能，再测试PI光谱仪的相关功能	✔

2、解决"循环测试"出问题的bug	✔

3、完成"视频局部视野切割"模块	✔

```
考虑到通过手动ROI划定局部视频，减少背景的噪声影响，理论上准确率会更高，所以再补充一个模块"离线数据集检测"
输入：视频
处理：
    通过手动ROI，确定需要检测的区域；
    支持拖动视频进度条，实时观察ROI区域的视频变化
输出：新视频（ROI剪切后区域视频）

```

4、更新到前述UI，将关灯阶段视频剪去，再输入视频，打开被动补焦，观察/记录FocusScore曲线	✔





### 2026-7-17

1、在原有系统基础上，补充一个设备选项（可选择当前光谱仪 与 PI光谱仪）	✔

2、自动补焦误差评估，将绝对值阈值设计补充到"触发阈值"中；		✔

3、再补充一个按钮"关闭达标阈值"，点击该按钮之后，补焦时不再考虑"连续达标次数"，只有手动"停止"才能关闭"被动补焦";	✔

4、再补充一个按钮"FocusScore检测"，点击该按钮之后，触发补焦时，仅记录本次触发补焦的图像，而不启动硬件相关的操作/报错；	✔

5、再补充一个模块"离线数据集检测"，该模块：	✔

```
输入：视频 或 文件夹（内部包含若干图片/视频帧）
处理：
    if 输入了视频，则对视频帧进行提取，每3秒提取一张图片，并将所有图片保存至文件夹中
        手动选择基准图片（若未选择，则默认为文件夹中的第一张图片），
        对后续的图片计算FocusScore得分，并将各个图片的得分标注在"图像内的正上方"
        同时，当FocusScore得分为0时，则删除对应的图片
    elif 输入了文件夹，则直接
        手动选择基准图片（若未选择，则默认为文件夹中的第一张图片），
        对后续的图片计算FocusScore得分，并将各个图片的得分标注在"图像内的正上方"
        同时，当FocusScore得分为0时，则删除对应的图片
输出：新建一个文件夹（内部包含若干用FocusScore得分标注好的图片/视频帧）

```

 		





### 2026-7-16

```
JY光谱仪通信原理：原系统用Python发送命令，Labview TCP中间层架构进行光谱仪处理，并反馈相关数据至python UI界面	✔	
Pi光谱仪的系统代码更新 -- https://www.spark-opt.com.cn/product-item-202.html		✔
基于PyQt 和 PICam SDK完成一个PI光谱仪的测试UI（暂不需要.\PrincetonInstruments\Project）	✔
LightField 操作步骤：	✔
	1、点击两个开关；
    2、设置温度为 -25℃，等待温度降低至-25℃后，开始锁定locked；
    3、设置On-CCD Accumulations 为1000；
    4、设置GATING中，Gate Delay 为250ns；Gate Width 为10ms；
    5、Center Wavelength 为516nm。

找到PI SDK与UI界面对接，在本地完成原有系统的"选项"补充，并上传至github	✔
解决在"照明光"串口更新后，后台没有反应的问题，比如：默认是com20，手动改成com19后，结果仍是找串口com20	✔
触发阈值改为"绝对值±5%"，比如进行补焦时，触发阈值为0.95，计算Focus_Score小于0.95 或者大于1.05了，也要补焦	✔

```



### 2026-7-15

下次需要push大量文件时，一定先设计好gitignore，不然有我好受的T.T	✔

环境配置更新：pip install pyserial pylablib pyvisa pyvisa-py		✔

同步补焦效果代码，至我的笔记本	✔

学习光谱仪的使用流程与大致原理	✔

```
1、系统安装：笔记本连接硬件设备，安装软件程序LightField
2、启动官方软件：LightField启动与配置
3、冷却CCD，选择工作模式【本场景直接使用光谱模式】：
    成像模式：用于空间分辨测量【关注图像的空间分布，等离子体形态、荧光分布、燃烧场观测】
    光谱模式：配合IsoPlane 光谱仪进行光谱分析【波长测量、成分分析、发生光谱、吸收光谱】
    门控模式：使用supersynchro时序发生器进行纳秒级门控【捕捉瞬态：爆炸、放电脉冲等】
4、配置Intensifier
    选择增益模式（Gen II/GEN III filmless）【类似于优化器】
    设置波长范围（UV/SB/RB/SR/HBf/InGaAs）【按激光实际波长确定】
    设置门宽			【快门打开的时间长度，探测器实际的感光时间】
5、数据采集
    设置曝光时间 -- 电荷积分的时间窗口	 【采集持续时间】
    设置门宽 -- 探测器实际感光的时间	  【实际感光时间】
    设置 SuperSynchro时序			【精确控制合适开关门】
    执行采集 -- 单帧、视频采集、Kinetics高速采帧
6、LightField：
    相机控制软件，光谱系统的操作系统，用于操控相机、设置参数、显示图像以及保存数据
    主要控制包括：CCD相机、光谱仪、像增强器、时序控制器
7、为何要冷却CCD：
    低温抑制暗电流，并降低读出噪声，提高信噪比和灵敏度，不然会淹没真实信号
    低温下，电子迁移率更稳定，像素间的固定图案噪声FPN 和 随机噪声降低，
    从而让光谱仪能够看清更微弱的信号 

```

完成Pi光谱仪的系统代码更新 -- https://www.spark-opt.com.cn/product-item-202.html

```
【目标：在原有系统基础上，补充一个设备选项，统一光谱仪的输入与输出即可】
分析是否能用python -- 可直接基于python进行通信	✔
安装PI-LightField	-- emlCCD、PI-MAX4控制与图像采集	✔
安装ISOPLANEControl -- 专门控制IsoPlane SCT320 成像光谱仪调参、结合相机采集光谱	✔
* 安装MonoControl	-- 控制Acton单色仪的软件（包括IsoPlane）调参、结合相机采集光谱
* 安装"海洋控制器"
【* 表示，后续再安装，目前先实现IsoPLane SCT320 设备的功能闭环吧】
【参数：设置输出波长、切换光栅、控制滤光片轮、复合光拆分单色光输出到样品、CCD相机通信】

```





### 2026-7-14

标记"补焦模式（主动变被动）"检查点		 ✔

pip install mss						✔

照明光控制的串口：从 COM17 更新为 COM20	✔

默认硬件从"virtual"改成"real"			✔

优化"YOLO-OBB长边角度检测"的默认模型为：.\vision\best_wan12.2.pt	✔

同时，截图区域 与 "光谱补焦循环"中的手动选择的ROI区域同步更新			✔

优化一轮系统程序，尽快完成一轮闭环，步骤如下：							✔

```	
0、手动截取ROI截图，作为进行操作的图像区域，同时作为"补焦"的基准图
1、测光谱：关照明 - labview测光谱 - 开照明光 - 保存数据
2、等待2min
3、启动"补焦检测"：
    默认参数：触发阈值：0.95；目标阈值：0.95；连续触发次数：3；搜索策略：hill_climb；Z轴号：1；Z控制器索引：0；Z控制器后端：auto；
    间隔（s）：1；"被动补焦"；最大连续补焦次数：10；连续达标次数：5、输出目录：focus_output（同时补充对应的FocusScore曲线）。
   if 没到触发阈值 then 跳过本步骤；
   if 超过阈值 then 进行补焦调整。
4、回到第1步继续测光谱
```

落地测试，完成变焦的闭环反馈【今日接入系统有一定的风险，所以将“加入对比图”作为备份点】		✔

将主动技能转成被动技能	✔

```
为"循环参数"设计一个配置，用于在后台循环进行补焦，直到FocusScore曲线通过"触发阈值"；
其中，设置一个"启用被动补焦"的选项，勾选该选项后，不再参考"循环轮数"（主动变被动），仅考虑"间隔"参数。
结束条件1：设置"最大达标次数"n：若当前检测的FocusScore连续n次大于"触发阈值"，则停止补焦；
结束条件2：点击"停止"按钮，手动停止补焦。

```

接入当前系统【如何高效地插入该功能模块】	✔

```
参考程序“0_measurement_workflow_real_virtual_same_detection_6.25.py”，优化"自动对焦"的效果
同时，该程序的步骤如下：
'''
    单轮循环测量流程：
    Step 1：角度检测一次，得到 current_angle
    Step 2：生成保存路径
    Step 3：照明光 OFF，并按 stable_wait_ms 等待稳定
    Step 4：LabVIEW 光谱采集
    Step 5：照明光 ON
    Step 5.5：保存本轮数据；保存角度使用 Step1 的 YOLO-OBB baseline 原始角度
    Step 6：打开激光
    Step 7：A推动B(logic/rule_ab.py)，每次运动后检测B对边角度，Step7角度只用于关闭激光/是否继续推动，不再覆盖本轮保存角度
    Step 8：检测颜色区域中心，与提前选定位置对齐；偏离过大则移动1/2通道
'''

```

在尽量不影响其他逻辑的前提下，将本项目的"自动对焦"的程序补充当前步骤的“Step 2”之后，完成"自动对焦"模块的补充，并生成一个新

的“0_measurement_workflow_real_virtual_same_detection_7_10.py”程序	✔

将当前对焦系统，写入师姐的系统中	✔

先解决代码更新问题，然后再继续进行优化【后续所有代码，均在本项目基础上实现，届时在304主机再缺啥补啥】	✔


完成笔记项目代码更新，再更新PI光谱仪的通信API	✔



### 2026-7-20

1、补焦区域画面解耦					✔

- 在 `0_measurement_workflow_real_virtual_same_detection_7_16.py` 中，将光谱补焦循环（SAF）的截图区域与 ROI 从标定、角度检测等主流程画面中解耦。
- `MeasurementConfig` 新增独立字段 `saf_capture_area` / `saf_focus_roi`；`__post_init__` 默认复制自主 `capture_area` / `focus_roi`，兼容旧配置。
- UI“9. 光谱补焦循环”面板新增“补焦截图区域”输入框；选择 ROI 后仅更新补焦专用区域，不再修改主 `capture_area`。
- `_make_saf_config()` 与 `_make_focus_config()` 均改用 `saf_*` 字段，确保标定/角度检测使用的主区域不受补焦区域影响。
- 更新并扩展 `test_spectrum_autofocus_loop.py`，新增 3 个解耦相关源码检查用例，全部通过。

### 2026-7-13

1、光谱补焦循环					✔

- 在 `0_measurement_workflow_real_virtual_same_detection_6.25.py` 新增“9. 光谱补焦循环”控制区（选择 ROI、开始/停止循环、状态显示）。
- 新增独立模块 `spectrum_autofocus_loop.py`，封装 ROI 交互选择、单图参考建立、光谱测量、被动补焦检测、FocusScore 曲线保存。
- `Focus/scorer.py` 新增 `build_reference_from_image()`，支持以单张已有图像建立 Focus 参考基线。
- 新增测试 `test_spectrum_autofocus_loop.py`，9 个用例全部通过。
- 回归测试 `test_passive_mode.py` 全部通过，GUI 文件通过 `py_compile` 语法检查。

### 2026-7-11

1、设置默认"触发阈值"为 0.95		✔

2、目标阈值_含义					✔

​		闭环补焦搜索停止并认为聚焦已恢复时的 FocusScore_ratio；达到该值即停止 Z 轴移动，

​		且为了防止逻辑混乱，触发阈值与目标阈值建议保持一致；

3、问题讨论						✔

​	FocusScore是否仅与图像清晰度相关，而与基准图中的物体形状关系不大？

​		>> 部分相关，部分无关，其中：默认指标（highfreq/tenengrad/brenner）主要反映清晰度，

​		但 red_blue_ratio 与清晰度无关；且所有指标都基于 ROI 内容计算，样品形状、位置、照明变化都会干扰分数。

​	大于1是否说明当前图像相较于之前还更加清晰？还是存在一定的BUG之处？

​		>> 通常是的，但可能由噪声、对比度增强、过曝、样品结构变化等非聚焦因素导致，不能等同于光学聚焦绝对更优。







### 2026-7-10

1、落地测试，完成变焦的闭环反馈
解决"采集源"为"屏幕区域"时下述问题：

（1）默认截图区域、ROI区域为当前设备的全屏幕（比如：1920*1080） ✔*

*（2）“实时预览”应该实时获取当前的画面，而不是画面帧 ✔*

*（3）优化屏幕区域的ROI 取图功能 ✔*

*“建立参考”：抓取当前画面作为清晰聚焦的"参考基准"。程序多次采集并计算最优指标，作为后续自动聚焦的对比目标 ✔*

*"启动闭环"：开始自动聚焦循环，每轮自动截图 → 对比参考基准 → 若偏离则自动调节 Z 轴恢复清晰聚焦 ✔
当前我在点击"启动闭环"之后，会出现闪退的情况 ✔*

*解决闭环控制中的清晰度问题 ✔
而当前的问题是：点击"启动闭环"，循环结束之后，无法重新进行"启动闭环"。要求在“启动闭环”，直到循环结束后，应在日志中返回循环结束，同时"结束"按钮可以点动，并且支持再次"建立参考"或者"启动闭环"的操作 ✔*

*设定默认的补焦时间为 1.5秒 ✔*

*设定默认的循环次数为30次 ✔*

*保存已通过闭环的"自动对焦V1" ✔*

*在"启动闭环"之后，在"实时预览"中加入“基准图”和当前"实时图"的对比，方便人眼比较，并且在最终循环结束之后，弹出一个对话框，用于显示2*2个从头到尾的图片变化过程

2、自动对焦核心逻辑 ✔

（1）设置参考图像，计算图片14项聚焦指标

（2）对显微镜头图像进行实时预览，计算聚焦评分

（3）设置阈值（评分+轮次）触发补焦

（4）设计4种闭环搜索：hillclimb、fullsweep、CurveFit、GoldenSection完成闭环

<br />

### 2026-7-8

1、完成剖面线实时预览

对"实时预览"中的光斑计算剖面图，计算RGB三原色各自的像素值，并提供横线和竖线，用以绘制线上的剖面图，实现光斑的聚焦程度量化	✔

补充一个本地视频的采集源	✔

设计默认主题为"浅色模式"	✔

“采集区域”的逻辑更改：输入视频 or 导入视频后，通过手动设置ROI，该区域自动识别到相关参数（ROI区域、截图区域、视频分辨率、帧率等信息）	✔

"实时预览"中，播放器要求实时播放视频，并提供原视频的进度条拉取功能									✔

再将"聚焦指标"、"FocusScore曲线"、"剖面图"，移动至"实时预览"下方，并将原来的日志信息框移动到其他地方	  ✔

在"实时预览"窗口中，设置"竖线剖面"、"横线剖面"的可选框，进而控制两根线是否出现在视频上				   ✔

在"剖面图"的实时图形中，通过鼠标悬停，支持实时查看某一点的像素坐标(r,g,b,x,y)					    ✔

补充简单的光斑检测识别算法（基于机器视觉），识别到光斑后自动进行剖面图的更新，同时也支持手动选择光斑（应对识别多个光斑的问题）	✔

为自动检测光斑的算法类型（bright、otsu等）、参数等配置，在左侧的容器中也单独加入一个模块			 ✔

要求用户能控制"实时预览"中的播放器大小比例，可以通过拉动容器的边界线，实现播放器的比例大小的动态调整	  ✔

补充左侧容器系统的下划边栏，用于查看超过屏幕大小的UI部分										 ✔

要求选中ROI之后，"实时预览"中对的播放器，仅播放/识别光斑/ROI框中框取的内容					 	  ✔

在识别到多个光斑后，对每个光斑的部分区域进行小范围截图，然后与对话框一起供用户选择					✔

要求在右侧容器处，也补充系统的下划边栏，用于查看超过屏幕大小的UI部分				   				 ✔

当前ROI手动选取的截图，并不是实际截取的位置，请继续修复、优化、测试ROI截图的功能					  ✔

“FocusScore曲线” 和 “聚焦指标”依旧没有反馈对应的内容，请继续修复、优化、测试这个问题				✔

去除的"运行模型"之一的"模拟模式(无硬件)"														✔

继续优化“FocusScore曲线”，并为用户提供更多的提示信息										  ✔

在"剖面图"的实时图形中，通过鼠标悬停，支持实时查看某一点的像素坐标(r,g,b,x,y)，当前的显示信息缺失了"b"	✔

补充实时光斑跟随机制（光斑检测+坐标定位 -- 强度加权质心算法：削减背景+强度加权+计算重心）			 ✔

```
1、解决光斑移动后，横竖线不跟上光斑的问题（在用户选中需要检测的光斑后，要求横线剖面、竖线剖面的交点，实时跟随该光斑进行移动）
2、已有动态跟随"光斑"的趋势了，但仍缺乏实时性，需要解决存在延误的问题（需要多次手动点击"自动检测光斑"才能快速回正）
3、保证全视频光斑检测的高效性，保证系统流畅运行 
4、优化"光斑"跟踪的精度，保证每一帧都能顺利检测到真正的光斑 
5、保证ROI选取屏幕前后，动态"光斑"检测的准确性 

```

2、实现功能总结	✔

```
1、运动模式：支持本地视频文件、USB相机、屏幕区域、窗口ROI
    USB设备：自动检测相关成像设备，用以选择
    本地视频文件：用于导入实现准备好的光斑变焦前后的演示视频
    屏幕区域：用于实时检测当前屏幕中的区域，比较万能
    窗口ROI：自动检测桌面当前打开的窗口（不包括最小化的窗口）
2、采集区域：
    用于自动检测"实时预览"区域的ROI 和 截图像素区域；
    提供横竖剖面在"实时预览"区域的可视化情况
    提供自动检测光斑的设置，用于用户自主选择检测到的光斑（若仅检测到一种光斑，则跳过用户选择过程，因为没得选~）
3、光斑检测：用于自动检测到光斑，提供算法选择（暂时仅支持机器视觉算法，后续会补充深度学习模型）、参数调整
4、补焦参数：用于显微镜镜头，进行自动补焦的参数配置
5、循环参数：用于定时检测当前的"补焦情况"
5、实时预览：用于可视化当前的镜头情况，并提供：
    可拖动的横竖剖线、手动ROI屏幕划取、屏幕缩放、视频帧信息读取、聚焦指标、剖面图、Focus得分曲线(用于实际补焦闭环)、运行日志等信息

```

3、聚焦指标-含义	✔

```
均用于"评估图像清晰度/聚焦质量"，数值越大通常代表越清晰（ edge_width 和 halo_width 除外），如下：
    1 tenengrad 		梯度能量总和 对灰度图做 Sobel 算子求 x/y 方向梯度，计算 Σ(gx² + gy²) 值越大，边缘越锐利，图像越清晰 
    2 laplacian_var 	拉普拉斯方差 对灰度图做 Laplacian 二阶微分，计算方差 聚焦良好时高频细节多，方差大；失焦时趋近 0 
    3 brenner Brenner 	梯度 计算每个像素与右侧第 2 个像素的差值平方和： Σ(I(x+2,y) - I(x,y))² 经典对焦指标，值越大越清晰 
    4 highfreq_ratio 	高频能量占比 FFT 变换后，高频区域能量 / 总能量 反映图像中细节/纹理的比例，失焦时高频被滤除，比值下降 
    5 local_contrast 	局部对比度 将图像分块，计算每块标准差的均值 局部反差越大，纹理越丰富，图像越清晰 
    6 edge_width 		边缘宽度（像素）检测图像中最显著的阶跃边缘，范围：10%~90%，越小越清晰 
    7 halo_width 		光晕/振铃宽度 检测边缘附近的过冲/欠冲范围，值越小越好 
    8 brightness_mean 	平均亮度 灰度图所有像素的均值 反映整体曝光水平，39.9 偏暗（0-255 范围） 
    9 brightness_std 	亮度标准差 灰度图像素值的标准差 反映亮度分布的离散程度，值大说明画面明暗对比丰富

```

### 2026-7-7

1、对"实时预览"中的光斑计算剖面图，计算RGB三原色各自的像素值，并提供横线和竖线，用以绘制线上的剖面图，实现光斑的聚焦程度量化	✔

2、补充一个本地视频的采集源	✔

3、完成光斑刨面图的视频采集	✔

### 2026-7-6

1、梳理原项目--基本流程：  ✔

```
	（1）自定义位置，截取屏幕指定区域
    （2）针对截取图片，计算10项聚焦指标（整图&ROI）
    （3）建立聚焦参考基线
    （4）再计算FocusScore_ratio
     即手动调整显微镜至聚焦状态，连续截图n次（默认5次）
     每次计算 10项聚焦指标
     对每项指标取 n次的均值，并存入roi_metric_ref
     后续，将"当前图片的实时评分" 与 "聚焦图片的最佳评分"roi_metric_ref进行比较
     即为 FocusScore_ratio
    （5）评估补焦触发条件
     自动补焦的高层编排器

```

2、基于开源项目的尝试 	✔

```
DeepFocu：Micro-Manager 2.0gamma API + java + python-grpc + imageJ，显微镜自动对焦步骤理解，步骤：
     0. 补充java环境
     1. 加载标定曲线 (.pickle)
     2. 用户在 ImageJ 选 ROI（或自动 2D/3D ROI）
     3. 在当前 Z 位置拍第一张图，发给 Python
     4. Python 用 U-Net 计算 focus map
     5. 用黄金分割搜索决定下一个 Z 位置
     6. Java 移动 Z 轴，拍新图，回传
     7. 重复直到采集到 max_iter 张图
     8. 对采集到的小样本做曲线相关，估计每个像素的最佳 Z
     9. 对 ROI 内所有像素的最佳 Z 做 MeanShift 聚类，得到最终最佳 Z
     10. 移动 Z 轴到最终位置
 【由于该项目年代较为久远，故不浪费时间进行理解与复现，直接使用当前最前沿 or 最落地的技术栈来实现】

```

3、补充了对应的qt UI界面（一般直接基于项目解析后，再基于PYQT生成UI界面即可，但注意强调新增的模块只仅次于XXX项目目录内）	✔

4、优化UI界面的四种画面通信方式	✔
