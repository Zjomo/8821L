# SpotZoom v78 综合检测报告（2026-05-18）

## 1. 本轮目标与代码补充

- 目标：基于 v77 继续补充 `SpotZoom.py`，引入新的前沿可借鉴守卫模块，并完成构建、启动、页面访问、核心功能流程、接口请求、数据库读写、前端控制台、网络异常、静态资源与代码质量自动检查。
- 新增模块：`frontier_v78_consistency_bandit_guard`（一致性-探索守卫）。
  - 触发条件：`v77` 出现触发但未 refine，或出现 reject 信号。
  - 核心策略：
    - 压力帧 streak 驱动的动态搜索半径扩展；
    - 模板匹配 + 相位相关双候选融合（template/phase）；
    - 预测一致性门控（consensus gate）与可靠度分层阈值；
    - 最大位移约束与置信度重标定。
  - 接线范围：默认配置、open-source bundle、`AlignmentConfig`、运行时状态、指标埋点、CLI 参数、参数校验、v57 主关联流水线。

## 2. 前沿类似项目调研与可模仿模块

1. CoTracker3（Meta FAIR）
- 链接：<https://github.com/facebookresearch/co-tracker>
- 可模仿点：长序列点跟踪中的时序一致性与在线鲁棒性。
- 映射：v78 在压力帧下引入“预测一致性+门控”二级判定。

2. TAPNext++ / TAPNet（Google DeepMind）
- 链接：<https://github.com/google-deepmind/tapnet>
- 可模仿点：点级跟踪的稳定关联与误差累积抑制。
- 映射：v78 将模板与相位候选做多线索融合，降低单信号漂移风险。

3. Track-On
- 链接：<https://github.com/gorkaydemir/track_on>
- 可模仿点：在线点跟踪中 verifier-guided 自适应策略。
- 映射：v78 以 `v77` reject/无效触发作为“验证失败信号”驱动探索半径调度。

4. ByteTrack
- 链接：<https://github.com/FoundationVision/ByteTrack>
- 可模仿点：低分候选不直接丢弃，而是进入二阶段关联。
- 映射：v78 对低可靠但可解释候选采用 relaxed threshold 再判定。

5. OC-SORT
- 链接：<https://github.com/noahcao/OC_SORT>
- 可模仿点：观测中心的运动一致性约束，减少运动模型漂移。
- 映射：v78 将预测点一致性误差纳入硬门控（consensus reject）。

6. Norfair
- 链接：<https://github.com/tryolabs/norfair>
- 可模仿点：轻量距离函数与跟踪状态管理。
- 映射：v78 通过可配权重融合 template/phase 两个候选，提高策略可调性。

7. OpenCV 官方文档
- `phaseCorrelate`：<https://docs.opencv.org/4.x/d7/df3/group__imgproc__motion.html>
- Template Matching：<https://docs.opencv.org/4.x/de/da9/tutorial_template_matching.html>
- 映射：v78 继续采用局部模板匹配与相位相关组成双证据通道。

## 3. 自动化检查执行项

- 构建/静态检查
  - `python -m py_compile 8821L/SpotZoom.py`
  - `python -m compileall 8821L/SpotZoom.py 8821L/SpotZoom_Machine_Learning`
  - `python -m py_compile XPS/xps_admin_console/server.py`
  - `node --check XPS/xps_admin_console/static/app.js`
- SpotZoom 回放矩阵（6 case）
  - `smoke / offcenter_noisy / probe / reject_probe / missing_image / xps_fail`
- `reject_probe` AB 复核
  - baseline=`v77`，enabled=`v78`，各 3 次
- XPS 控制台端到端
  - 页面/静态资源/API/异常接口请求
  - Playwright 抓取 `console/pageErrors/requestFailed`
  - DB 读写路径覆盖（not-connected 分支）
- 代码质量与结构统计
  - LOC、函数规模、长函数数量、耦合信号

## 4. 本轮检测结果

### 4.1 SpotZoom case matrix（v78）

- 汇总：`total=6, success=4, failed=2`
- 成功：`smoke / offcenter_noisy / probe / reject_probe`
- 失败：`missing_image / xps_fail`
  - 均为预期失败路径（缺图、控制器不可达）。

### 4.2 v78 指标表现

- 汇总指标：
  - `frontier_v78_triggered=5`
  - `frontier_v78_guard_refines=0`
  - `frontier_v78_guard_rejects=2`
  - `frontier_v78_consensus_rejects=2`
- 观察：v78 已按设计介入，但本轮依旧保守，未产生有效 refine；在 `reject_probe` 出现 2 次一致性拒绝。
- 额外调参探测：在 `reject_probe` 上执行 16 组 (`exploration_gain / reliability_floor / consensus_gate / template_weight`) 快速扫描，`guard_refines` 仍为 0，且部分组合抬升了对齐轮次与拒绝率。

### 4.3 reject_probe AB 复核

- `baseline_v77`: `2/3 success`
- `enabled_v78`: `2/3 success`
- 结论：本轮未观察到统计改善，v78 目前属于“可控但未增益”状态。

### 4.4 页面/API/网络/静态资源

- HTTP 状态分布：`200x7, 400x3, 404x2, 413x1`
- 页面与静态资源可访问：`/`, `/static/app.js`, `/static/styles.css`
- 异常请求按预期命中：`/api/connect`, `/api/controller/raw`, `/api/files/upload`

### 4.5 前端控制台与请求失败

- Playwright：
  - `console=0`
  - `pageErrors=0`
  - `requestFailed=0`

### 4.6 数据库读写路径

- `read_stages_db` / `read_system_config`：HTTP 200，返回 `Not connected` 分支。
- 写入路径：
  - 非法 base64 -> 400
  - 合法 payload + 未连接 -> 400
- 结论：connected 分支仍未覆盖。

### 4.7 代码质量与结构

- `SpotZoom.py`：`25582 LOC`、`270 functions`、`>=200 行函数 21 个`
- 耦合信号：`self.cfg.` 引用 `1553` 次，`self._inc_metric(...)` `519` 次
- 结构风险：`parse_args` / `__init__` / `check_environment` 仍超长，回归扩散面持续增大。

## 5. 问题优先级、影响模块、修复建议

### P1：v78 已触发但 0 refine（收益未落地）
- 影响模块：`_apply_frontier_v78_consistency_bandit_guard`
- 证据：`frontier_v78_guard_refines=0`
- 建议：
  - 下调 `--frontier-v78-reliability-floor`，并放宽 `--frontier-v78-consensus-gate-px` 做网格搜索；
  - 将 `template_weight` 与噪声/运动一致性联动（而非常量）；
  - 对 `shift_norm < 0.20` 的软接受阈值引入自适应比例。

### P1：单文件耦合继续上升
- 影响模块：`SpotZoom.py` 主流程、参数层、校验层
- 证据：LOC/函数规模/配置与指标引用持续增长
- 建议：
  - 将 v73-v78 guard 抽成独立策略子模块；
  - 参数定义与校验拆为 schema + validator；
  - 用统一 guard 接口减少流水线硬编码。

### P2：AB 无显著改善
- 影响模块：v57-v78 关联链路
- 证据：`baseline_v77=2/3`，`enabled_v78=2/3`
- 建议：固定随机种子、扩大样本 (`N>=30`)，统计 p95/p99 失败率与对齐轮次。

### P2：DB connected 分支覆盖缺失
- 影响模块：`/api/system/*`、`/api/files/upload`
- 建议：引入可连控制器或仿真控制器，补齐 connected 分支回归基线。

## 6. 本轮产物

- `8821L/SpotZoom.py`
- `8821L/artifacts/spotzoom_help_v78.txt`
- `8821L/artifacts/spotzoom_case_matrix_v78.json`
- `8821L/artifacts/spotzoom_case_metrics_v78.json`
- `8821L/artifacts/spotzoom_run_summary_v78.json`
- `8821L/artifacts/spotzoom_v78_ab_reject_probe.json`
- `8821L/artifacts/spotzoom_v78_ab_reject_probe_summary.json`
- `8821L/artifacts/xps_http_check_v78.json`
- `8821L/artifacts/xps_db_check_v78.json`
- `8821L/artifacts/xps_server_v78_stdout.log`
- `8821L/artifacts/xps_server_v78_stderr.log`
- `8821L/artifacts/spotzoom_code_quality_v78.json`
- `8821L/SpotZoom_v78_综合检测报告_20260518.md`

## 7. 下一步优化方案

1. 对 v78 做针对性网格搜索（`floor/gate/template_weight/max_shift`）并以 `guard_refines` 为主目标、误修正率为约束。
2. 扩充 `reject_probe` 压力集并固定随机种子，建立可复现实验基线。
3. 将 v73-v78 guard 抽象为独立模块，先拆参数层与校验层，降低回归爆炸半径。
4. 在 connected 环境补齐 DB 读写闭环回归，形成 API + DB 联动健康基线。
