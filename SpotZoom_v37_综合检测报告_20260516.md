# SpotZoom v37 综合检测与优化报告（2026-05-16）

## 1. 本轮代码补充

- 新增模块：`8821L/SpotZoom_Machine_Learning/innovation_frontier_v37.py`
  - 模块名：trajectory-consensus association gate
  - 主要机制：
    - 观测中心速度 + 加速度预测（OC-SORT 风格）
    - 漂移速度估计与门控补偿（BoT-SORT/GMC 思路）
    - 轨迹方向一致性系数（coherence）
    - 温度门控（temperature）和滞回桥接（hysteresis bridge）
- 已接入文件：`8821L/SpotZoom.py`
  - 动态导入/fallback
  - 默认配置、`AlignmentConfig` 字段、CLI 参数
  - `check_environment` 参数校验与模块可用性告警
  - 运行时初始化、平滑链路调用、reset 生命周期
  - `RuntimeMetrics` 与 run report 指标输出

## 2. 互联网前沿同类项目调研（可模仿模块）

1. ByteTrack（https://arxiv.org/abs/2110.06864）
- 可模仿：高低置信检测分层关联（低分框不立即丢弃）

2. BoT-SORT（https://arxiv.org/abs/2206.14651, https://github.com/NirAharon/BoT-SORT）
- 可模仿：相机/视场运动补偿 + 自适应关联门控

3. OC-SORT（https://github.com/noahcao/OC_SORT）
- 可模仿：Observation-Centric 速度更新，提高遮挡与抖动下稳定性

4. CoTracker3（https://arxiv.org/abs/2410.11831, https://github.com/facebookresearch/co-tracker）
- 可模仿：长时轨迹一致性（online/offline 轨迹连续约束）

5. Norfair（https://tryolabs.github.io/norfair/2.1/reference/tracker/）
- 可模仿：hit counter 生命周期管理 + 距离阈值关联

6. Ultralytics Tracking（https://docs.ultralytics.com/modes/track/）
- 可模仿：工程化暴露 tracker 参数（高/低阈值、缓冲、门控）

## 3. 自动化检查结果

### 3.1 构建与启动
- `python -m py_compile SpotZoom.py`：通过
- `python -m py_compile SpotZoom_Machine_Learning/innovation_frontier_v37.py`：通过
- `run_spotzoom.bat --check-env --frontier-v37-assoc-gate`：通过
- 环境告警：`pylablib` 间接依赖 `pkg_resources` 的 deprecation warning

### 3.2 核心功能流程（dryrun + classic + simulated frame）
- center：`success`（`artifacts/run_report_center_v37.json`）
- offcenter：`success`（`artifacts/run_report_offcenter_v37.json`）
- black：`failed`（预期内，初始检测为空；`artifacts/run_report_black_v37.json`）
- offcenter+jitter：`success`（`artifacts/run_report_offcenter_jitter_v37.json`）

### 3.3 A/B 抖动对比（6 轮）
- 结果文件：`artifacts/ab_v37_summary.json`
- v36 baseline：
  - avg align_rounds = `3.833333`
  - avg x_move_commands = `3.333333`
  - avg y_move_commands = `2.0`
- v37 enabled：
  - avg align_rounds = `6.666667`
  - avg x_move_commands = `2.666667`
  - avg y_move_commands = `5.833333`
  - avg frontier_v37_assoc_rejects = `0.0`
  - avg frontier_v37_assoc_bridge_hits = `0.166667`

结论：v37 在本轮随机抖动样本中未显著提升整体收敛效率，且出现高方差（单次 `align_rounds=29`）。

### 3.4 页面访问 / 接口请求 / 网络异常 / 静态资源
- 结果文件：`XPS/xps_admin_console/artifacts/xps_http_check_v37.json`
- 页面与静态资源：`/`、`/app.js`、`/styles.css` 均 `200`
- API：`/api/features`、`/api/session`、`/api/system/stages-db` 均 `200`
- 异常路径：
  - `/api/unknown` -> `404`
  - `/api/connect`（不可达 host / 非法 payload）-> `400`
  - 大包体请求 -> `413`

### 3.5 数据库读写
- 本项目主链路无独立 DB 持久层；XPS 管理台以控制器会话与文件接口为主。
- “读”侧：`/api/system/stages-db` 返回 `200`（未连接时返回错误消息体）
- “写”侧：`/api/files/write` 未连接状态返回 `400`

### 3.6 前端控制台日志
- `playwright` 不可用（`PLAYWRIGHT_MISSING`），本轮无法自动抓取浏览器 console。
- 替代检查：`node --check static/app.js` 通过；静态资源与接口链路可达性已覆盖。

### 3.7 代码质量与模块结构
- `flake8`（E9/F63/F7/F82）通过。
- `pylint --enable=E,F`：存在大量 `cv2` no-member、可选硬件依赖 import-error、动态导入误报。
- 结构分析文件：`artifacts/code_quality_v37.json`
  - SpotZoom.py：`13514` 行，`22` 类，`209` 函数
  - 高风险热点：
    - `check_environment`：642 行 / 423 分支
    - `SpotZoomController.__init__`：1361 行 / 240 分支
    - `parse_args`：1734 行
  - 重复逻辑信号：
    - `self._last_detection = detection`：17 次
    - `self._track_miss_streak = 0`：17 次

## 4. 问题分级（优先级 / 影响模块 / 修复建议）

### P1
1. v37 抖动场景方差偏大，收敛回合出现长尾
- 影响模块：`innovation_frontier_v37.py`、`SpotZoomController._smooth_detection`
- 建议：
  - 加入 deterministic 回归种子与固定扰动序列
  - 增加 `coherence/temperature` 上下限与 reject 触发的硬约束
  - 将 v37 维持默认关闭，仅作为实验开关

2. 单体脚本耦合持续升高，回归风险扩大
- 影响模块：`SpotZoom.py` 全局
- 建议：优先拆分 `parse_args`、`check_environment`、`frontier smoothing orchestration`

### P2
1. API 错误语义偏粗（连接失败统一 400）
- 影响模块：`XPS/xps_admin_console/server.py`
- 建议：细分 408/409/504 等状态码，减少调用端误判

2. 前端可观测性不足（无 console 自动采集）
- 影响模块：`XPS/xps_admin_console/static` 运行态质量
- 建议：补装 Playwright 后增加 console error/warn 断言

### P3
1. `pylablib/pkg_resources` 警告噪声
- 影响模块：环境检查日志可读性
- 建议：在环境探针中分级展示第三方 deprecation 警告

## 5. 下一步优化方案

1. 为 jitter 回归新增 deterministic 模式（固定扰动种子 + 固定序列）并重复评估 v36/v37。
2. 对 v37 增加“高温降级”路径：当温度和 reject_streak 超阈值时自动回退到 v36 输出。
3. 抽取统一 `accept/reject detection` 公共函数，减少 `_detect_center_with_retry` 分支重复。
4. 给 XPS 管理台补一轮状态码语义修正与最小 e2e API 合规测试。
