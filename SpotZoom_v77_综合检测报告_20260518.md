# SpotZoom v77 综合检测报告（2026-05-18）

## 1. 本轮目标与代码补充

- 目标：在 v76 基础上，继续参考前沿跟踪项目补充 `SpotZoom.py`，并完成构建、启动、页面访问、核心流程、接口请求、数据库读写、前端控制台、网络异常、静态资源、代码质量与模块结构的自动化检测。
- 新增模块：`frontier_v77_hysteresis_guard`（滞回可靠度守卫）。
  - 触发条件：v76 出现触发但无修正，或出现 reliability reject 信号。
  - 核心策略：
    - 压力帧计数（hysteresis streak）；
    - `phaseCorrelate` + `phaseCorrelateIterative` 组合位移估计；
    - 结合历史运动预测做一致性门控（consensus gate）；
    - 可靠度阈值按 streak 渐进放宽（带 floor），并施加最大位移约束。
  - 接线范围：默认配置、open-source bundle、`AlignmentConfig`、运行时状态、指标埋点、CLI 参数、参数校验、v57 主关联流水线。

## 2. 前沿类似项目调研与可模仿模块

1. CoTracker3（Meta FAIR）
- 链接：<https://github.com/facebookresearch/co-tracker>
- 可模仿点：在线点跟踪的时序一致性和长序列鲁棒性。
- 映射：v77 引入“压力帧滞回 + 时序预测一致性” gating。

2. Track-On
- 链接：<https://github.com/gorkaydemir/track_on>
- 可模仿点：真实噪声条件下的点级在线跟踪稳定策略。
- 映射：v77 只在高风险帧介入，常态帧不增加额外扰动。

3. Norfair
- 链接：<https://github.com/tryolabs/norfair>
- 可模仿点：轻量、距离门控、跟踪状态管理。
- 映射：v77 使用 consensus gate 与 shift clamp 限制漂移。

4. ByteTrack
- 链接：<https://github.com/FoundationVision/ByteTrack>
- 可模仿点：低分候选二次决策而非直接丢弃。
- 映射：v77 对 v76 压力信号提供“阈值渐进放宽”的二次判定路径。

5. OpenCV 官方文档
- `phaseCorrelate` / `phaseCorrelateIterative`：<https://docs.opencv.org/4.x/d7/df3/group__imgproc__motion.html>
- Template Matching：<https://docs.opencv.org/4.x/de/da9/tutorial_template_matching.html>
- 映射：v77 采用相位相关位移估计与局部一致性判定组合。

## 3. 自动化检查执行项

- 构建/静态检查
  - `python -m py_compile 8821L/SpotZoom.py`
  - `python -m compileall 8821L/SpotZoom.py 8821L/SpotZoom_Machine_Learning`
  - `python -m py_compile XPS/xps_admin_console/server.py`
  - `node --check XPS/xps_admin_console/static/app.js`
- SpotZoom 回放矩阵（6 case）
  - `smoke / offcenter_noisy / probe / reject_probe / missing_image / xps_fail`
- `reject_probe` AB 复核
  - baseline=`v76`，enabled=`v77`，各 3 次
- XPS 控制台端到端
  - 页面/静态资源/API/异常接口请求
  - Playwright 抓取 `console/pageErrors/requestFailed`
  - DB 读写路径覆盖（not-connected 分支）
- 代码质量与结构统计
  - LOC、函数规模、长函数数量、耦合信号

## 4. 本轮检测结果

### 4.1 SpotZoom case matrix（v77）

- 汇总：`total=6, success=4, failed=2`
- 成功：`smoke / offcenter_noisy / probe / reject_probe`
- 失败：`missing_image / xps_fail`
  - 二者均为预期失败路径（缺图、控制器不可达）。

### 4.2 v77 指标表现

- 汇总指标：
  - `frontier_v77_triggered=4`
  - `frontier_v77_hysteresis_hits=4`
  - `frontier_v77_guard_refines=0`
  - `frontier_v77_guard_rejects=0`
- 观察：v77 已按设计在压力帧介入，但本轮仍偏保守，尚未产生有效 center refine 样本。

### 4.3 reject_probe AB 复核

- `baseline_v76`: `2/3 success`
- `enabled_v77`: `3/3 success`
- 结论：v77 组未出现失败，但考虑单批样本较小，仍需扩大样本确认统计显著性。

### 4.4 页面/API/网络/静态资源

- HTTP 状态分布：`200x7, 400x3, 404x2, 413x1`
- 页面与静态资源可访问：`/`, `/static/app.js`, `/static/styles.css`
- 异常请求按预期命中：`/api/connect`, `/api/controller/raw`, `/api/files/upload`

### 4.5 前端控制台与请求失败

- Playwright：
  - `console=0`
  - `pageErrors=0`
  - `requestFailed=0`

### 4.6 数据库读写路径

- `read_stages_db` / `read_system_config`：HTTP 200，返回 `Not connected` 分支。
- 写入路径：
  - 非法 base64 -> 400
  - 合法 payload + 未连接 -> 400
- 结论：connected 分支尚未覆盖。

### 4.7 代码质量与结构

- `SpotZoom.py`：`25157 LOC`、`269 functions`、`>=200 行函数 20 个`
- 耦合信号：`self.cfg.` 引用 `1531` 次，`self._inc_metric(...)` `501` 次
- 结构风险：`parse_args` / `__init__` / `check_environment` 依旧超长，高回归扩散风险。

## 5. 问题优先级、影响模块、修复建议

### P1：v77 已触发但未产生有效 refine
- 影响模块：`_apply_frontier_v77_hysteresis_guard`
- 证据：`frontier_v77_guard_refines=0`
- 建议：
  - 下调/网格搜索：`frontier-v77-reliability-floor`、`frontier-v77-consensus-gate-px`、`frontier-v77-max-shift-px`
  - 将 `shift` 软接受阈值与噪声估计联动，而非固定阈值

### P1：主文件耦合继续上升
- 影响模块：`SpotZoom.py` 主流程、参数层、校验层
- 证据：LOC 与 `cfg`/metric 引用持续增长
- 建议：
  - 把 v73-v77 guard 抽成策略子模块（统一 I/O 协议）
  - 参数定义与校验拆分为 schema + validator

### P2：DB connected 分支覆盖缺失
- 影响模块：`/api/system/*`、`/api/files/upload`
- 建议：引入可连控制器或仿真控制器进行 connected 分支回归基线

### P2：reject_probe 尾部稳定性仍需扩样确认
- 影响模块：v57-v77 关联链路
- 建议：固定随机种子 + `N>=30` 重放，统计 p95/p99 失败率与对齐轮次

## 6. 本轮产物

- `8821L/SpotZoom.py`
- `8821L/artifacts/spotzoom_help_v77.txt`
- `8821L/artifacts/spotzoom_case_matrix_v77.json`
- `8821L/artifacts/spotzoom_case_metrics_v77.json`
- `8821L/artifacts/spotzoom_run_summary_v77.json`
- `8821L/artifacts/spotzoom_v77_ab_reject_probe.json`
- `8821L/artifacts/spotzoom_v77_ab_reject_probe_summary.json`
- `8821L/artifacts/xps_http_check_v77.json`
- `8821L/artifacts/xps_db_check_v77.json`
- `8821L/artifacts/xps_server_v77_stdout.log`
- `8821L/artifacts/xps_server_v77_stderr.log`
- `8821L/artifacts/spotzoom_code_quality_v77.json`

## 7. 下一步优化方案

1. 做 v77 参数网格（离线回放优先），目标是提升 `v77_guard_refines` 且不放大误修正。
2. 增加固定随机种子的高噪声稳定性基准集，追踪 `reject_probe` 的真实尾部风险。
3. 拆分 guard 管线与参数层，降低 `SpotZoom.py` 耦合与回归扩散面。
4. 在 connected 控制器环境补齐 DB 读写闭环回归。
