# SpotZoom v70 综合检测报告（2026-05-18）

## 1. 本轮目标与结论
- 本轮在 `SpotZoom.py` 中完成 **frontier v70 failover guard** 增量，主链已接入 `v69 -> v70 -> v57`。
- 构建/语法检查通过，XPS 控制台服务可启动，页面/API/静态资源访问正常。
- 前端运行时（Playwright）本轮 `console/pageErrors/requestFailed` 均为 `0`，Google Fonts 外链故障已消除。
- 关键风险仍在 `probe` 场景：`v70_probe` 仍未收敛（max alignment rounds reached）。

## 2. 互联网前沿类似项目与可模仿模块（Primary Sources）

| 项目 | 前沿点 | 可模仿到 SpotZoom 的模块 |
|---|---|---|
| [ByteTrack (arXiv:2110.06864)](https://arxiv.org/abs/2110.06864) | 低分检测二次关联，减少漏检引起的轨迹断裂 | 在 `v70` 后增加“低置信候选二次关联池”，仅对高漂移帧启用，降低过早 reject |
| [BoT-SORT (arXiv:2206.14651)](https://arxiv.org/abs/2206.14651) / [GitHub](https://github.com/NirAharon/BoT-SORT) | 相机运动补偿（CMC）+ 关联鲁棒性 | 继续强化 `v69` 的 CMC 失败回退，与 `v70` 熔断联动（连续 reject 后强制切 CMC/模板兜底） |
| [OC-SORT (arXiv:2203.14360)](https://arxiv.org/abs/2203.14360) | Observation-Centric Re-Update，强调观测一致性 | `v70` 已采纳 re-update 思路；可再补“观测窗口内最小残差优先”策略 |
| [CoTracker3 (arXiv:2410.11831)](https://arxiv.org/abs/2410.11831) / [GitHub](https://github.com/facebookresearch/co-tracker) | 稠密点协同跟踪 + 可见性信号 | 将可见性从单值阈值扩展为“局部点集一致性分数”输入 `v70` gate |
| [SAHI (GitHub)](https://github.com/obss/sahi) | 切片推理与小目标召回 | `v68` 已引入 tiled-rescue；下一步做切片尺度自适应（按噪声/漂移动态调 tile） |
| [trackpy adaptive search](https://soft-matter.github.io/trackpy/dev/generated/trackpy.link.html) | `adaptive_step / adaptive_stop` 自适应收敛 | 为 `v70` 新增“reject_streak 驱动的 step 退火”，收敛失败时自动放松搜索 |
| [Norfair Tracker docs](https://tryolabs.github.io/norfair/2.1/reference/tracker/) | `hit_counter_max / initialization_delay` 轨迹生命周期管理 | 把 SpotZoom 的 guard 状态机显式化，减少 guard 链之间隐式耦合 |

> 推断说明：上述“可模仿模块”是基于论文/官方文档能力与 SpotZoom 当前 v63-v70 链路形态做的工程映射，不是原项目直接给出的 SpotZoom 实现方案。

## 3. SpotZoom.py 本轮补充点（v70）
- 新增开关与参数（默认配置 + CLI + 参数校验 + bundle）：
  - `frontier_v70_failover_guard_enabled`
  - `frontier_v70_history_size`
  - `frontier_v70_reject_streak_for_refine`
  - `frontier_v70_reject_streak_for_circuit_breaker`
  - `frontier_v70_base_gate_px`
  - `frontier_v70_visibility_relax_*`
  - `frontier_v70_drift_penalty_gain`
  - `frontier_v70_consensus_quantile/blend`
  - `frontier_v70_confidence_penalty/boost`
- 新增运行态与指标：
  - 状态：`_frontier_v70_center_history`、`_frontier_v70_reject_streak`
  - 指标：`frontier_v70_guard_checks/refines/rejects/soft_accepts/consensus_rescues/circuit_breaker_hits`
- 新增方法：`_apply_frontier_v70_failover_guard(...)`
  - 核心逻辑：reject streak 跟踪、共识中心估计、可见性/漂移门控、置信度惩罚与补偿。
- 主链接入：`_apply_frontier_v70_failover_guard` 在 `_apply_frontier_v57_assoc` 前执行。

## 4. 自动化检测结果（本轮）

### 4.1 构建与静态检查
- `python -m py_compile 8821L/SpotZoom.py`：PASS
- `python -m py_compile XPS/xps_admin_console/server.py`：PASS
- `node --check XPS/xps_admin_console/static/app.js`：PASS

### 4.2 环境检查
- `python SpotZoom.py --check-env --frontier-v57-assoc-gate --frontier-v69-cmc-guard --frontier-v70-failover-guard`：FAIL
- 错误：`Missing dependency for motor control: pylablib`
- 同时出现警告：`PyTorch not available. Some features will be disabled.`

### 4.3 启动/页面/API/静态资源/异常请求
来源：`artifacts/xps_http_check_v70.json`
- 服务启动：成功（`http://127.0.0.1:8939` ready）
- 页面与静态资源：
  - `GET /` -> 200
  - `GET /static/app.js` -> 200
  - `GET /static/styles.css` -> 200
- 核心 API：
  - `GET /api/features` -> 200
  - `GET /api/session` -> 200
  - `POST /api/disconnect` -> 200
- 异常请求覆盖：
  - `GET /api/unknown` -> 404
  - `GET /static/not_found.js` -> 404
  - `POST /api/connect` -> 400（连接超时）
  - `POST /api/controller/raw` -> 400（未连接）
  - `POST /api/files/upload` -> 400（非法 base64）
  - `POST /api/files/upload` -> 413（请求体过大）
- 状态码分布：`200x7, 404x2, 400x3, 413x1`

### 4.4 前端控制台与网络异常
来源：`artifacts/xps_http_check_v70.json` 的 Playwright 采样
- `console` 错误数：0
- `pageErrors`：0
- `requestFailed`：0

### 4.5 数据库/配置读写链路专项
来源：`artifacts/xps_db_check_v70.json`
- `GET /api/system/stages-db` -> 200，返回 `{error: "Not connected...", content: null}`
- `GET /api/system/config` -> 200，返回 `{error: "Not connected...", content: null}`
- `POST /api/files/upload`（合法 payload，未连接）-> 400（Not connected）

结论：本项目无本地 DB 引擎，实际为远端 XPS 配置文件（`stages.ini/system.ini`）读写模型；在未连接控制器条件下，读写路径的错误语义可复现。

### 4.6 SpotZoom 核心流程回放（v70）

| case | status | elapsed_s | v69 rejects/refines | v70 rejects/refines | v70 rescues |
|---|---:|---:|---:|---:|---:|
| smoke | success | 4.515 | 0 / 0 | 0 / 0 | 0 |
| offcenter_noisy | success | 4.787 | 1 / 1 | 1 / 0 | 0 |
| probe | failed | 15.915 | 6 / 5 | 6 / 2 | 2 |
| reject_probe | success | 7.270 | 3 / 2 | 3 / 1 | 1 |
| missing_image | failed(预期) | 0.012 | 0 / 0 | 0 / 0 | 0 |
| xps_fail | failed(预期) | 2.052 | 0 / 0 | 0 / 0 | 0 |

补充：`probe`/`reject_probe` 中已出现 `frontier_v70_failover_refined` 事件，说明 v70 分支真实触发。

## 5. 问题清单（优先级 / 影响模块 / 修复建议）

### P1
1. `probe` 场景仍不收敛（max alignment rounds reached）
- 影响模块：`SpotZoom.py` v69/v70 守卫链与对齐主循环
- 风险：高噪声或偏移场景下回归失败，影响自动对焦稳定性
- 建议：
  - 增加“多轮失败后的 gate 自动放宽 + 二阶段候选重排”
  - 将 `v70` 的 circuit-breaker 触发阈值与 `max_alignment_rounds` 绑定，避免死耗时

2. 用例退出码与报告状态不一致（自动化判定抖动）
- 现象：`probe` 报告 `failed` 但 `exit_code=0`；`reject_probe` 报告 `success` 但 `exit_code=1`
- 影响模块：测试编排脚本/PowerShell 管道
- 风险：CI 误报或漏报
- 建议：统一以 `report.status` 为最终判定，并修复 `NativeCommandError` 对退出码的污染。

### P2
1. `check-env` 在默认驱动下对 `pylablib` 强依赖
- 影响模块：`check_environment()`
- 风险：无硬件/仿真环境也会判失败，影响自动化可用性
- 建议：在 `--frame-source-image` 或 `--xy-driver dryrun` 场景下降级为 warning。

2. 模块耦合与重复逻辑偏高
- 量化信号：
  - `SpotZoom.py` 22601 行
  - `parser.add_argument` 621 项
  - `frontier` 系列方法 42 个
  - `except Exception` 136 处
- 影响模块：`SpotZoom.py`（单文件多职责）
- 风险：回归影响面大、定位困难
- 建议：拆分 `frontier guards` 为独立模块 + 统一 guard 接口（输入候选、输出候选、指标钩子）。

### P3
1. 检测耗时指标不完整
- 现象：`detector_latency_ms` 在回放报告中为空列表
- 影响模块：性能分析与瓶颈定位
- 建议：补采 detector/guard 各阶段耗时并输出 P50/P95。

## 6. 下一步优化方案（建议执行顺序）
1. 先修自动化判定一致性（exit_code 与 report.status 统一），防止后续评估基线漂移。
2. 针对 `probe` 做 v69/v70 参数 AB：重点扫描 `base_gate_px`、`consensus_blend`、`reject_streak` 阈值。
3. 抽离 v63-v70 为 `frontier_guard_pipeline.py`，保留统一上下文结构与指标接口。
4. 补齐耗时观测：对 detect / v69 / v70 / align loop 分段计时并出 P95。
5. 在可接入真实 XPS 后补跑“配置读写成功路径”（目前仅完成未连接错误路径校验）。
