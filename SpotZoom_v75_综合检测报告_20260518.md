# SpotZoom v75 综合检测报告（2026-05-18）

## 1. 本轮目标与代码补充

- 目标：在 `SpotZoom.py` 现有 v74 链路上，基于前沿项目思路补充可落地模块，并完成构建/启动/页面/API/DB/日志/网络/静态资源/代码质量全链路检测。
- 本轮新增：`frontier_v75_bidirectional_retrack_guard`（双向一致性重跟踪守卫）。
  - 核心逻辑：
    - 在 v74 触发且出现“触发未修正/拒绝/PSR拒绝”信号时介入；
    - 使用模板匹配 top1-top2 峰值间隔抑制歧义峰；
    - 使用 backward rematch（一致性误差）过滤伪匹配；
    - 使用 `cv.phaseCorrelate` 响应值作为额外置信过滤；
    - 通过独立指标与事件日志输出诊断信号。

## 2. 互联网前沿类似项目调研与可模仿模块

### 2.1 ReTracker（ICCV 2025）
- 参考点：强调 image matching 驱动的 online point tracking，并显式提到 two-view matching data + correction module。
- 可模仿模块：
  - 双向一致性校验（forward/backward consistency）
  - 纠错模块式“重跟踪后再验证”
- 已落地到 v75：`backward_max_error_px` + 歧义峰拒绝。

### 2.2 Track-On / Track-On-R（ICLR 2025）
- 参考点：real-world fine-tuning with verifier-guided pseudo-labels。
- 可模仿模块：
  - verifier 驱动的触发机制
  - 失败后校验器分级决策
- 已映射：v75 只在 v74 失败信号出现时触发，避免常态路径开销。

### 2.3 CoTracker3
- 参考点：Pseudo-Labelling + online tracking variants。
- 可模仿模块：
  - 在线链路中的“弱监督校验 + 异常筛除”
- 已映射：v75 ambiguity/backward/phase 三段筛选。

### 2.4 AllTracker
- 参考点：causal all-point tracking，面向 high-resolution videos 的高效可扩展框架。
- 可模仿模块：
  - 因果时序中的统一筛选策略（低延迟门控）
- 已映射：v75 仅在异常触发时执行，保持主流程实时性。

### 2.5 Trackpy + OpenCV 文档
- Trackpy：adaptive search strategy（`adaptive_stop`/`adaptive_step` 思路）
- OpenCV：
  - `matchTemplate + minMaxLoc` 适合单目标峰值定位；
  - `phaseCorrelate` 响应值可作为匹配可靠性指标。
- 已映射：v74 动态搜索半径 + v75 峰值/相位响应联合过滤。

## 3. 自动化检测执行项

- 构建/静态：
  - `python -m py_compile SpotZoom.py`
  - `python -m compileall SpotZoom.py SpotZoom_Machine_Learning`
  - `python -m py_compile ../XPS/xps_admin_console/server.py`
  - `node --check ../XPS/xps_admin_console/static/app.js`
- SpotZoom 核心流程：6 case matrix（smoke/offcenter_noisy/probe/reject_probe/missing_image/xps_fail）
- XPS 控制台：
  - 服务器启动、主页与静态资源访问
  - API 正常与异常请求
  - Playwright 前端 console/pageErrors/requestFailed
- DB 路径：读配置、读 stages-db、写文件（合法/非法 payload）
- 代码质量：LOC/函数规模/高耦合信号/异常块密度

## 4. 本轮检测结果

### 4.1 SpotZoom case matrix（v75）

- 汇总：`total=6, success=4, failed=2`
- 成功：`smoke / offcenter_noisy / probe / reject_probe`
- 失败：`missing_image / xps_fail`（预期失败路径）
- 关键指标：
  - `frontier_v74_guard_refines = 0`（各成功 case）
  - `frontier_v75_guard_refines = 0`（各成功 case）
  - v75 在成功 case 中有触发（`frontier_v75_triggered=1`），并以 `soft_accept` 或 `ambiguity_reject` 退出。

### 4.2 页面访问 / 接口请求 / 网络异常 / 静态资源

- XPS HTTP 状态画像：`200x7, 400x3, 404x2, 413x1`
- 页面与静态资源：`/`, `/static/app.js`, `/static/styles.css` 可访问
- 异常请求路径（预期）：
  - `/api/connect` timeout -> 400
  - bad base64 upload -> 400
  - oversized upload -> 413
  - unknown API/static -> 404

### 4.3 前端控制台日志

- Playwright 结果：
  - `console = 0`
  - `pageErrors = 0`
  - `requestFailed = 0`

### 4.4 数据库读写路径

- `xps_db_check_v75.json` 显示：
  - `read_stages_db` / `read_system_config` 返回 not connected 分支（HTTP 200 + error 字段）
  - 写入合法 payload 在未连接状态被拒绝（HTTP 400）
- 结论：DB 真实连接分支本轮未覆盖（环境受限）。

### 4.5 代码质量与模块结构

- `SpotZoom.py`：
  - 行数：24372
  - 函数数：267
  - 长函数（>=200行）：18
  - `self.cfg.` 引用：1494
  - `self._inc_metric(...)`：466
- 高耦合/复杂度信号仍明显：`parse_args` / `__init__` / `check_environment` 超长。

## 5. 问题优先级、影响模块、修复建议

### P1
- 问题：v75 已触发但本轮 `guard_refines=0`，增益主要体现在“拒绝/软接受”而非正向纠偏。
- 影响模块：`frontier_v75_bidirectional_retrack_guard`，以及 v73/v74/v75 串联策略。
- 建议：
  - 用离线回放网格搜索 `min_peak_margin/backward_max_error/phase_response_min`；
  - 增加“低风险小位移直接 refine”的分支（当前偏保守）；
  - 增加 `sim_seed` 固定化，降低噪声随机性干扰回归判断。

### P1
- 问题：`reject_probe` 历史上存在不稳定性，当前虽成功但存在尾部回归风险。
- 影响模块：`_align_to_target` 与 v57-v75 guard 串联。
- 建议：
  - 把 `reject_probe` 纳入多次 CI 回归（建议>=10次）；
  - 增加“max rounds 触发前一次兜底降阶策略”（例如暂时放宽 tolerance 或切换守卫阈值）。

### P2
- 问题：DB 真实连接读写分支未覆盖。
- 影响模块：`xps_admin_console` 会话与文件接口。
- 建议：接入可用控制器/模拟器，补全 connected 分支自动化用例。

### P2
- 问题：单文件超大（2.4w+ 行）导致耦合高、回归面大。
- 影响模块：`SpotZoom.py` 全局。
- 建议：拆分为 `guards/v6x_v7x.py`、`cli_args.py`、`env_checks.py`、`runtime_metrics.py`，先做无行为改动的结构重构。

## 6. 下一步优化方案（建议顺序）

1. 固定随机种子，新增 v75 参数扫描脚本（先针对 reject_probe/offcenter_noisy）。
2. 给 v75 增加“安全 refine 通道”（当前过于保守导致 `refines=0`）。
3. 补齐 DB connected 分支自动化（读写 + 权限 + 异常）并纳入 nightly。
4. 启动 `SpotZoom.py` 模块化重构（保持行为等价，先拆再测）。

## 7. 产出文件

- `artifacts/spotzoom_case_matrix_v75.json`
- `artifacts/spotzoom_case_metrics_v75.json`
- `artifacts/spotzoom_run_summary_v75.json`
- `artifacts/spotzoom_code_quality_v75.json`
- `artifacts/xps_http_check_v75.json`
- `artifacts/xps_db_check_v75.json`
- `artifacts/xps_server_v75_stdout.log`
- `artifacts/xps_server_v75_stderr.log`
- `artifacts/spotzoom_help_v75.txt`

## 8. 参考来源

- ReTracker (ICCV 2025): https://openaccess.thecvf.com/content/ICCV2025/html/Tan_ReTracker_Exploring_Image_Matching_for_Robust_Online_Any_Point_Tracking_ICCV_2025_paper.html
- Track-On (GitHub): https://github.com/gorkaydemir/track_on
- CoTracker3: https://cotracker3.github.io/
- AllTracker: https://alltracker.github.io/
- Trackpy adaptive search: https://soft-matter.github.io/trackpy/dev/tutorial/adaptive-search.html
- OpenCV template matching: https://docs.opencv.org/4.x/d4/dc6/tutorial_py_template_matching.html
- OpenCV phaseCorrelate: https://docs.opencv.org/4.x/d7/df3/group__imgproc__motion.html
