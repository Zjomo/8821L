# SpotZoom v66 综合检测报告（2026-05-18）

## 1. 前沿同类项目调研（本轮）

1. CoTracker3（ICCV 2025）
- 参考: https://openaccess.thecvf.com/content/ICCV2025/html/Karaev_CoTracker3_Simpler_and_Better_Point_Tracking_by_Pseudo-Labelling_Real_Videos_ICCV_2025_paper.html
- 可模仿模块: 点跟踪中的 visibility/confidence 联合输出、遮挡场景鲁棒性、在线/离线统一轨迹更新。

2. Norfair Tracker
- 参考: https://tryolabs.github.io/norfair/2.1/reference/tracker/
- 可模仿模块: `hit_counter_max` / `initialization_delay` / `pointwise_hit_counter_max` 这类“连续命中-拒绝”平衡策略。

3. trackpy Adaptive Search
- 参考: https://soft-matter.github.io/trackpy/dev/tutorial/adaptive-search.html
- 可模仿模块: `adaptive_step` / `adaptive_stop` 风格的动态门控与可观测诊断指标。

4. BoT-SORT
- 参考: https://github.com/NirAharon/BoT-SORT
- 可模仿模块: camera motion compensation（CMC）与运动+外观联合关联框架。

## 2. SpotZoom.py 本轮补充

目标文件: `E:\jupyter file\2_Optics\8821L\SpotZoom.py`

新增 `frontier v66 curvature guard`（可见性驱动的曲率一致性守卫）：
- 新增配置项（默认配置 / `AlignmentConfig` / CLI 参数 / 环境校验）:
  - `frontier_v66_curvature_guard_enabled`
  - `frontier_v66_history_size`
  - `frontier_v66_max_turn_deg`
  - `frontier_v66_max_jerk_px`
  - `frontier_v66_max_residual_px`
  - `frontier_v66_visibility_relax_threshold`
  - `frontier_v66_visibility_relax_gain`
  - `frontier_v66_confidence_penalty`
  - `frontier_v66_confidence_boost`
- 新增运行指标:
  - `frontier_v66_guard_checks`
  - `frontier_v66_guard_refines`
  - `frontier_v66_guard_rejects`
  - `frontier_v66_guard_soft_accepts`
  - `frontier_v66_guard_failures`
- 新增控制器状态:
  - `self._frontier_v66_center_history`
  - `self._frontier_v66_reject_streak`
- 新增方法:
  - `_apply_frontier_v66_curvature_guard(...)`
- 主链路更新:
  - `v57 -> v58 -> v59 -> v63 -> v64 -> v65 -> v66`
- Open-source bundle 联动:
  - `microscopy_tracking` / `resilient_association` / `frontier_2026_tracking_stack`
  已默认启用 `frontier_v66_curvature_guard_enabled`。

## 3. 自动化检测结果

### 3.1 构建 / 启动 / 语法
- `python -m py_compile 8821L/SpotZoom.py`: PASS
- `python -m py_compile XPS/xps_admin_console/server.py`: PASS
- `node --check XPS/xps_admin_console/static/app.js`: PASS
- `python SpotZoom.py --check-env ... --frontier-v66-curvature-guard --skip-roi`: PASS

### 3.2 SpotZoom 核心流程回放

1) `spotzoom_run_report_v66_smoke.json`
- 状态: success
- 指标: `detect_success=5/5`
- v64: `checks=5, refines=0, rejects=0, soft_accepts=5`
- v65: `checks=5, refines=0, rejects=0, soft_accepts=5`
- v66: `checks=5, refines=0, rejects=0, soft_accepts=5`

2) `spotzoom_run_report_v66_offcenter_noisy.json`
- 状态: success
- 指标: `detect_success=5/5`
- v65: `checks=5, refines=1, rejects=0, soft_accepts=4`
- v66: `checks=5, refines=2, rejects=0, soft_accepts=3`

3) `spotzoom_run_report_v66_probe.json`（高扰动+紧门控）
- 状态: success
- 指标: `detect_success=5/5`
- v65: `checks=5, refines=2, rejects=1, soft_accepts=2`
- v66: `checks=5, refines=2, rejects=0, soft_accepts=3`

4) `spotzoom_run_report_v66_reject_probe.json`（极限拒绝压测）
- 状态: failed（符合预期：门控极严，主动压测拒绝分支）
- 指标: `detect_success=64/64`，但迭代未收敛
- v66: `checks=64, refines=4, rejects=56, soft_accepts=4`
- 说明: v66 reject 分支已被大量真实触发（事件流可复核）。

5) 故障注入
- `spotzoom_run_report_v66_missing_image.json`: failed（符合预期，缺失图像）
- `spotzoom_run_report_v66_xps_fail.json`: failed（符合预期，XPS 连接失败）

### 3.3 页面访问 / 静态资源 / 接口请求 / 网络异常
- 文件: `8821L/artifacts/xps_http_check_v66.json`
- 服务启动日志: `8821L/artifacts/xps_server_v66_stdout.log`
- GET:
  - `/`, `/static/app.js`, `/static/styles.css`, `/api/features`, `/api/session` -> 200
  - `/api/unknown`, `/static/not_found.js` -> 404（预期）
- POST:
  - `/api/connect`（127.0.0.1:1 超时）-> 400
  - `/api/controller/raw`（未连接）-> 400
  - `/api/files/upload`（非法 base64）-> 400
  - `/api/files/upload`（超 2MB）-> 413
  - `/api/disconnect` -> 200

### 3.4 数据库读写
- 在 `SpotZoom.py` 与 `XPS/xps_admin_console/server.py` 未检出 sqlite/mysql/postgres/redis/mongodb 等访问关键词。
- 当前架构以图像处理、设备控制和 HTTP 交互为主，不是数据库密集型业务。

### 3.5 前端控制台日志
- `playwright` 运行时依赖缺失（`playwright=missing`），本轮未完成浏览器 runtime console 采集。
- 已完成前端静态语法与 HTTP 行为检查，但 console runtime 仍是覆盖缺口。

### 3.6 代码质量与模块结构
- `SpotZoom.py`: 19607 行，`except Exception` 130 处，`_apply_frontier_v*` 方法 38 个。
- `SpotZoomController.__init__`: 2183 行（4433 -> 6616），耦合度高。
- `XPS/xps_admin_console/server.py`: 916 行，`except Exception` 13 处。

## 4. 问题分级（优先级 / 影响模块 / 修复建议）

### P1
1) 单文件耦合持续偏高
- 影响模块: `8821L/SpotZoom.py`
- 现象: `SpotZoomController` 构造与 frontier 链过长，回归传播面大。
- 建议: 抽离 `v58~v66` 为独立 post-association pipeline 模块，保留统一 `apply(...)` 接口。

2) v66 在极限阈值下存在“高拒绝导致不收敛”风险
- 影响模块: `frontier_v66` 参数组
- 现象: `reject_probe` 下 `frontier_v66_guard_rejects=56`，控制回路可失败。
- 建议: 对 `max_turn/jerk/residual` 增加保护下限或动态回退策略，避免参数过紧导致硬失败。

### P2
3) 接口异常语义仍偏粗
- 影响模块: `XPS/xps_admin_console/server.py`
- 现象: timeout/未连接/参数错误主要映射 400。
- 建议: 细化 408/409/422/5xx + 结构化 `error_code` 字段。

4) 广谱异常捕获密度高
- 影响模块: `SpotZoom.py`, `server.py`
- 现象: `except Exception` 较多，根因定位依赖日志文本。
- 建议: 分阶段替换 typed exceptions，并统一 error event schema。

### P3
5) 浏览器层 runtime console 未自动化
- 影响模块: 自动化检测链路
- 现象: Playwright 缺失，无法抓取 JS runtime 异常与 failed request 细节。
- 建议: 补齐 Playwright 检查步骤（console/error/network）。

## 5. 下一步优化方案

1. 抽离 `frontier_post_assoc_chain_v58_v66.py`，降低主文件复杂度和改动冲突率。
2. 为 v66 增加参数保护机制（最小阈值 + reject streak 上限后的自动放宽）。
3. 在 `server.py` 输出结构化错误码并按语义映射 HTTP 状态。
4. 安装并固化 Playwright 检查，补齐前端 console/runtime 异常覆盖。
