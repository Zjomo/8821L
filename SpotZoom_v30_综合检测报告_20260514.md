# SpotZoom 本轮综合检测与优化报告（v30）

- 执行时间: 2026-05-14 (Asia/Shanghai)
- 目标文件: `8821L/SpotZoom.py`
- 本轮新增模块: `8821L/SpotZoom_Machine_Learning/innovation_frontier_v29.py`

## 1. 前沿同类项目调研与可模仿模块

1. CoTracker3（Meta）
   - 参考: https://github.com/facebookresearch/co-tracker
   - 可借鉴点: 长时序点轨迹一致性、时序平滑和短时漂移抑制。
2. ByteTrack（FoundationVision）
   - 参考: https://github.com/FoundationVision/ByteTrack
   - 可借鉴点: 低置信检测参与关联（而不是直接丢弃），增强遮挡/弱信号下连续性。
3. OpenWFS
   - 参考: https://openwfs.readthedocs.io/en/latest/core.html
   - 可借鉴点: 处理器链式执行与结果对象化，强调稳定性优先和可诊断性。
4. DeepTrack2
   - 参考: https://github.com/DeepTrackAI/DeepTrack2
   - 可借鉴点: 科学成像场景中的鲁棒检测与时序噪声容忍。
5. slmsuite
   - 参考: https://github.com/slmsuite/slmsuite
   - 可借鉴点: 光学控制中的实时反馈、标定和工程化模块分层。

## 2. 本轮代码补充

1. 新增 `innovation_frontier_v29.py`
   - 实现 `TemporalReliabilityFilter`（numpy-only）
   - 核心能力: 速度先验预测、置信度自适应门控、时序融合修正、异常跳变拒绝
2. 在 `SpotZoom.py` 接入 v29
   - 增加 CLI 参数: `--frontier-v29-*`
   - 增加配置字段、环境检查、运行指标、事件上报、资源释放
   - 在 `_smooth_detection()` 中串联 v29 可靠性修正
3. 修复默认性能开销问题
   - 新增 `--frontier-v4-bundle` 开关
   - 将 v4 扩展模块初始化改为显式开启，默认关闭，避免每次运行无条件加载
4. 观测性补充
   - 检测失败重试时新增 `detection_miss` 事件写入 run report

## 3. 自动化检测覆盖与结果

### 3.1 构建/启动
- `python -m py_compile SpotZoom.py` 通过
- `python -m py_compile innovation_frontier_v29.py` 通过
- `python SpotZoom.py --check-env ...` 通过

### 3.2 核心流程冒烟
1. 成功场景（中心光斑）
   - 命令: dryrun + classic + simulated image
   - 结果: 收敛成功
   - 报告: `spotzoom_run_report_v30_center_after_patch.json`
2. 失败场景（纯黑图）
   - 结果: 按预期报错 `Initial P1: detection failed after 2 attempts`
   - 报告: `spotzoom_run_report_v30_black_after_patch2.json`
   - 事件: 已记录 `detection_miss`
3. v29 场景（抖动模拟）
   - 结果: 成功，`frontier_v29_reliability_calls=5`，`refines=1`
   - 报告: `spotzoom_run_report_v30_v29.json`

### 3.3 页面访问/前端控制台/静态资源
- SpotZoom 主流程为 Python 本地控制脚本，非 Web 前端应用。
- 未发现可访问的业务页面与浏览器控制台链路；此项判定为 N/A。

### 3.4 接口请求/网络异常
- 无 HTTP API 调用；网络链路主要为 XPS TCP 控制（`XPSZAxis`）。
- 本轮 dryrun 未触发真实网络连接；已通过代码路径检查其异常抛出逻辑。

### 3.5 数据库读写
- 目标流程未使用数据库读写（未发现 sqlite/mysql/postgres 业务调用）。

### 3.6 代码质量与结构
- Flake8（SpotZoom.py）统计: 20 项（以空行/长行为主）
- Pylint E/F: 主要为 OpenCV 类型推断误报 + 可选模块静态推断告警
- AST 结构: `functions=201`, `classes=22`
- 超长函数风险: `parse_args`、`SpotZoomController.__init__`、`check_environment` 体量过大

## 4. 问题优先级与修复建议

1. P1 - 模块耦合过高
   - 表现: `SpotZoomController.__init__` 和 `parse_args` 过长，配置/依赖/初始化集中在单体文件。
   - 影响: 回归风险高，新增功能容易引入连锁问题。
   - 建议: 分拆为 `config.py` / `frontier_registry.py` / `controller_runtime.py`。
2. P1 - 隐式默认行为风险（已修复）
   - 表现: v4 扩展模块此前默认无条件初始化。
   - 影响: 启动耗时和资源占用上升，非必要依赖失败会污染主流程。
   - 建议: 保持显式开关（当前已通过 `--frontier-v4-bundle` 解决）。
3. P2 - 观测性不足（部分修复）
   - 表现: 检测失败重试阶段缺少结构化 miss 事件。
   - 影响: 失败归因困难。
   - 建议: 已新增 `detection_miss`；后续补充“失败原因标签”聚合统计。
4. P2 - 静态检查噪声较高
   - 表现: Pylint 对 cv2/动态导入产生大量噪声告警。
   - 影响: 真问题信噪比低。
   - 建议: 引入针对 `cv2` 和可选依赖的 pylint/flake8 规则白名单。
5. P3 - 风格一致性
   - 表现: 长行、空行规范问题较多。
   - 影响: 可读性下降。
   - 建议: 分阶段执行 `ruff/black`（先小范围模块，不直接全文件自动化重排）。

## 5. 下一步优化计划

1. 将检测/恢复链拆成可配置 pipeline（按模块注册执行），降低主控制器复杂度。
2. 为 v29 增加 A/B 开关和基准回放集，量化 jitter 抑制与误拒绝率。
3. 增加 `--target-mode {initial,frame-center,fixed}`，避免目标定义歧义。
4. 补充 CI 级 smoke 测试（中心光斑/偏移光斑/黑图/低信噪）并自动比对 run report 指标。
