# SpotZoom v72 综合检测报告（2026-05-18）

## 1. 本轮目标与结论
- 已在 `SpotZoom.py` 新增 **frontier v72 memory-verifier guard**（Track-On/Track-On2 风格 memory teacher + verifier 共识门控），并接入默认配置、open-source bundle、`AlignmentConfig`、CLI、参数校验、运行指标与主链（`v57 -> ... -> v71 -> v72`）。
- 构建与语法检查通过：`py_compile(SpotZoom.py/server.py)`、`node --check(app.js)` 全部 PASS。
- 自动化回放矩阵已生成：`artifacts/spotzoom_case_matrix_v72.json`。
- v72 分支在 `reject_probe` 场景真实触发一次 `frontier_v72_memory_verifier_refined`（`teacher_switches=1`）；`probe/reject_probe` 仍因 `max alignment rounds reached` 失败。

## 2. 互联网前沿类似项目与可模仿模块（Primary Sources）

| 项目 | 前沿点 | 建议映射到 SpotZoom 的模块 |
|---|---|---|
| [Track-On (arXiv:2501.18487)](https://arxiv.org/abs/2501.18487) | 在线点跟踪中的 memory + verifier 机制 | v72 已落地“teacher hypotheses + verifier agreement + entropy 监控” |
| [Track-On2 (arXiv:2509.19115)](https://arxiv.org/abs/2509.19115) | 空间/上下文双记忆模块 + 在线稳定跟踪 | v72 可继续扩展为“短期轨迹记忆 + 上下文记忆”双通道打分 |
| [Track-On GitHub](https://github.com/gorkaydemir/track_on) | 训练流水线中显式 verifier 与 teacher 路径 | 将 v72 事件上报纳入离线 AB 比较（teacher_switch/low_agreement） |
| [CoTracker3 (arXiv:2410.11831)](https://arxiv.org/abs/2410.11831) | 可见性与点级一致性信号 | 将单点 `visibility_score` 升级为局部点簇一致性分数 |
| [OA-SORT (arXiv:2603.06034)](https://arxiv.org/abs/2603.06034) | 遮挡感知关联 | 把 v71/v72 的遮挡触发条件从阈值拓展到状态机 |
| [BoT-SORT (arXiv:2206.14651)](https://arxiv.org/abs/2206.14651) | CMC + 关联鲁棒性 | 将 v69 CMC 质量注入 v72 verifier 评分项 |
| [ByteTrack (arXiv:2110.06864)](https://arxiv.org/abs/2110.06864) | 高低分检测统一关联 | 为 v72 增加“低置信候选二次校验池” |
| [OC-SORT (arXiv:2203.14360)](https://arxiv.org/abs/2203.14360) | Observation-centric re-update | v72 中引入观测更新优先级，降低历史记忆过拟合 |

> 说明：映射建议基于论文/官方仓库描述与 SpotZoom 当前 v57-v72 结构做工程推断。

## 3. SpotZoom.py 本轮补充（v72）
- 新增配置/CLI 参数：
  - `frontier_v72_memory_verifier_guard_enabled`
  - `frontier_v72_history_size`
  - `frontier_v72_teacher_blend`
  - `frontier_v72_verifier_residual_gate_px`
  - `frontier_v72_verifier_agreement_min`
  - `frontier_v72_visibility_relax_threshold`
  - `frontier_v72_visibility_relax_gain`
  - `frontier_v72_drift_penalty_gain`
  - `frontier_v72_max_offset_px`
  - `frontier_v72_confidence_penalty`
  - `frontier_v72_confidence_boost`
- 新增运行态与指标：
  - 状态：`_frontier_v72_center_history`、`_frontier_v72_last_v71_refines`、`_frontier_v72_last_v71_rejects`、`_frontier_v72_teacher_entropy_ema`
  - 指标：`frontier_v72_guard_checks/refines/rejects/soft_accepts/failures/teacher_switches/verifier_low_agreement/entropy_spikes`
- 新增守卫方法：`_apply_frontier_v72_memory_verifier_guard(...)`
  - 逻辑：基于历史记忆、预测点、候选点形成 teacher 假设，使用 residual/trend/shift 评分与 agreement/entropy 判据进行 refine/reject。
- 主链接入：在 `v71` 之后串接 `v72`。
- Bundle 接入：`microscopy_tracking`、`resilient_association`、`frontier_2026_tracking_stack` 均默认启用 v72。

## 4. 自动化检测结果（本轮）

### 4.1 构建/静态检查
- `python -m py_compile SpotZoom.py`：PASS
- `python -m py_compile XPS/xps_admin_console/server.py`：PASS
- `node --check XPS/xps_admin_console/static/app.js`：PASS

### 4.2 环境检查
- `--check-env`（默认驱动）: FAIL
  - 错误：`Missing dependency for motor control: pylablib`
- `--check-env --xy-driver dryrun --z-driver dryrun`: PASS
  - 告警：YOLO/ultralytics 未安装（自动降级 classic）

### 4.3 核心流程回放（v72）
来源：`artifacts/spotzoom_case_matrix_v72.json` + `spotzoom_run_report_v72_*.json`

| case | exit_code | status | elapsed_s | v71 refines/rejects | v72 checks/refines/rejects/switches | 备注 |
|---|---:|---|---:|---:|---:|---|
| smoke | 0 | success | 4.054 | 0 / 0 | 5 / 0 / 0 / 0 | 稳态通过 |
| offcenter_noisy | 0 | success | 4.590 | 0 / 0 | 5 / 0 / 0 / 0 | 稳态通过 |
| probe | 1 | failed | 10.817 | 0 / 0 | 10 / 0 / 0 / 0 | `max alignment rounds reached` |
| reject_probe | 1 | failed | 10.835 | 1 / 0 | 10 / 1 / 0 / 1 | 出现 `frontier_v72_memory_verifier_refined` |
| missing_image | 1 | failed(预期) | 0.009 | 0 / 0 | 0 / 0 / 0 / 0 | 输入缺失路径覆盖 |
| xps_fail | 1 | failed(预期) | 2.037 | 0 / 0 | 0 / 0 / 0 / 0 | XPS 连接失败路径覆盖 |

补充：
- `spotzoom_events_v72_reject_probe.jsonl`：`frontier_v72_memory_verifier_refined` 出现 1 次。
- `spotzoom_events_v72_probe.jsonl`：v72 refine/reject 事件未触发。

### 4.4 页面访问 / 静态资源 / 接口请求 / 网络异常 / 前端日志
来源：`artifacts/xps_http_check_v72.json`
- 状态分布：`200x7, 404x2, 400x3, 413x1`
- 页面与静态资源：`/`、`/static/app.js`、`/static/styles.css` 均 200
- 核心 API：`/api/features`、`/api/session`、`/api/disconnect` 均 200
- 异常请求覆盖：`/api/unknown` 404、`/static/not_found.js` 404、`/api/connect` 400、`/api/controller/raw` 400、`/api/files/upload` 400/413
- Playwright runtime：`console=0, pageErrors=0, requestFailed=0`

### 4.5 数据库/配置读写链路
来源：`artifacts/xps_db_check_v72.json`
- `GET /api/system/stages-db` -> 200，返回 `Not connected`
- `GET /api/system/config` -> 200，返回 `Not connected`
- `POST /api/files/upload`（合法 payload，未连接）-> 400

结论：DB/配置链路仍仅覆盖“未连接”路径，尚未覆盖真实连接成功读写。

### 4.6 代码质量与模块结构
- `SpotZoom.py`：`22133` 行
- `parser.add_argument(...)`：`643`
- `_apply_frontier_*` 方法数：`46`
- `except Exception`：`138`
- 最大函数长度：`parse_args` 3495 行、`SpotZoomController.__init__` 2308 行、`check_environment` 1278 行

## 5. 问题分级（优先级 / 影响模块 / 修复建议）

### P1
1. `probe/reject_probe` 仍未收敛（`max alignment rounds reached`）
- 影响模块：`SpotZoom.py` 对齐主循环 + v63-v72 守卫链
- 风险：高噪声/高扰动场景回归失败
- 建议：
  - 对 `tolerance_px`、`max_align_rounds`、`v72_verifier_residual_gate_px` 做 AB 联动
  - 新增“连续低 agreement 熔断回退”（v72 -> v71 -> v70）

2. 默认环境检查硬依赖导致自动化误报
- 影响模块：`check_environment`
- 风险：无硬件环境下持续 FAIL
- 建议：在 `dryrun + frame-source-image` 模式将 `pylablib` 从 error 下调到 warning

### P2
1. v72 触发密度偏低，监控告警偏高
- 现象：`probe` 中 `v72_low_agreement=8`、`entropy_spikes=7`，但 `refine=0`
- 影响模块：v72 参数面与评分函数
- 建议：下调 `agreement_min` 或引入动态阈值（由 `v71_reject_delta` 调制）

2. 前端文本存在编码/标记异常迹象
- 现象：`index.html` 响应中出现乱码片段与可疑闭合标签文本（`...鏈繛鎺?/div>`）
- 影响模块：`XPS/xps_admin_console/static/index.html`
- 建议：统一 UTF-8（无 BOM）重编码并修复异常标签文本

### P3
1. 单文件耦合持续上升
- 现象：`SpotZoom.py` 超 22k 行，且 `parse_args`/`__init__` 超长
- 影响模块：变更扩散与回归范围
- 建议：优先拆分 `v63-v72` guard pipeline、参数注册、环境检查到独立模块

## 6. 下一步优化方案（建议顺序）
1. 做 v72 参数 AB：`agreement_min`、`teacher_blend`、`residual_gate` 与 `tolerance/max_align_rounds` 联动。
2. 增加 `v72` 连续低 agreement 熔断回退，避免在噪声场景长时间空转。
3. 拆分 `SpotZoom.py` 的 guard 链和 CLI 注册，降低耦合并补单测。
4. 修复 XPS 前端编码与文本显示异常，重跑 Playwright console/network 采样。
