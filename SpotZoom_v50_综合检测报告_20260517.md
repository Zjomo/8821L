# SpotZoom v50 综合检测报告（2026-05-17）

## 1. 本轮新增（SpotZoom.py 补充）

- 新增模块：`SpotZoom_Machine_Learning/innovation_frontier_v50.py`
  - 实现 `DualThresholdDriftAssociationGate`（双阈值关联 + 漂移补偿）
  - 输出结构化结果：`DualThresholdDriftAssociationResult`
  - 仅依赖 `numpy`（与 v46/v47 一致，低耦合可插拔）
- `SpotZoom.py` 已完成 v50 全链路接入：
  - 动态导入/类型绑定/默认配置/`AlignmentConfig` 字段
  - CLI 开关与参数：`--frontier-v50-*`
  - 参数校验与模块可用性告警
  - 控制器初始化、平滑链路调用、运行指标计数、资源重置
  - 事件上报：`frontier_v50_assoc_accepted/rejected/failed`

## 2. 互联网前沿同类项目调研与可模仿模块

- SAM2（流式视频分割与 memory 机制）：
  - 可模仿：持续状态缓存 + 帧间增量更新策略
- CoTracker3（长序列点跟踪）：
  - 可模仿：长时序一致性与在线轨迹更新
- ByteTrack（低分检测再利用）：
  - 可模仿：双阈值关联（高分主路径 + 低分补偿路径）
- Norfair（轻量实时跟踪）：
  - 可模仿：距离优先关联 + 摄像机/全局运动补偿
- OpenWFS / slmsuite / HCIPy（光学控制与闭环）：
  - 可模仿：硬件无关抽象、控制环参数化、仿真-实机一致接口

本轮 v50 对应落地点：
- 已落地：ByteTrack/Norfair 风格双阈值关联 + 漂移补偿。
- 待落地：OpenWFS/slmsuite/HCIPy 风格的“控制环与光学模型完全解耦”。

## 3. 自动化检测覆盖与结果

### 3.1 构建与启动

- `python -m py_compile`：
  - `SpotZoom.py`：通过
  - `innovation_frontier_v50.py`：通过
  - `xps_admin_console/server.py`：通过

### 3.2 SpotZoom 核心流程

- 运行（smoke）：
  - `artifacts/spotzoom_run_report_v50_smoke_clean.json`：`status=success`
  - v50 指标：`frontier_v50_assoc_calls=5`，`failures=0`
- 运行（offcenter+jitter+noise）：
  - `artifacts/spotzoom_run_report_v50_offcenter_noisy_clean.json`：`status=success`
  - v50 指标：`calls=11`，`refines=2`，`rejects=8`，`drift_comp_hits=2`，`failures=0`
- 运行（XPS 网络失败场景）：
  - `artifacts/spotzoom_run_report_v50_xps_fail_clean.json`：`status=failed`
  - 失败原因：`XPS controller connection failed`（符合预期）

### 3.3 页面访问 / 接口请求 / 静态资源 / 网络异常

- `artifacts/xps_http_check_v50.json`
  - 页面：`GET / -> 200`
  - 静态：`/styles.css -> 200`，`/app.js -> 200`
  - API：`/api/session -> 200`，`/api/features -> 200`
  - 异常请求：
    - 非法 JSON：`POST /api/connect -> 400`
    - 超大请求体：`POST /api/connect -> 413`
    - 网络超时（不可达 IP）：`POST /api/connect -> 400(timed out)`
  - 安全：
    - 路径穿越 `/../../server.py -> 403`
  - 兼容性观察：
    - `/static/styles.css -> 404`（当前页面实际使用 `/styles.css`）

### 3.4 数据库读写

- 未发现数据库驱动/ORM调用路径（SpotZoom 核心无 DB 读写链路）。
- `xps_admin_console` 为 HTTP + XPS/FTP 访问，不是本地 DB 架构。

### 3.5 前端控制台日志

- 本轮环境未提供浏览器自动化控制台抓取（Playwright/Selenium 仍缺失），
  仅完成静态资源可达性与 JS 语法检查：
  - `node --check static/app.js`：通过

### 3.6 代码质量与模块结构

- `artifacts/code_quality_v50.json`
  - `SpotZoom.py`：16699 行，`parse_args` 约 2340 行，`check_environment` 分支节点 575
  - `frontier_apply_method_count=25`，前沿模块链条持续增长
  - `xps_admin_console/server.py`：1011 行，API 分发集中在 `_handle_api_get/_post`

## 4. 问题清单（优先级 / 影响模块 / 修复建议）

### P1 - 噪声场景下 v50 拒绝率偏高，存在控制震荡风险

- 现象：
  - `frontier_v50_assoc_rejects=8/11`（offcenter noisy）
  - 对齐阶段出现多次重复修正动作，耗时上升到 3.353s
- 影响模块：
  - `SpotZoom.py`：`_apply_frontier_v50_assoc`、`_align_once` 联动路径
  - `innovation_frontier_v50.py`：`update` 门控参数
- 建议：
  - 下调 `min_low_confidence_score` 或提高低置信路径权重
  - 引入“连续拒绝后动态放宽 gate”的短时自适应策略
  - 在控制层加“拒绝后单步冻结/半步修正”抑制振荡

### P1 - 文案编码混乱导致 API 文本乱码

- 现象：
  - `/api/features` 返回中文摘要存在乱码（mojibake）
- 影响模块：
  - `XPS/xps_admin_console/server.py` 的 `FEATURES` 文案区域
- 建议：
  - 统一源文件编码为 UTF-8，无 BOM，批量修复历史乱码文本
  - 加入接口层文本回归测试（至少校验关键中文字段）

### P2 - 静态路径兼容性不一致（`/static/*` 404）

- 现象：
  - `/styles.css` 正常，但 `/static/styles.css` 404
- 影响模块：
  - `xps_admin_console/server.py` 静态路由策略
- 建议：
  - 增加 `/static/` 前缀兼容映射或统一前端引用策略
  - 在监控脚本固定双路径巡检，避免部署环境差异触发白屏

### P2 - 模块结构耦合与重复逻辑持续扩大

- 现象：
  - `SpotZoom.py` 单文件超大，`_apply_frontier_vXX` 大量相似流程重复
- 影响模块：
  - `SpotZoom.py` 全局
- 建议：
  - 抽象统一的 `AssociationAdapter` 接口与注册表
  - 将参数校验从 `check_environment` 拆分为独立 validators
  - 把 `parse_args` 切分为多组 builder 函数

## 5. 下一步优化方案（建议顺序）

1. 对 v50 做参数 A/B：
   - 重点扫描 `min_low_confidence_score`、`low_confidence_gate_scale`、`drift_gate_scale`
   - 目标：`rejects` 降低 30%+，同时不提升误检偏移
2. 修复 `FEATURES` 文案乱码并做 API 文本快照测试。
3. 增加 `/static/*` 兼容路由，避免不同网关下资源路径失败。
4. 启动 `SpotZoom.py` 结构拆分：
   - `frontier_pipeline.py`、`cli_args.py`、`validators.py` 三段优先拆分。

