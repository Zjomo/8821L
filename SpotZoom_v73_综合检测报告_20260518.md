# SpotZoom v73 综合检测报告（2026-05-18）

## 1. 本轮结论
- 已在 `SpotZoom.py` 新增 **frontier v73 retrack guard**（ReTracker 风格局部模板重匹配 + 低 agreement 连续触发），并完成配置、CLI、参数校验、指标、bundle、主链接入。
- 构建与静态检查通过：
  - `python -m py_compile 8821L/SpotZoom.py`
  - `python -m py_compile XPS/xps_admin_console/server.py`
  - `python -m compileall 8821L/SpotZoom.py 8821L/SpotZoom_Machine_Learning XPS/xps_admin_console`
  - `node --check XPS/xps_admin_console/static/app.js`
- SpotZoom 六场景回放（v73）中：`smoke/offcenter_noisy/probe/reject_probe` 成功，`missing_image/xps_fail` 按预期失败覆盖异常路径。
- XPS 页面/API/前端日志巡检完成：HTTP 状态 `200x7, 400x3, 404x2, 413x1`；Playwright `console/pageErrors/requestFailed = 0/0/0`。

## 2. 互联网前沿类似项目（主来源）与可模仿模块

| 项目 | 主来源 | 可模仿模块（映射 SpotZoom） |
|---|---|---|
| ReTracker (ICCV 2025) | https://openaccess.thecvf.com/content/ICCV2025/html/Tan_ReTracker_Exploring_Image_Matching_for_Robust_Online_Any_Point_Tracking_ICCV_2025_paper.html | 局部图像匹配重定位（已映射为 v73：低 agreement 下 template rematch） |
| Track-On | https://github.com/gorkaydemir/track_on | memory + verifier 在线机制（v72 已有，v73 承接其低 agreement 触发） |
| CoTracker3 | https://arxiv.org/abs/2410.11831 | 可见性/遮挡连续性信号（可继续增强 v71/v72/v73 的可见性状态机） |
| TAPIP3D (NeurIPS 2025) | https://papers.nips.cc/paper_files/paper/2025/hash/c4edc5113b4ffd4632718558fb66b9ef-Abstract-Conference.html | 长序列时序一致性训练与稳定性指标（可用于离线回放集扩充） |
| Trackpy | https://soft-matter.github.io/trackpy/dev/ | 搜索半径与轨迹连续性启发式（可用于 v73 动态 search radius） |
| micro-sam | https://github.com/computational-cell-analytics/micro-sam | 显微分割+跟踪结合策略（可扩展 v73 在低纹理场景的 patch 选择） |
| OpenCV Template Matching | https://docs.opencv.org/4.x/d4/dc6/tutorial_py_template_matching.html | `TM_CCOEFF_NORMED` 匹配评分与局部窗口重定位（v73 当前实现核心） |

## 3. SpotZoom.py 本轮补充（v73）
- 新增默认配置键：
  - `frontier_v73_retrack_guard_enabled`
  - `frontier_v73_low_agreement_streak_trigger`
  - `frontier_v73_patch_radius_px`
  - `frontier_v73_search_radius_px`
  - `frontier_v73_min_match_score`
  - `frontier_v73_max_shift_px`
  - `frontier_v73_confidence_penalty`
  - `frontier_v73_confidence_boost`
- 新增运行指标：
  - `frontier_v73_guard_checks/refines/rejects/soft_accepts/failures/triggered`
  - `frontier_v73_match_attempts/match_success/match_low_score`
- 新增运行态：
  - `_frontier_v73_prev_gray`、`_frontier_v73_prev_center`
  - `_frontier_v73_low_agreement_streak`
  - `_frontier_v73_last_v72_low_agreement`、`_frontier_v73_last_v72_rejects`
- 新增守卫方法：`_apply_frontier_v73_retrack_guard(...)`
  - 触发条件：v72 低 agreement 连续计数/拒绝增量
  - 动作：对上一帧模板做局部 NCC 重匹配，约束最大位移并更新置信度
- 主链接入：在 `v72` 之后串接 `v73`。
- Bundle 接入：`microscopy_tracking`、`resilient_association`、`frontier_2026_tracking_stack` 默认启用 v73。

## 4. 自动检测结果（本轮 v73）

### 4.1 环境检查
- `python SpotZoom.py --check-env`：`EXIT=1`
  - 错误：`Missing dependency for motor control: pylablib`
- `python SpotZoom.py --check-env --xy-driver dryrun --z-driver dryrun`：`EXIT=0`
  - 告警：`ultralytics` 缺失，自动降级 classic backend。

### 4.2 SpotZoom 六场景回放
来源：`artifacts/spotzoom_case_matrix_v73.json` + `artifacts/spotzoom_run_summary_v73.json`

| case | exit_code | status | elapsed_s | v72 low_agree | v73 triggered/refines/rejects | 备注 |
|---|---:|---|---:|---:|---:|---|
| smoke | 0 | success | 5.059 | 3 | 2 / 0 / 0 | 稳态通过 |
| offcenter_noisy | 0 | success | 5.130 | 3 | 2 / 2 / 0 | v73 触发并修正 |
| probe | 0 | success | 6.817 | 4 | 4 / 3 / 0 | v73 有效参与 |
| reject_probe | 0 | success | 10.656 | 7 | 7 / 7 / 0 | v73 高频介入 |
| missing_image | 1 | failed(预期) | 0.010 | 0 | 0 / 0 / 0 | 缺失输入异常覆盖 |
| xps_fail | 1 | failed(预期) | 2.044 | 0 | 0 / 0 / 0 | XPS 连接失败覆盖 |

补充事件统计（JSONL）：
- `spotzoom_events_v73_reject_probe.jsonl` 中 `frontier_v73_retrack_refined` = 7。
- 本轮 v73 主矩阵未出现 `frontier_v73_retrack_failed`。

### 4.3 XPS 页面/API/网络/前端日志
来源：`artifacts/xps_http_check_v73.json`
- 页面与静态资源：`/`、`/static/app.js`、`/static/styles.css` 均 200。
- API 覆盖：`/api/features`、`/api/session`、`/api/disconnect` 均 200。
- 异常覆盖：`/api/unknown` 404、`/static/not_found.js` 404、`/api/connect` 400、`/api/controller/raw` 400、`/api/files/upload` 400/413。
- Playwright 运行时：`console=0`、`pageErrors=0`、`requestFailed=0`。

### 4.4 数据库/配置读写链路
来源：`artifacts/xps_db_check_v73.json`
- `GET /api/system/stages-db`：200（未连接返回错误对象）
- `GET /api/system/config`：200（未连接返回错误对象）
- `POST /api/files/upload`：
  - 非法 base64 -> 400（参数校验有效）
  - 合法 payload + 未连接 -> 400（连接态校验有效）

## 5. 代码质量与模块结构
- `SpotZoom.py`：`23666` 行，`22` class，`265` def。
- `parser.add_argument(...)`：`651`。
- `except Exception`：`139`。
- Top 函数长度：
  - `parse_args` 3543 行
  - `SpotZoomController.__init__` 2324 行
  - `check_environment` 1296 行
- 数据库关键字扫描（SpotZoom + xps_admin_console）：未发现 SQLite/MySQL/PostgreSQL/Redis/Mongo 调用关键词（本项目仍是文件/接口驱动）。

## 6. 问题分级（优先级 / 影响模块 / 修复建议）

### P1
1. 高噪声 + 激进触发参数下存在回归风险（AB 复测出现一次失败）
- 现象：`v73_ab_reject_enabled` 在 `--frontier-v73-low-agreement-streak-trigger 1` 下出现 `max alignment rounds reached`。
- 影响模块：`SpotZoom.py` v73/v72/v57 组合链路。
- 建议：
  - 默认保持 `streak_trigger=2`（不要在生产默认使用 1）。
  - 增加 `v73` 触发冷却（例如连续触发后 1-2 轮仅观测不改写）。
  - 在 `reject_probe` 压测下引入固定随机种子回放，降低随机噪声导致的结论抖动。

2. 默认环境检查仍对硬件依赖敏感
- 现象：默认 `--check-env` 因 `pylablib` 缺失返回 1。
- 影响模块：`check_environment`。
- 建议：在 dryrun + frame-source 模式将硬件依赖下调为 warning（不阻断 CI）。

### P2
1. 前端响应体仍有编码异常痕迹
- 现象：`/` 返回片段出现 `鏈繛鎺?/div>` 等乱码。
- 影响模块：`XPS/xps_admin_console/static/index.html`。
- 建议：统一 UTF-8（无 BOM）并校验模板文本节点闭合。

2. SpotZoom 单体持续膨胀，回归面扩大
- 影响模块：`parse_args`、`check_environment`、`SpotZoomController.__init__`。
- 建议：优先拆 `v57-v73 guard pipeline` 与 CLI 参数注册模块。

### P3
1. 广泛异常捕获较多（`except Exception=139`）
- 影响：定位根因成本高。
- 建议：分层收窄异常类型并统一错误码上报。

## 7. 下一步优化方案
1. 在 v73 中加入“触发冷却 + 触发预算”（避免同一轮过密重匹配）。
2. 补充 `reject_probe` 固定种子回放集，做 v72/v73 参数网格 AB（`streak_trigger`, `min_match_score`, `max_shift_px`）。
3. 修复 `index.html` 编码/标签异常后重跑 Playwright + HTTP 探针。
4. 启动 SpotZoom 模块化拆分：先拆 CLI 与 guard 链，降低回归耦合。
