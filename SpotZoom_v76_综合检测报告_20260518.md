# SpotZoom v76 综合检测报告（2026-05-18）

## 1. 本轮目标与代码补充

- 目标：基于互联网前沿类似项目能力，对 `SpotZoom.py` 在 v75 后补充可落地模块，并完成构建、启动、页面访问、核心流程、接口、数据库读写、前端日志、网络异常、静态资源、代码质量与结构的自动化检查。
- 新增模块：`frontier_v76_verifier_fusion_guard`（verifier 评分 + 软融合纠偏守卫）。
  - 触发逻辑：v75 出现触发但未修正，或出现 reject/ambiguity/backward/phase 失败信号时介入。
  - 核心动作：局部模板重匹配 + 多信号可靠度融合（match score / peak margin / phase response / backward consistency）+ 受限位移软融合纠偏。
  - 接线范围：默认配置、bundle 预设、`AlignmentConfig`、运行时状态、指标埋点、CLI 参数、参数校验、主关联流水线。

## 2. 互联网前沿类似项目调研与可模仿模块

> 说明：下列结论均来自项目官方仓库/官方文档。

1. CoTracker3（Meta FAIR）
- 链接：<https://github.com/facebookresearch/co-tracker>
- 可模仿点：online 跟踪中的置信信号/伪标签强化、针对长时序的鲁棒一致性。
- 映射到 SpotZoom：v76 将硬拒绝链路后补充“可靠度打分 + 软纠偏”，避免纯 reject 导致纠偏机会损失。

2. TAPIR / TAPNet（Google DeepMind）
- 链接：<https://github.com/google-deepmind/tapnet>
- 可模仿点：Tracking Any Point 的点级跟踪与可见性/可靠性思路。
- 映射到 SpotZoom：v76 使用多信号融合可靠度（含 phase/backward），做低风险融合修正。

3. Track-On
- 链接：<https://github.com/gorkaydemir/track_on>
- 可模仿点：更贴近真实世界噪声与遮挡条件下的点跟踪鲁棒性策略。
- 映射到 SpotZoom：v76 仅在 v75 压力信号出现时介入，降低常态开销并聚焦高风险帧。

4. ByteTrack
- 链接：<https://github.com/FoundationVision/ByteTrack>
- 可模仿点：在关联阶段保留低分候选并做二次决策，而非简单丢弃。
- 映射到 SpotZoom：v76 在 v75 后增加“可解释的二次打分与软融合”，降低过早拒绝。

5. OpenCV 官方文档
- `phaseCorrelate`：<https://docs.opencv.org/4.x/d7/df3/group__imgproc__motion.html>
- Template Matching：<https://docs.opencv.org/4.x/de/da9/tutorial_template_matching.html>
- 可模仿点：相位相关响应作为可靠性信号；模板匹配用于局部峰值重定位。
- 映射到 SpotZoom：v76 延续 v75 的 patch rematch 并在评分层融合 phase/backward 信号。

## 3. 自动化检查执行项

- 构建/静态检查
  - `python -m py_compile 8821L/SpotZoom.py`
  - `python -m compileall 8821L/SpotZoom.py 8821L/SpotZoom_Machine_Learning`
  - `python -m py_compile XPS/xps_admin_console/server.py`
  - `node --check XPS/xps_admin_console/static/app.js`
- SpotZoom 核心流程回放（6 case）
  - `smoke / offcenter_noisy / probe / reject_probe / missing_image / xps_fail`
- 页面访问 / 接口请求 / 网络异常 / 静态资源
  - `/`, `/static/app.js`, `/static/styles.css`, `/api/features`, `/api/session`, `/api/unknown`, `/static/not_found.js`
  - 异常请求：`/api/connect` timeout、`/api/controller/raw` not connected、`/api/files/upload` bad base64 / oversized payload
- 前端运行时
  - Playwright 抓取 `console/pageErrors/requestFailed`
- DB 路径
  - `GET /api/system/stages-db`、`GET /api/system/config`、`POST /api/files/upload`
- 代码质量与结构
  - AST/统计：LOC、函数规模、长函数数量、耦合指标信号

## 4. 本轮检测结果

### 4.1 SpotZoom case matrix（v76）

- 汇总：`total=6, success=3, failed=3`
- 成功：`smoke / offcenter_noisy / probe`
- 失败：`reject_probe / missing_image / xps_fail`
  - `missing_image`、`xps_fail` 为预期失败路径。
  - `reject_probe` 本次出现 `max alignment rounds reached`。
- 关键观察：v76 有触发（`frontier_v76_triggered`），但本轮 `frontier_v76_guard_refines=0`，未形成正向纠偏落地。

### 4.2 reject_probe 回归风险复核（AB）

- `baseline_v75`: `3/3 success`
- `enabled_v76`: `3/3 success`
- 结论：矩阵中 `reject_probe` 的失败更接近高噪声尾部非确定性风险，而非稳定复现回归。

### 4.3 页面/API/网络/静态资源

- HTTP 状态画像：`200x7, 400x3, 404x2, 413x1`
- 页面与静态资源可访问：`/`, `/static/app.js`, `/static/styles.css`
- 预期异常请求命中：`connect timeout`、`not connected`、`bad base64`、`oversized payload`

### 4.4 前端控制台与请求失败

- Playwright：
  - `console = 0`
  - `pageErrors = 0`
  - `requestFailed = 0`

### 4.5 数据库读写

- `read_stages_db` / `read_system_config`：HTTP 200，但返回 `Not connected` 分支。
- 写入路径：
  - 非法 base64 -> 400
  - 合法 payload + 未连接 -> 400
- 结论：真实控制器连接态下的 DB 读写分支仍未覆盖。

### 4.6 代码质量与结构

- `SpotZoom.py`：`24765 LOC`、`268 functions`、`>=200行函数 19 个`
- 高耦合信号：`self.cfg.` 引用 `1513` 次，`self._inc_metric(...)` `482` 次。
- 结构风险：`parse_args`、`__init__`、`check_environment` 超长，回归影响面大。

## 5. 问题优先级、影响模块、修复建议

### P1：高噪声尾部不稳定（`reject_probe` 间歇失败）
- 影响模块：v57-v76 关联链路、对齐轮次控制。
- 现象：单轮矩阵出现 `max alignment rounds reached`，AB 复核显示非稳定复现。
- 建议：
  - 在 `reject_probe` 场景补 `N>=20` 稳定性回放（固定随机种子）统计 p95/p99。
  - 对 `max_align_rounds` 与 `tolerance_px` 做联动策略，避免极端噪声下无效迭代。

### P1：v76 已触发但无实质纠偏（`guard_refines=0`）
- 影响模块：`_apply_frontier_v76_verifier_fusion_guard`。
- 现象：触发存在，但没有形成“纠偏成功”样本。
- 建议：
  - 进行离线网格调参：`min_reliability_score`、`blend_strength_[min,max]`、`max_shift_px`。
  - 将 `shift_norm` 软接受阈值由固定值改为与噪声强度/漂移统计联动（自适应阈值）。

### P2：模块耦合高、函数过长，回归风险集中
- 影响模块：`parse_args`、`__init__`、`check_environment`、v57+ 关联总线。
- 建议：
  - 将 v73-v76 guard 提取为独立策略类（统一输入输出协议）。
  - 将参数定义与校验分层（schema + validator），缩短主文件关键函数长度。

### P2：DB 连接态覆盖缺口
- 影响模块：XPS 控制台后端 `/api/system/*`、`/api/files/upload` 连接分支。
- 建议：在可连硬件/仿真控制器环境补齐 connected 分支回归基线。

## 6. 本轮产物

- `8821L/SpotZoom.py`（v76 模块补充）
- `8821L/artifacts/spotzoom_help_v76.txt`
- `8821L/artifacts/spotzoom_case_matrix_v76.json`
- `8821L/artifacts/spotzoom_case_metrics_v76.json`
- `8821L/artifacts/spotzoom_run_summary_v76.json`
- `8821L/artifacts/xps_http_check_v76.json`
- `8821L/artifacts/xps_db_check_v76.json`
- `8821L/artifacts/spotzoom_code_quality_v76.json`
- `8821L/artifacts/spotzoom_v76_ab_reject_probe.json`
- `8821L/artifacts/spotzoom_v76_ab_reject_probe_summary.json`

## 7. 下一步优化方案

1. 做 `v76` 参数网格搜索（先离线回放，再接在线灰度），目标是把 `guard_refines` 提升到可观测水平且不增大 reject 尾部风险。
2. 增加固定随机种子的稳定性测试集（尤其 `reject_probe`），把“单轮偶发失败”量化成可比较指标。
3. 拆分 `SpotZoom.py` 的 guard 管线与参数层，降低耦合并缩小回归面。
4. 在可连控制器环境补全 DB connected 分支回归，形成端到端验收闭环。
