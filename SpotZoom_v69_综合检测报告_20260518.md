# SpotZoom v69 综合检测报告（2026-05-18）

## 1. 前沿同类项目调研（本轮）

1. BoT-SORT（含相机运动补偿 CMC）
- 仓库: https://github.com/NirAharon/BoT-SORT
- 论文: https://arxiv.org/abs/2206.14651
- 可模仿模块:
  - 基于全局运动估计的 camera motion compensation；
  - 在关联前先做运动补偿，降低视角抖动带来的误拒绝。

2. ByteTrack
- 仓库: https://github.com/ifzhang/ByteTrack
- 论文: https://arxiv.org/abs/2110.06864
- 可模仿模块:
  - 低分框二次关联（不是直接丢弃），提升遮挡/噪声场景召回。

3. Norfair
- 文档: https://tryolabs.github.io/norfair/2.1/reference/tracker/
- 可模仿模块:
  - `hit_counter_max / initialization_delay / pointwise_hit_counter_max` 的轨迹惯性建模。

4. trackpy Adaptive Search
- 文档: https://soft-matter.github.io/trackpy/dev/tutorial/adaptive-search.html
- 可模仿模块:
  - 逐步收缩搜索门限（`adaptive_step / adaptive_stop`），平衡稳定性与速度。

5. CoTracker3
- 仓库: https://github.com/facebookresearch/co-tracker
- 可模仿模块:
  - 点级 `pred_visibility` 作为可见性信号，驱动 guard 门限松弛。

6. SAHI
- 文档: https://obss.github.io/sahi/quick-start/
- 可模仿模块:
  - 切片窗口检索 + overlap 融合，用于小目标和局部高噪声补救。

## 2. SpotZoom.py 本轮补充（v69）

目标文件: `E:\jupyter file\2_Optics\8821L\SpotZoom.py`

新增 `frontier v69 camera-motion compensated guard`（接在 v68 之后）：
- 核心思路:
  - BoT-SORT 风格全局相机位移估计（`phaseCorrelate`）补偿预测轨迹；
  - trackpy 风格残差门限自适应（可见性/拒绝压力/漂移惩罚）；
  - 模板救援（上一帧模板 + 当前帧 ROI 匹配）做 refine/reject。

新增配置（默认配置 / `AlignmentConfig` / CLI / 参数校验 / bundle / metrics 全链路接入）：
- `frontier_v69_cmc_guard_enabled`
- `frontier_v69_history_size`
- `frontier_v69_base_gate_px`
- `frontier_v69_phase_response_min`
- `frontier_v69_cmc_gain`
- `frontier_v69_cmc_response_gain`
- `frontier_v69_visibility_relax_threshold`
- `frontier_v69_visibility_relax_gain`
- `frontier_v69_reject_relax_gain`
- `frontier_v69_drift_penalty_gain`
- `frontier_v69_max_rescue_shift_px`
- `frontier_v69_template_radius_px`
- `frontier_v69_template_min_std`
- `frontier_v69_template_min_score`
- `frontier_v69_confidence_penalty`
- `frontier_v69_confidence_boost`

新增运行指标：
- `frontier_v69_guard_checks`
- `frontier_v69_guard_refines`
- `frontier_v69_guard_rejects`
- `frontier_v69_guard_soft_accepts`
- `frontier_v69_guard_failures`
- `frontier_v69_rescue_candidates`

主链路更新：
- `v57 -> v58 -> v59 -> v63 -> v64 -> v65 -> v66 -> v67 -> v68 -> v69`

open-source bundle 联动：
- `microscopy_tracking`
- `resilient_association`
- `frontier_2026_tracking_stack`

## 3. 自动化检测结果

### 3.1 构建 / 启动 / 语法

- `python -m py_compile 8821L/SpotZoom.py`: PASS
- `python -m py_compile XPS/xps_admin_console/server.py`: PASS
- `node --check XPS/xps_admin_console/static/app.js`: PASS
- `python SpotZoom.py --check-env ... --frontier-v69-cmc-guard --skip-roi`: PASS
  - 产物: `8821L/artifacts/spotzoom_check_env_v69.txt`

### 3.2 SpotZoom 核心流程回放

1) `spotzoom_run_report_v69_smoke.json`
- 状态: success
- 指标: `detect_success=5/5`
- v69: `checks=5, refines=0, rejects=0, soft_accepts=5, rescue_candidates=0`

2) `spotzoom_run_report_v69_offcenter_noisy.json`
- 状态: success
- 指标: `detect_success=5/5`
- v69: `checks=5, refines=0, rejects=0, soft_accepts=5, rescue_candidates=0`

3) `spotzoom_run_report_v69_probe.json`
- 状态: failed（`Iter 1 align P3` 超过最大对齐轮次）
- 指标: `detect_success=12/12`, `elapsed_s=10.185`
- v69: `checks=12, refines=3, rejects=7, soft_accepts=2, rescue_candidates=10`

4) `spotzoom_run_report_v69_reject_probe.json`
- 状态: success
- 指标: `detect_success=12/12`, `elapsed_s=10.167`
- v69: `checks=11, refines=3, rejects=6, soft_accepts=2, rescue_candidates=9`
- 说明: reject 分支被真实触发，同时保持可收敛。

5) 故障注入
- `spotzoom_run_report_v69_missing_image.json`: failed（符合预期，图像不存在）
- `spotzoom_run_report_v69_xps_fail.json`: failed（符合预期，XPS 连接失败）

### 3.3 页面访问 / 静态资源 / 接口请求 / 网络异常 / 前端控制台

- 文件: `8821L/artifacts/xps_http_check_v69.json`
- 服务日志: `8821L/artifacts/xps_server_v69_stdout.log`
- 状态分布: `200 x7`, `404 x2`, `400 x3`, `413 x1`
- 覆盖项:
  - 页面: `/`
  - 静态资源: `/static/app.js`, `/static/styles.css`
  - 接口: `/api/features`, `/api/session`, `/api/connect`, `/api/controller/raw`, `/api/files/upload`, `/api/disconnect`
  - 异常请求: `/api/unknown`, `/static/not_found.js`, 非法 base64 上传、超大上传
- Playwright runtime:
  - 当前环境中 `page.goto` 超时（`playwright_failed`），本轮未采到有效 `requestFailed` 样本。

### 3.4 数据库读写

- 对 `SpotZoom.py`、`server.py`、`app.js` 做关键字扫描，未发现 sqlite/mysql/postgres/redis/mongodb 等数据库读写路径。

### 3.5 代码质量与模块结构

- `SpotZoom.py`: 22248 行，`except Exception` 135 处。
- `SpotZoomController.__init__`: 约 2248 行（高耦合热点）。
- `XPS/xps_admin_console/server.py`: 1002 行，`except Exception` 13 处。
- `XPS/xps_admin_console/static/app.js`: 994 行。

## 4. 问题分级（优先级 / 影响模块 / 修复建议）

### P1

1) `probe` 场景仍可能不收敛（对齐轮次耗尽）
- 影响模块: `SpotZoom.py`（v69 与 v68/v67 串联）
- 证据: `spotzoom_run_report_v69_probe.json` + `spotzoom_case_probe_v69.log`
- 建议: 增加“连续 reject 熔断回退到 v67/v68”与门限动态放宽策略（按 reject_streak/噪声估计）。

2) 前端 runtime 采集链路不稳定
- 影响模块: 检测脚本与 XPS 页面 runtime 采样环节
- 证据: `xps_http_check_v69.json` 的 `playwright_failed`
- 建议: 将 `page.goto` 改成 `commit`/更短路由检查，并对外链字体请求做本地化后再采样。

### P2

3) 文案编码乱码仍存在
- 影响模块: `XPS/xps_admin_console/server.py`（`FEATURES` 文案）与前端显示
- 证据: `/api/features` 返回 `summary` 字段乱码。
- 建议: 统一 UTF-8 源码与文件保存编码，补回归快照测试。

4) 主控模块耦合偏高
- 影响模块: `SpotZoom.py`
- 证据: 超大 `__init__` + 多版本 guard 串行堆叠。
- 建议: 抽离 `v58~v69` guard pipeline 为独立可插拔模块。

### P3

5) 泛化异常捕获较多
- 影响模块: `SpotZoom.py`, `server.py`
- 证据: `except Exception` 总量较高。
- 建议: 按设备层/检测层/HTTP 层替换为 typed exception，收敛异常语义。

## 5. 下一步优化方案

1. 给 v69 增加 reject 熔断与回退序列（`v69 -> v68 -> v67`），优先保障收敛。
2. 对 `probe` 建立参数 AB 基线（至少一组“触发 refine/reject 且收敛成功”配置）。
3. 前端去外网字体依赖（本地字体/系统字体回退），再重跑 Playwright console/network 采样。
4. 拆分 `SpotZoomController` 的后处理守卫链，降低回归冲突面。
5. 规范 `server.py` 错误码与 HTTP 状态语义（400/408/409/422/5xx）。
