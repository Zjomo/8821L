# SpotZoom v62 综合检测与优化报告（2026-05-17）

## 1. 本轮目标与改动范围
- 目标：在已有 v59 共识守门补充基础上，完成一轮“构建/启动/页面/API/核心流程/故障注入/结构质量”自动检测，并给出优先级修复建议。
- 代码改动：本轮未新增业务代码改动；沿用上一轮已完成的 `SpotZoom.py` v59 补充进行回归验证。

## 2. 互联网前沿相似项目与可模仿模块
参考项目（官方仓库）：
- CoTracker（Meta）：https://github.com/facebookresearch/co-tracker
- TAPNet / TAPNext++（Google DeepMind）：https://github.com/google-deepmind/tapnet
- Track-On（ICLR 2025 / TPAMI 2026）：https://github.com/gorkaydemir/track_on
- SAM2（Meta）：https://github.com/facebookresearch/sam2
- Norfair：https://github.com/tryolabs/norfair
- DeepTrack2：https://github.com/DeepTrackAI/DeepTrack2

可继续模仿的模块：
1. 长时遮挡重检（TAPNext++/Track-On）
- 增加“遮挡态 -> 重现态”显式状态机，建立 re-detect 冷却窗口与多证据确认。
2. 在线记忆分层（CoTracker/Track-On）
- 将短时轨迹记忆、长期先验记忆、教师共识记忆彻底解耦为独立策略对象，减少 SpotZoomController 内耦合。
3. 视频分割状态缓存（SAM2）
- 将当前 SAM2 refiner 的帧级处理升级为可选“跨帧状态缓存”，降低抖动场景下误检漂移。
4. 轻量跟踪核（Norfair）
- 提供可替换的 distance/kalman/re-id 接口层，便于在不同算力档位下切换策略。
5. 仿真数据回放管线（DeepTrack2）
- 增加统一的 synthetic scene + 噪声模板回放器，提升回归用例可重复性。

## 3. 本轮自动检测执行结果

### 3.1 构建与静态语法
- `python -m py_compile SpotZoom.py`：PASS
- `python -m py_compile XPS/xps_admin_console/server.py`：PASS
- `node --check XPS/xps_admin_console/static/app.js`：PASS

### 3.2 SpotZoom 启动与核心流程（v62 新产物）
- `spotzoom_run_report_v62_smoke.json`：success
  - v57_calls=5, v58_checks=5/rejects=0, v59_checks=5/rejects=0
- `spotzoom_run_report_v62_offcenter_noisy.json`：success
  - v57_calls=5, v58_checks=5/rejects=0, v59_checks=5/rejects=0
- `spotzoom_run_report_v62_v59_probe.json`：success（严格阈值探针）
  - v59_checks=5, v59_rejects=4，事件 `frontier_v59_consensus_rejected`=4
- `spotzoom_run_report_v62_missing_image.json`：failed（预期失败）
  - error=`Simulation image not found: __missing_image__.jpg`
- `spotzoom_run_report_v62_xps_fail.json`：failed（预期失败）
  - error=`XPS controller connection failed`

### 3.3 页面访问 / 接口请求 / 静态资源加载
- 启动 `xps_admin_console/server.py` 后 HTTP 探测结果（`artifacts/xps_http_check_v62.json`）：
  - 200：`/`、`/static/app.js`、`/static/styles.css`、`/api/features`、`/api/session`
  - 404：`/api/unknown`（符合预期）
  - 400：`POST /api/connect`（不可达目标）、`POST /api/controller/raw`（未连接状态）
- 服务日志：`artifacts/xps_server_v62_stdout.log`
  - 可见 timeout / not connected / getaddrinfo 失败等异常路径被触发。

### 3.4 数据库读写与文件读写链路
- 未发现 sqlite/mysql/postgres 等数据库驱动调用。
- XPS “stages database/system config” 接口为控制器文件读取语义（`/Admin/Config/*.ini`），非本地数据库。
- 文件列表接口在未连接时返回 400，符合当前保护策略。

### 3.5 前端控制台日志
- 本轮未采集浏览器 Console（当前环境缺少可直接复用的浏览器自动化运行时）。
- 已完成后端 stdout 日志与 HTTP 失败链路覆盖，可作为替代证据。

### 3.6 代码质量与模块结构
- `SpotZoom.py`：LOC=19396，Functions=239，Classes=22
- `SpotZoomController` 单类体积：10228 行
- `parse_args`：2787 行
- `except Exception`：126 处
- `_apply_frontier_vXX_assoc` 系列方法：22 个

## 4. 问题清单（优先级 / 影响模块 / 建议）

### P1
1. 异常语义折叠：POST 通用异常统一映射 400
- 影响模块：`XPS/xps_admin_console/server.py`（约 883-885 行）
- 现象：timeout、未连接、解析失败等都返回 400，诊断粒度不足。
- 建议：
  - `TimeoutError/ConnectionError -> 504/502`
  - 业务前置条件（未连接）-> 409 或 503
  - 非法参数/JSON -> 400

2. 敏感超长输入被原样写日志，存在日志膨胀风险
- 影响模块：`XPS/xps_admin_console/server.py`（connect 日志）
- 现象：`artifacts/xps_server_v62_stdout.log` 最大行长 70071 字符。
- 建议：对 host/command/path 做长度截断与可打印字符过滤（如 128 或 256 上限）。

### P2
1. SpotZoom 单体耦合过高，回归风险随版本叠加增加
- 影响模块：`SpotZoom.py`（`SpotZoomController` 与 `parse_args`）
- 现象：10k+ 行控制器 + 22 条 vXX 关联链，重复逻辑明显。
- 建议：提取 `frontier pipeline` 插件化接口（策略注册 + 统一输入输出契约）。

2. 广域 `except Exception` 数量高（126）
- 影响模块：`SpotZoom.py` 多处
- 现象：错误分类与报警粒度不足，易掩盖回归。
- 建议：按依赖域细分异常类型，并把不可恢复错误上抛到 run report。

3. 前端 HTML 文本编码/标签异常
- 影响模块：`XPS/xps_admin_console/static/index.html`
- 现象：中文文案出现乱码，且存在 `...?/div>` 形态（如 24 行）导致结构不规范。
- 建议：统一 UTF-8 无 BOM 保存，修复错误闭合标签并引入最小 HTML 结构校验。

### P3
1. 前端 Console 自动采集缺失
- 影响模块：测试基础设施
- 建议：补充 Playwright/浏览器插件链路，定时抓取 console error + failed request。

2. YOLO/PyTorch 依赖告警常驻
- 影响模块：运行环境
- 建议：将可选依赖告警分级（首次提示 + 静默模式），避免干扰主诊断输出。

## 5. 下一步优化方案
1. 一周内
- 完成 XPS API 异常映射分层与日志截断。
- 增加前端 HTML 编码修复与基础 DOM 结构校验。

2. 两周内
- 抽离 `SpotZoomController` 中 v5x-v59 关联链为策略注册表（最小可运行子集先拆 v57/v58/v59）。
- 增加 v59 参数自动调参与 A/B 回归脚本（baseline vs strict probe）。

3. 持续项
- 引入浏览器端 console/network 自动采集。
- 将 `except Exception` 逐步收敛为 typed exception + 错误码。
