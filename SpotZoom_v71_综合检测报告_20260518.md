# SpotZoom v71 综合检测报告（2026-05-18）

## 1. 本轮目标与结论
- 在 `SpotZoom.py` 新增 **frontier v71 occlusion guard**（遮挡感知 + 动量回补），并完整接入默认配置、open-source bundle、`AlignmentConfig`、CLI、参数校验、运行指标与主链（`v57 -> ... -> v70 -> v71`）。
- 构建与语法检查通过：`py_compile(SpotZoom.py/server.py)`、`node --check(app.js)` 全部 PASS。
- XPS 管理页、静态资源、核心 API 与异常请求覆盖完成；Playwright 运行时 `console/pageErrors/requestFailed` 均为 `0`。
- v71 分支在 `probe/reject_probe` 场景真实触发（`frontier_v71_occlusion_refined` 事件出现），但 `probe/reject_probe` 仍未收敛，主风险仍在高噪声偏移场景。

## 2. 互联网前沿类似项目与可模仿模块（Primary Sources）

| 项目 | 前沿点 | 建议映射到 SpotZoom 的模块 |
|---|---|---|
| [ByteTrack (arXiv:2110.06864)](https://arxiv.org/abs/2110.06864) | 高/低置信检测双阶段关联 | 在 v71 后补“低置信候选二次重排池”，仅对连续 reject 帧触发 |
| [BoT-SORT (arXiv:2206.14651)](https://arxiv.org/abs/2206.14651) | CMC + 强关联鲁棒性 | 把 v69 CMC 质量分数写入 v70/v71 联合门控，避免坏 CMC 放大漂移 |
| [OC-SORT (arXiv:2203.14360)](https://arxiv.org/abs/2203.14360) | Observation-Centric Re-Update | 对 v70/v71 加“观测一致性窗口”评分，降低单帧噪声误修正 |
| [OA-SORT (arXiv:2603.06034)](https://arxiv.org/abs/2603.06034) | 遮挡感知线索融合 | 继续扩展 v71 的遮挡信号（可见度/残差/置信跌落 + 轨迹状态） |
| [CoTracker (GitHub)](https://github.com/facebookresearch/co-tracker) | 点集一致性与可见性信号 | 用局部点集一致性替代单点可见度阈值，作为 v71 触发条件之一 |
| [trackpy adaptive search docs](https://soft-matter.github.io/trackpy/dev/generated/trackpy.link.html) | `adaptive_step` / `adaptive_stop` 收敛控制 | 给 v71 增加 reject-streak 驱动的步长退火 |
| [Norfair Tracker docs](https://tryolabs.github.io/norfair/2.1/reference/tracker/) | `hit_counter_max`/`initialization_delay` 生命周期管理 | 把 v70/v71 的 streak 转成显式状态机，减少链路隐式耦合 |
| [SAHI (GitHub)](https://github.com/obss/sahi) | 切片召回与小目标鲁棒检测 | 与 v68 结合做尺度自适应切片策略（按噪声级别动态调 tile/overlap） |

> 推断说明：映射建议是基于论文/官方文档能力与 SpotZoom 当前 v57-v71 工程形态的实现推断。

## 3. SpotZoom.py 本轮补充（v71）
- 新增配置/CLI参数：
  - `frontier_v71_occlusion_guard_enabled`
  - `frontier_v71_history_size`
  - `frontier_v71_occlusion_visibility_threshold`
  - `frontier_v71_occlusion_residual_gate_px`
  - `frontier_v71_confidence_drop_gate`
  - `frontier_v71_momentum_gain`
  - `frontier_v71_momentum_damping`
  - `frontier_v71_drift_penalty_gain`
  - `frontier_v71_max_offset_px`
  - `frontier_v71_confidence_penalty`
  - `frontier_v71_confidence_boost`
- 新增运行态与指标：
  - 状态：`_frontier_v71_center_history`、`_frontier_v71_velocity`、`_frontier_v71_occlusion_streak`、`_frontier_v71_last_v70_rejects`
  - 指标：`frontier_v71_guard_checks/refines/rejects/soft_accepts/failures/occlusion_hits`
- 新增守卫方法：`_apply_frontier_v71_occlusion_guard(...)`
  - 逻辑：v70 reject delta + 可见度 + 残差 + 置信跌落联合触发；基于动量预测的中心回补与限幅修正。
- 主链接入：在 `v70` 之后串接 `v71`。

## 4. 自动化检测结果（本轮）

### 4.1 构建/静态检查
- `python -m py_compile SpotZoom.py`：PASS
- `python -m py_compile XPS/xps_admin_console/server.py`：PASS
- `node --check XPS/xps_admin_console/static/app.js`：PASS

### 4.2 环境检查
- `--check-env`（默认驱动）: FAIL
  - 错误：`Missing dependency for motor control: pylablib`
- `--check-env --xy-driver dryrun --z-driver dryrun`: PASS
  - 保留告警：YOLO 依赖未安装、PyTorch 可选依赖告警

### 4.3 核心流程回放（v71）
来源：`artifacts/spotzoom_case_matrix_v71.json` + `spotzoom_run_report_v71_*.json`

| case | exit_code | status | elapsed_s | v69 rejects/refines | v70 rejects/refines | v71 checks/refines/rejects/hits |
|---|---:|---|---:|---:|---:|---:|
| smoke | 0 | success | 4.168 | 0 / 0 | 0 / 0 | 5 / 0 / 0 / 0 |
| offcenter_noisy | 0 | success | 5.949 | 0 / 0 | 0 / 0 | 5 / 0 / 0 / 0 |
| probe | 1 | failed | 13.127 | 5 / 3 | 5 / 2 | 10 / 1 / 3 / 4 |
| reject_probe | 1 | failed | 13.064 | 4 / 3 | 4 / 1 | 10 / 2 / 1 / 3 |
| missing_image | 1 | failed(预期) | 0.010 | 0 / 0 | 0 / 0 | 0 / 0 / 0 / 0 |
| xps_fail | 1 | failed(预期) | 2.094 | 0 / 0 | 0 / 0 | 0 / 0 / 0 / 0 |

补充：`spotzoom_events_v71_probe.jsonl` 出现 `frontier_v71_occlusion_refined` 1 次；`spotzoom_events_v71_reject_probe.jsonl` 出现 2 次。

### 4.4 页面访问 / 静态资源 / 接口请求 / 网络异常 / 前端日志
来源：`artifacts/xps_http_check_v71.json`
- 状态分布：`200x7, 404x2, 400x3, 413x1`
- 页面与静态资源：`/`、`/static/app.js`、`/static/styles.css` 均 200
- 核心 API：`/api/features`、`/api/session`、`/api/disconnect` 均 200
- 异常请求路径覆盖：`/api/unknown` 404、`/static/not_found.js` 404、`/api/connect` 400、`/api/controller/raw` 400、`/api/files/upload` 400/413
- Playwright runtime：`console=0 pageErrors=0 requestFailed=0`

### 4.5 数据库/配置读写链路
来源：`artifacts/xps_db_check_v71.json`
- `GET /api/system/stages-db` -> 200，返回 `Not connected`
- `GET /api/system/config` -> 200，返回 `Not connected`
- `POST /api/files/upload`（合法 payload，未连接）-> 400

结论：当前 DB/配置链路仍是“未连接错误路径”覆盖，真实连接成功路径尚未验证。

### 4.6 代码质量与结构扫描
- `SpotZoom.py`：`21735` 行
- `parser.add_argument(...)`：`632`
- `_apply_frontier_*` 方法数：`45`
- `except Exception`：`137`

## 5. 问题分级（优先级 / 影响模块 / 修复建议）

### P1
1. `probe/reject_probe` 仍失败（收敛不足）
- 影响模块：`SpotZoom.py` v69-v71 守卫链 + 对齐循环
- 风险：高噪声偏移场景回归失败
- 建议：
  - 对 `v71` 做参数 AB（`occlusion_visibility_threshold`, `residual_gate`, `momentum_gain`, `max_offset`）
  - 将 `v70/v71` 与 `max_align_rounds` 做联动熔断，避免坏帧反复耗时

2. 默认 `check-env` 对硬件依赖过硬（pylablib）
- 影响模块：环境检查入口
- 风险：无硬件环境自动化持续误判 FAIL
- 建议：`dryrun`/`frame-source-image` 模式下将硬依赖降级为 warning

### P2
1. 守卫链耦合偏高、回归面大
- 影响模块：`SpotZoom.py` 单文件多职责
- 风险：局部修改触发跨链副作用
- 建议：拆分 `v63-v71` 到独立 pipeline 模块，统一上下文与指标接口

2. API 错误语义一致性不足
- 现象：部分读取接口在“未连接”状态返回 200 + error 文本
- 影响模块：XPS admin server API
- 建议：统一错误码语义（读/写失败时返回 4xx，并保持错误体结构一致）

### P3
1. 观测指标仍可增强
- 现状：已有 `detector_latency_ms`（本轮用例均有采样）
- 建议：补齐 v69/v70/v71 分段耗时与 P95，以便定位瓶颈守卫

## 6. 下一步优化方案（建议顺序）
1. 先做 `probe/reject_probe` 的 v71 参数 AB，目标是把 `probe` 从 failed 拉到 success。
2. 抽离 `v63-v71` 守卫链到独立模块，降低单文件耦合并提高可测试性。
3. 统一 `check-env` 的硬件依赖判定策略（默认模式 vs dryrun 模式）。
4. 补充真实 XPS 连接条件下的配置读写成功路径回归（当前仅覆盖未连接错误路径）。
