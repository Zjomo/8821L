# SpotZoom v68 综合检测报告（2026-05-18）

## 1. 前沿同类项目调研（本轮）

1. SAHI（Slicing Aided Hyper Inference）
- 文档: https://obss.github.io/sahi/quick-start/
- 论文: https://arxiv.org/abs/2202.06934
- 可模仿模块:
  - 重叠切片（`slice_height/width` + `overlap_*_ratio`）增强小目标可检出性；
  - 切片结果 NMS/NMM 融合策略，减少局部重复框。

2. trackpy Adaptive Search
- 文档: https://soft-matter.github.io/trackpy/dev/tutorial/adaptive-search.html
- 可模仿模块:
  - `adaptive_step` 逐步缩小搜索范围；
  - `adaptive_stop` 作为失败边界，避免无穷扩大搜索代价。

3. Norfair Tracker
- 文档: https://tryolabs.github.io/norfair/dev/reference/tracker/
- 可模仿模块:
  - `hit_counter_max` / `initialization_delay` / `pointwise_hit_counter_max` 的命中惯性与稳态筛选。

4. CoTracker3（ICCV 2025）
- 论文页: https://openaccess.thecvf.com/content/ICCV2025/html/Karaev_CoTracker3_Simpler_and_Better_Point_Tracking_by_Pseudo-Labelling_Real_Videos_ICCV_2025_paper.html
- 仓库: https://github.com/facebookresearch/co-tracker
- 可模仿模块:
  - online/offline 双模式点追踪；
  - `pred_visibility` 作为遮挡/低可见时的轨迹可信度参考。

## 2. SpotZoom.py 本轮补充（v68）

目标文件: `E:\jupyter file\2_Optics\8821L\SpotZoom.py`

新增 `frontier v68 tiled-rescue guard`（接在 v67 之后）：
- 核心思路: SAHI 切片窗口搜索 + trackpy 对比度峰值选择 + CoTracker3 可见性松弛门控。
- 新增配置（默认配置 / `AlignmentConfig` / CLI / 参数校验 / open-source bundle / metrics 全链路接入）：
  - `frontier_v68_tiled_rescue_guard_enabled`
  - `frontier_v68_history_size`
  - `frontier_v68_base_gate_px`
  - `frontier_v68_tile_search_radius_px`
  - `frontier_v68_tile_size_px`
  - `frontier_v68_tile_overlap_ratio`
  - `frontier_v68_min_peak_contrast`
  - `frontier_v68_max_rescue_shift_px`
  - `frontier_v68_merge_iou_threshold`
  - `frontier_v68_visibility_relax_threshold`
  - `frontier_v68_visibility_relax_gain`
  - `frontier_v68_drift_penalty_gain`
  - `frontier_v68_confidence_penalty`
  - `frontier_v68_confidence_boost`
- 新增运行指标:
  - `frontier_v68_guard_checks`
  - `frontier_v68_guard_refines`
  - `frontier_v68_guard_rejects`
  - `frontier_v68_guard_soft_accepts`
  - `frontier_v68_guard_failures`
  - `frontier_v68_rescue_candidates`
- 主链路更新:
  - `v57 -> v58 -> v59 -> v63 -> v64 -> v65 -> v66 -> v67 -> v68`
- open-source bundle 联动:
  - `microscopy_tracking`
  - `resilient_association`
  - `frontier_2026_tracking_stack`

## 3. 自动化检测结果

### 3.1 构建 / 启动 / 语法

- `python -m py_compile 8821L/SpotZoom.py`: PASS
- `python -m py_compile XPS/xps_admin_console/server.py`: PASS
- `node --check XPS/xps_admin_console/static/app.js`: PASS
- `python SpotZoom.py --check-env ... --frontier-v68-tiled-rescue-guard --skip-roi`: PASS（`artifacts/spotzoom_check_env_v68.txt`）

### 3.2 SpotZoom 核心流程回放

1) `spotzoom_run_report_v68_smoke.json`
- 状态: success
- 指标: `detect_success=5/5`
- v68: `checks=5, refines=0, rejects=0, soft_accepts=5`

2) `spotzoom_run_report_v68_offcenter_noisy.json`
- 状态: success
- 指标: `detect_success=5/5`
- v68: `checks=5, refines=0, rejects=0, soft_accepts=5`

3) `spotzoom_run_report_v68_probe.json`（中高扰动）
- 状态: failed（未收敛）
- 指标: `detect_success=64/64`、`elapsed_s=73.456`
- v68: `checks=64, refines=4, rejects=27, soft_accepts=33, rescue_candidates=31`
- 说明: v68 refine/reject 分支都已真实触发，但门控偏紧导致收敛性下降。

4) `spotzoom_run_report_v68_reject_probe.json`（极限拒绝压测）
- 状态: failed（未收敛）
- 指标: `detect_success=64/64`、`elapsed_s=74.908`
- v68: `checks=60, refines=0, rejects=52, soft_accepts=8, rescue_candidates=52`
- 说明: 在高噪声极端场景，v68 拒绝分支主导，验证拒绝路径有效但参数鲁棒性不足。

5) 故障注入
- `spotzoom_run_report_v68_missing_image.json`: failed（符合预期，缺失图像）
- `spotzoom_run_report_v68_xps_fail.json`: failed（符合预期，XPS 连接失败）

### 3.3 页面访问 / 静态资源 / 接口请求 / 网络异常 / 前端控制台

- 文件: `8821L/artifacts/xps_http_check_v68.json`
- 服务日志: `8821L/artifacts/xps_server_v68_stdout.log`
- 状态分布: `200 x7`, `404 x2`, `400 x3`, `413 x1`
- Playwright runtime:
  - `console.error`: `Failed to load resource: net::ERR_CONNECTION_TIMED_OUT`
  - `requestFailed`: Google Fonts CSS 外网请求失败
  - `pageErrors`: 0

### 3.4 数据库读写

- 本轮在 `SpotZoom.py` 和 `XPS/xps_admin_console/server.py` 未检出 sqlite/mysql/postgres/redis/mongodb 等读写路径。

### 3.5 代码质量与模块结构

- `SpotZoom.py`: 21637 行，`except Exception` 132 处。
- `SpotZoomController.__init__`: 2225 行（高耦合）。
- `XPS/xps_admin_console/server.py`: 1016 行，`except Exception` 13 处。

## 4. 问题分级（优先级 / 影响模块 / 修复建议）

### P1

1) v68 在噪声场景下收敛风险上升
- 影响模块: `SpotZoom.py`（v68 守卫）
- 现象: probe / reject_probe 出现大量 `frontier_v68_guard_rejects`，整体未收敛。
- 建议: 增加“连续拒绝回退到 v67”策略，并将 `base_gate_px` 与 `min_peak_contrast` 绑定噪声估计自适应。

2) 主控耦合持续过高
- 影响模块: `SpotZoom.py`
- 现象: `SpotZoomController.__init__` 2225 行。
- 建议: 抽离 `v58~v68` 守卫链到独立 pipeline 模块，降低回归冲突面。

3) 前端外网字体依赖导致网络异常
- 影响模块: `XPS/xps_admin_console/static/index.html`
- 现象: Google Fonts 请求失败会在 console/network 产生错误。
- 建议: 改本地字体静态资源或无外网降级字体栈。

### P2

4) XPS API 错误码语义偏粗
- 影响模块: `XPS/xps_admin_console/server.py`
- 现象: timeout/未连接/参数错误主要返回 400。
- 建议: 细分 408/409/422/5xx，并统一 `error_code` 字段。

5) 文案编码链路存在乱码
- 影响模块: `/api/features` 返回内容
- 现象: 中文摘要字段出现乱码片段（`body_preview` 可见）。
- 建议: 统一 UTF-8 源文件编码并补 API 编码回归测试。

### P3

6) 广谱异常捕获较多
- 影响模块: `SpotZoom.py`, `server.py`
- 现象: typed exception 覆盖不足，静态可推断性弱。
- 建议: 按设备层/检测层/接口层分阶段收敛异常类型。

## 5. 下一步优化方案

1. 为 v68 增加“拒绝熔断回退”与噪声自适应阈值（先保收敛，再提精度）。
2. 将 `v58~v68` 拆分为可插拔后处理链模块，主控只保留调度。
3. 移除前端外网字体强依赖，固化离线环境 console/network 零错误基线。
4. 在 `server.py` 引入结构化错误码规范与状态码矩阵。
5. 对 `probe` 场景建立参数 AB 回归（至少 1 组“触发 refine 且收敛成功”基线）。
