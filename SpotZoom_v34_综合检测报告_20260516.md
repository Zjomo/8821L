# SpotZoom v34 综合检测与优化报告（2026-05-16）

## 1. 本轮目标与范围
- 目标：对标互联网前沿项目，补充 `SpotZoom.py` 能力，并完成构建/启动/流程/质量自动检查。
- 代码变更：
  - 新增 `SpotZoom_Machine_Learning/innovation_frontier_v33.py`
  - 更新 `SpotZoom.py`（v33 模块全链路接入：配置、CLI、校验、初始化、平滑链路、指标、事件、清理）

## 2. 前沿项目对标与可模仿模块

### 2.1 CoTracker（Meta）
- 参考：`https://github.com/facebookresearch/co-tracker`
- 可模仿点：任意点时序跟踪 + 可见性/可靠性输出（`pred_tracks`, `pred_visibility`）
- 本项目映射：
  - 已有 v32 轨迹一致性门控（LK 前后向一致性）
  - 建议下一步引入“多点可靠性聚合”，不只依赖单中心点

### 2.2 OpenCV TrackerCSRT + MOSSE/DCF 思路
- 参考：
  - `https://docs.opencv.org/3.4/d2/da2/classcv_1_1TrackerCSRT.html`
  - `https://www.cs.colostate.edu/~draper/papers/bolme_cvpr10.pdf`
- 可模仿点：局部相关性响应图、峰值可信度（PSR）用于漂移抑制
- 本项目映射：
  - 本轮新增 v33 响应可靠性门控（template matching + PSR）
  - 与 v29/v30/v31/v32 串联，形成“时序+频域+轮廓+响应”复合门控链

### 2.3 OpenFlexure 显微软件
- 参考：`https://openflexure-microscope-software.readthedocs.io/en/latest/webapp/pane_capture.html`
- 可模仿点：采集流程里将扫描/自动对焦作为可配置步骤
- 本项目映射：
  - 建议把现有 `detect-retry` 与 `recovery` 路径升级为“阶段化采集策略”（粗搜/细搜）

### 2.4 pymmcore-plus / pycro-manager 事件驱动采集
- 参考：
  - `https://pymmcore-plus.readthedocs.io/en/stable/examples/mda.html`
  - `https://pycro-manager.readthedocs.io/en/latest/acq_events.html`
- 可模仿点：事件信号 + 队列化采集（frameReady / sequenceStarted 等）
- 本项目映射：
  - 已有 `RunReporter` 事件流（jsonl），建议升级为“状态机事件总线 + 可回放”

### 2.5 laserbeamsize（ISO 11146）
- 参考：`https://github.com/scottprahl/laserbeamsize`
- 可模仿点：标准化光束形貌指标（中心、直径、能量）
- 本项目映射：
  - v31 已做 profile gate；建议下一步接入 ISO 风格 mask/采样策略

## 3. 本轮代码补充（v33）

### 3.1 新增模块
- 文件：`SpotZoom_Machine_Learning/innovation_frontier_v33.py`
- 核心能力：
  - 局部模板匹配（`cv2.matchTemplate`）
  - 响应峰值 + PSR 可信度门控
  - 低置信度时提高响应阈值
  - 置信度感知的中心融合与模板动量更新

### 3.2 主流程接入点（SpotZoom.py）
- 新增 `frontier_v33_*` 配置默认值/`AlignmentConfig` 字段
- 新增 CLI 参数组：`--frontier-v33-response-gate` 及阈值参数
- 新增 `check_environment` 可用性与参数范围校验
- 新增运行指标：`frontier_v33_response_calls/refines/rejects/failures`
- 新增事件：
  - `frontier_v33_response_accepted`
  - `frontier_v33_response_rejected`
  - `frontier_v33_response_failed`
- 新增 `_apply_frontier_v33_response()` 并串入 `_smooth_detection()`

## 4. 自动检查结果

### 4.1 构建与启动
- `python -m py_compile SpotZoom.py SpotZoom_Machine_Learning/innovation_frontier_v33.py`：通过
- `python SpotZoom.py --check-env`：失败（默认硬件模式缺 `pylablib`，符合预期）
- `python SpotZoom.py --check-env --xy-driver dryrun --z-driver dryrun --detector-backend classic --frontier-v29-reliability --frontier-v30-phase-lock --frontier-v31-profile-gate --frontier-v32-coherence --frontier-v33-response-gate`：通过

### 4.2 核心功能流程（模拟场景）
- Center：`spotzoom_run_report_v34_center_v33_tuned.json`，成功
- Offcenter：`spotzoom_run_report_v34_offcenter_v33_tuned.json`，成功
- Black：`spotzoom_run_report_v34_black_v33_tuned.json`，失败（初始检测缺失，预期）
- Offcenter+Jitter：`spotzoom_run_report_v34_offcenter_jitter_v33_tuned2.json`，成功

关键指标（jitter 场景）：
- detect_success/attempts = `6/6`
- v29 refines = `4`
- v30 refines = `5`
- v31 refines = `1`
- v32 refines = `2`
- v33 refines = `5`
- v33 rejects/failures = `0/0`

### 4.3 页面访问 / 前端控制台 / 静态资源 / API / 数据库
- 未检测到可运行 Web 前端入口（项目以 Python 控制脚本 + 报告生成脚本为主）。
- 未检测到业务 API 调用链（HTTP 客户端/服务端主流程缺失）。
- 未检测到数据库读写主链路（sqlite/mysql/postgres/redis 未进入主流程）。
- 因无前端运行对象，前端 console/network/static 维度本轮标记为 N/A。

### 4.4 代码质量与结构
- `flake8`：共 `1449` 条（以 `E501` 长行为主）
- `pylint -E`：以 OpenCV 动态属性与可选依赖误报为主，含部分真实风险（可选模块初始化路径复杂）
- 结构扫描：
  - `SpotZoom.py` 约 `11590` 行
  - 函数数 `205`
  - `AlignmentConfig` 字段数 `373`
  - 超长函数：`parse_args` 1494 行、`__init__` 1205 行、`check_environment` 554 行

## 5. 问题优先级（本轮）

### P1
1. 默认启动依赖硬件库（`pylablib`）导致非硬件环境 `--check-env` 失败
- 影响模块：环境校验 / 启动链路
- 影响：CI/本地无硬件环境无法“一键绿灯”
- 建议：拆分 `check-env` 为 `core` 与 `hardware` 两级；默认先跑 core

2. `SpotZoom.py` 单体过大，配置与启动逻辑耦合高
- 影响模块：`parse_args`、`check_environment`、`SpotZoomController.__init__`
- 影响：回归风险与改动扩散大
- 建议：按“参数定义/参数校验/控制器装配/运行策略”四层解耦

3. 缺少自动化测试基线
- 影响模块：整体
- 影响：新模块接入仅靠烟测，难覆盖边界回归
- 建议：先建立 `dryrun` 下 4 场景最小回归集（center/offcenter/black/jitter）

### P2
1. 风格债务高（flake8 1449）
- 影响模块：全局
- 建议：先按“仅新增/变更区域”执行 lint gate，再逐步推广

2. 可选依赖加载路径复杂（pylint 错误噪声高）
- 影响模块：动态导入与初始化
- 建议：对 `cv2`、可选 ML 模块加 typing/stub 和集中封装，降低误报

## 6. 下一步优化方案
1. 建立 `tests/test_spotzoom_dryrun_smoke.py`，固化 4 场景回归门禁。
2. 抽离 `check_environment` 为模块化校验器（core/hardware/model 三段）。
3. 抽离 `frontier` 插件注册表，替换当前 `SpotZoom.py` 内部硬编码串接。
4. 引入事件总线回放器（从 jsonl 回放一次 run），用于异常复盘与性能对比。
5. 分阶段降低 flake8 噪声：先 `E9/F63/F7/F82`，再逐步收敛 E/W 风格项。

## 7. 产物清单
- 代码：
  - `SpotZoom.py`
  - `SpotZoom_Machine_Learning/innovation_frontier_v33.py`
- 运行报告：
  - `spotzoom_run_report_v34_center_v33_tuned.json`
  - `spotzoom_run_report_v34_offcenter_v33_tuned.json`
  - `spotzoom_run_report_v34_black_v33_tuned.json`
  - `spotzoom_run_report_v34_offcenter_jitter_v33_tuned2.json`
- 事件流：
  - `spotzoom_events_v34_center_v33_tuned.jsonl`
  - `spotzoom_events_v34_offcenter_v33_tuned.jsonl`
  - `spotzoom_events_v34_black_v33_tuned.jsonl`
  - `spotzoom_events_v34_offcenter_jitter_v33_tuned2.jsonl`
