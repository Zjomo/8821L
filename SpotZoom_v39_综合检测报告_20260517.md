# SpotZoom v39 综合检测报告（2026-05-17）

## 1. 本轮目标与产出
- 时间: 2026-05-17 00:27:54 +08:00
- 自动化目标: 对标前沿项目、补充 SpotZoom.py、执行构建/启动/页面/API/核心流程/质量巡检。
- 主要代码改动:
  - 新增 SpotZoom_Machine_Learning/innovation_frontier_v39.py（外观+运动融合关联门控）。
  - 在 SpotZoom.py 全链路接入 v39（导入、配置默认值、CLI、参数校验、运行时初始化、平滑链路、指标上报、生命周期 reset）。

## 2. 互联网前沿对标（可模仿模块）
- Deep SORT（外观特征 + 运动模型关联）:
  - 可模仿模块: ppearance_embedding + motion_gate 双约束。
- ByteTrack（高/低置信度分层关联）:
  - 可模仿模块: 低置信桥接恢复、分层阈值策略。
- BoT-SORT（鲁棒关联与轨迹稳定性）:
  - 可模仿模块: 抖动/漂移下的关联稳健化。
- Ultralytics Tracker 集成实践（BoT-SORT / ByteTrack）:
  - 可模仿模块: 统一开关式 tracker pipeline。
- SAM2:
  - 可模仿模块: 目标区域掩膜细化（可与 spot 定位结果互补）。
- HCIPy / slmsuite:
  - 可模仿模块: 光学仿真与波前/SLM 闭环能力，补强物理一致性约束。

## 3. 自动检查执行结果

### 3.1 构建与启动
- python -m py_compile SpotZoom.py innovation_frontier_v39.py 通过。
- un_spotzoom.bat --check-env --xy-driver dryrun --z-driver dryrun ... --frontier-v39-assoc-gate 通过。
- python SpotZoom.py --check-env（默认硬件路径）失败：缺失 pylablib。

### 3.2 核心流程（SpotZoom）
- 场景A（center smoke，v39 开启）成功：
  - rtifacts/spotzoom_run_report_v39_center_smoke.json
  - 关键指标: rontier_v39_assoc_calls=5, efines=0, ejects=0, ailures=0。
- 场景B（center+jitter，v38 基线）成功：
  - rtifacts/spotzoom_run_report_v39_center_jitter_v38.json
  - 关键指标: 38_calls=5, 38_refines=3, 38_rejects=1。
- 场景C（center+jitter，v38+v39）成功：
  - rtifacts/spotzoom_run_report_v39_center_jitter_v38v39.json
  - 关键指标: 38_calls=5, 38_refines=4, 38_rejects=0; 39_calls=5, 39_refines=4, 39_rejects=0, 39_failures=0。

### 3.3 页面访问 / 接口请求 / 静态资源 / 网络异常（XPS Admin Console）
- 编译检查: python -m py_compile XPS/xps_admin_console/server.py 通过。
- HTTP 探测报告: XPS/xps_admin_console/artifacts/xps_http_check_v39.json
  - GET /, /styles.css, /app.js, /api/features, /api/session, /api/system/stages-db => 200。
  - GET /api/unknown => 404（符合预期）。
  - POST /api/connect（错误凭据）=> 400。
  - POST /api/controller/raw（未连接）=> 400。
  - POST /api/files/write（非法请求）=> 400。

### 3.4 数据库读写
- 未发现数据库栈（sqlite3/sqlalchemy/mysql/redis）接入痕迹。
- /api/system/stages-db 为配置读取语义，不是 DB CRUD。

### 3.5 前端控制台日志
- 本轮未接入浏览器自动化抓取 console runtime log（仅完成页面/API/静态资源 HTTP 级检查）。

### 3.6 代码质量与结构
- pylint -E innovation_frontier_v39.py：无错误。
- pylint -E SpotZoom.py：仍有大量历史级错误（OpenCV 静态成员识别、可选依赖导入、超大单体导致的类型推断噪声）。
- lake8 innovation_frontier_v39.py：存在多处 E501（行长）风格问题。
- 结构统计（SpotZoom.py）:
  - 总行数约 14118。
  - SpotZoomController 约 7128 行。
  - parse_args 约 1854 行。
  - _apply_frontier_v38_assoc 与 _apply_frontier_v39_assoc 代码相似度约 0.866（重复逻辑风险）。

## 4. 问题优先级清单

### P1
- 问题: 默认环境检查失败（缺少 pylablib）。
- 影响模块: SpotZoom 启动与硬件控制链路。
- 证据: python SpotZoom.py --check-env 报错 Missing dependency for motor control: pylablib。
- 建议: 固化依赖安装脚本或提供 equirements-hw.txt + 安装前置检查。

### P1
- 问题: SpotZoom.py 高耦合单体，回归面大。
- 影响模块: CLI、检测链路、恢复链路、前沿模块接入。
- 证据: Controller/parse_args 体量过大。
- 建议: 按 detector/association/recovery/io/config 分层拆分；将 v34-v39 通用事件上报模板化。

### P2
- 问题: v38/v39 关联 apply 流程重复度高。
- 影响模块: 关联门控族维护成本、缺陷扩散风险。
- 建议: 抽取统一 pply_assoc_gate() 模板（中心校验、shift gate、事件打点、metric 计数）。

### P3
- 问题: 新增模块存在行长风格告警（E501）。
- 影响模块: 可读性与规范一致性。
- 建议: 引入统一格式化策略（black/ruff）或仓库级 lint 基线配置。

### P3
- 问题: XPS README 中文出现编码乱码。
- 影响模块: 文档可读性。
- 建议: 全部统一为 UTF-8 并修复历史乱码文本。

## 5. 下一步优化方案
1. 抽象通用关联门控执行器（v34-v39）以消除重复逻辑，并保留模块差异策略。
2. 将前沿模块配置集中到独立 schema（自动生成 CLI + 校验），降低 parse_args 膨胀。
3. 增加最小回归集：center/offcenter/jitter/black 四场景固定随机种子，输出 A/B 对比统计。
4. 为 XPS 控制台补浏览器级自动化（console/error/network waterfall），覆盖前端运行时问题。
5. 提供硬件依赖分层安装文档（runtime-only 与 hw-full 两套环境）。
