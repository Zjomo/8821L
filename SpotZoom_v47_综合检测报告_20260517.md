# SpotZoom v47 综合检测与优化报告（2026-05-17）

## 1) 本轮代码补充（已落地）
- 新增 `SpotZoom_Machine_Learning/innovation_frontier_v47.py`：`AdaptiveMemoryBudgetAssociationGate`。
  - 核心能力：记忆预算裁剪、拒绝率自适应门控、低置信路径的桥接判定、轻量流式状态更新。
  - 依赖保持为 `numpy`，不引入额外运行时重依赖。
- 在 `SpotZoom.py` 完成 v47 全链路接入：
  - 动态导入与类型绑定（`innovation_frontier_v47`）
  - 默认配置、`AlignmentConfig`、CLI 参数（`--frontier-v47-*`）
  - 环境检查可用性告警、参数校验
  - 控制器初始化、平滑链路调用、reset 回收
  - 运行指标上报（calls/refines/rejects/bridge_hits/memory_path_hits/failures）

## 2) 互联网前沿同类项目与可模仿模块（本轮调研）

### 2.1 点跟踪与在线长序列处理
- CoTracker3（Meta）
  - README 更新记录显示：`2025-01-21` 有 Kubric 数据集更新，`2024-10-15` 发布 CoTracker3。
  - 支持 offline/online 两类 API，online 模式强调长视频的内存友好处理。
  - 可模仿模块：滑窗在线状态、长序列低内存调度、online/offline 双模式接口。

### 2.2 流式记忆与视频状态管理
- SAM 2（Meta）
  - README 明确提到：视频模型采用 `streaming memory` 做实时处理。
  - `2024-12-11` 更新中强调 `SAM2VideoPredictor` 的多目标/按对象独立推理能力与速度优化。
  - 可模仿模块：对象级状态机、流式记忆更新、可切换加速路径。

### 2.3 记忆更新策略（RL 方向）
- SAM2RL（arXiv:2507.08548，提交日期 `2025-07-11`）
  - 提出将记忆控制视为序列决策问题，用强化学习替代手工规则更新。
  - 可模仿模块：记忆保留策略学习化、困难场景下的自适应更新增益。

### 2.4 光学/AO 工程化框架
- HCIPy（GitHub 显示最新 release：`v0.7.0`，`2025-08-25`）
  - 提供高对比成像端到端仿真，并将 AO 与 coronagraphy 反馈链路放在同一框架。
  - 可模仿模块：物理约束反馈环、统一波前/像质指标基线。
- AOtools（GitHub 显示最新 release：`1.0.7`，`2023-01-27`）
  - AO 建模与分析工具集，适合做稳态统计与分析基线。
  - 可模仿模块：标准化 AO 分析算子、指标一致性校验。
- OpenWFS（文档）
  - 明确支持异步测量、自动同步、硬件无关算法抽象。
  - 可模仿模块：检测器/执行器/算法解耦、异步采样与等待机制。
- slmsuite（GitHub Releases）
  - `v0.4.1` 在 `2026-03-30` 发布；`v0.4.0` 说明含线程管理、测试基建与依赖分组工程化改造。
  - 可模仿模块：硬件线程模型、CI 测试分层、依赖分组部署。

## 3) 自动检测结果

### 3.1 构建与启动
- `python -m py_compile SpotZoom.py SpotZoom_Machine_Learning/innovation_frontier_v47.py ..\XPS\xps_admin_console\server.py`：通过。
- `python SpotZoom.py --check-env --frontier-v47-assoc-gate`：失败（预期环境缺口）。
  - 错误：缺少 `pylablib`（电机控制链路依赖）。

### 3.2 SpotZoom 核心流程（本轮主产物）
- 基线（启用 v42-v46，不启用 v47）
  - 报告：`artifacts/spotzoom_run_report_v47_baseline.json`
  - `status=success`，`elapsed_s=0.837`，`detect_success=5/5`
  - v47 指标：`calls=0`
- 启用 v47
  - 报告：`artifacts/spotzoom_run_report_v47_enabled.json`
  - `status=success`，`elapsed_s=0.827`，`detect_success=5/5`
  - v47 指标：`calls=5`, `refines=0`, `rejects=0`, `bridge_hits=0`, `memory_path_hits=0`, `failures=0`
- 抖动+噪声（jitter=11, noise=20）启用 v47
  - 报告：`artifacts/spotzoom_run_report_v47_noisy.json`
  - `status=success`，`elapsed_s=1.993`，`detect_success=8/8`
  - v46：`calls=8`, `refines=5`, `rejects=2`
  - v47：`calls=8`, `refines=3`, `rejects=3`, `bridge_hits=0`, `memory_path_hits=0`
- 缺失输入图像异常路径
  - 报告：`artifacts/spotzoom_run_report_v47_missing_asset.json`
  - `status=failed`
  - 错误：`Simulation image not found: artifacts\not_exists_v47.png`

### 3.3 A/B 性能抽样（3 次）
- 汇总：`artifacts/ab_v47_summary.json`
- clean：baseline `avg=0.804333s`，enabled `avg=0.824333s`，`delta=+0.020000s`
- noisy：baseline `avg=2.834333s`，enabled `avg=6.828667s`，`delta=+3.994334s`
- 结论：v47 在 clean 场景开销可控，但 noisy 场景存在显著波动与性能回退（出现 13.458s 长尾）。

### 3.4 页面访问 / API / 静态资源 / 网络异常（XPS 管理台）
- 报告：`artifacts/xps_http_check_v47.json`
- 启动：`..\XPS\xps_admin_console\server.py --host 127.0.0.1 --port 8934`
- 核心结果：
  - `GET /` -> 200
  - `GET /styles.css` -> 200
  - `GET /app.js` -> 200
  - `GET /api/features` -> 200
  - `GET /api/session` -> 200
  - `GET /api/system/stages-db` -> 200（未连接返回 error payload）
  - `GET /static/styles.css` -> 404
  - `POST /api/connect`（非法 JSON）-> 400
  - `POST /api/files/write`（未连接）-> 400
  - `POST /api/controller/raw`（超大 payload）-> 413
  - `GET /api/files/read?path=../server.py` -> 400
  - `network_timeout_unused_port` -> timeout（异常路径有效）

### 3.5 数据库读写检查
- 扫描 `SpotZoom.py`、`xps_admin_console/server.py`、`xps_admin_console/static/app.js`：
  - 未发现 sqlite/sqlalchemy/mysql/postgres/redis/mongodb 读写调用。
- 当前“数据库读写”风险判定：N/A（当前主要为 FTP/设备会话状态与文件接口）。

### 3.6 前端控制台日志
- `node --check ..\XPS\xps_admin_console\static\app.js`：通过（语法层）。
- 浏览器 runtime console 自动采集：未完成（环境缺少 `playwright` / `selenium`）。

### 3.7 代码质量与模块结构
- 报告：`artifacts/code_quality_v47.json`
- 关键统计：
  - `SpotZoom.py`：16378 LOC，22 class，219 function，平均函数长度 65.082 行
  - `innovation_frontier_v47.py`：652 LOC，4 class，21 function
  - `server.py`：1011 LOC，4 class，39 function
- 高风险热点：
  - `parse_args`：2292 行
  - `SpotZoomController.__init__`：1740 行
  - `check_environment`：840 行
- 重复窗口（5 行归一化）≥3 次：`231`（仍存在明显参数样板重复）。

## 4) 问题优先级（问题 / 影响模块 / 修复建议）

### P1
- 问题：v47 noisy 场景 A/B 明显回退，且长尾波动大（`+3.99s` 平均延迟增量）。
- 影响模块：`innovation_frontier_v47.py` 关联门控路径、实时性 SLA。
- 建议：
  - 引入固定随机种子（仿真路径）并扩展样本量（>=20）再判定回归；
  - 下调 `reject_ratio_gate_scale` 并限制 gate 扩张上界；
  - 对 memory-path/bridge-path 增加触发覆盖用例，避免“逻辑存在但不触发”。

### P1
- 问题：`--check-env` 仍因 `pylablib` 缺失失败。
- 影响模块：硬件链路验收、部署一致性。
- 建议：补充 `requirements-hw.txt` 并在 `check-env` 输出分层依赖提示（必需/可选）。

### P1
- 问题：前端 runtime console/network failed request 未自动采集（缺 `playwright`/`selenium`）。
- 影响模块：页面运行时回归覆盖率。
- 建议：补浏览器自动化依赖并把 `console.error`/`requestfailed` 结构化落盘。

### P2
- 问题：`/static/styles.css` 持续 404。
- 影响模块：静态资源兼容路径、历史页面引用。
- 建议：加 `/static/*` 映射或 301 到现有静态目录。

### P2
- 问题：`SpotZoom.py` 单体持续膨胀（16378 LOC），耦合高。
- 影响模块：参数系统、初始化链路、frontier 回归面。
- 建议：拆分 `frontier_registry.py`、`args_registry.py`、`env_validator.py`，引入统一 gate 适配层。

### P3
- 问题：未接入 lint/type-check CI 门禁。
- 影响模块：代码健康可持续性。
- 建议：补 `ruff/flake8 + mypy/pyright` 基线，并先对 `innovation_frontier_v47.py` 启用强规则。

## 5) 下一步优化方案
1. 对 v47 增加 deterministic 模式（固定随机源）和分段耗时上报（embedding/gating/memory-prune/update）。
2. 引入 `frontier-v47-profile`（clean/noisy）参数组，按场景切换阈值，优先压 noisy 长尾。
3. 增加 v47 覆盖用例：强遮挡、突变位移、低纹理、持续高噪声，验证 bridge/memory-path 实际命中率。
4. 实施 XPS `/static/*` 兼容修复并补 API/静态资源回归测试。
5. 推进 SpotZoom 模块化拆分，先从 `parse_args` 与 `check_environment` 下手，降低回归面。

## 6) 本轮关键产物
- `SpotZoom_Machine_Learning/innovation_frontier_v47.py`
- `SpotZoom.py`
- `artifacts/help_snapshot_v47.txt`
- `artifacts/spotzoom_run_report_v47_baseline.json`
- `artifacts/spotzoom_run_report_v47_enabled.json`
- `artifacts/spotzoom_run_report_v47_noisy.json`
- `artifacts/spotzoom_run_report_v47_missing_asset.json`
- `artifacts/ab_v47_summary.json`
- `artifacts/xps_http_check_v47.json`
- `artifacts/code_quality_v47.json`
- `SpotZoom_v47_综合检测报告_20260517.md`
