# SpotZoom v46 综合检测与优化报告（2026-05-17）

## 1) 本轮代码补充（已落地）
- 新增 `SpotZoom_Machine_Learning/innovation_frontier_v46.py`：`MemoryUncertaintyAssociationGate`（流式记忆 + 不确定性门控 + 低置信桥接）。
- 在 `SpotZoom.py` 完成 v46 全链路接入：
  - 动态导入与类型绑定（`innovation_frontier_v46`）
  - 默认配置、`AlignmentConfig`、CLI 参数（`--frontier-v46-*`）
  - 环境检查可用性告警、参数校验
  - 控制器初始化、平滑链路调用、reset 回收
  - 运行指标上报（calls/refines/rejects/bridge_hits/reacquires/failures）
- 追加修正：收敛 `memory_reacquire` 触发条件，避免 clean 场景过度重采样。

## 2) 互联网前沿同类项目与可模仿模块（本轮调研）

### 2.1 视频点跟踪 / 关联
- CoTracker3（Meta）
  - 可模仿模块：online chunk 状态更新、长序列内存友好跟踪、offline/online 双模式接口。
  - 参考：<https://github.com/facebookresearch/co-tracker>，<https://arxiv.org/abs/2410.11831>
- ByteTrack
  - 可模仿模块：低置信观测参与关联（而非直接抛弃）、遮挡恢复的二阶段关联。
  - 参考：<https://github.com/FoundationVision/ByteTrack>，<https://arxiv.org/abs/2110.06864>
- Norfair
  - 可模仿模块：轻量距离函数插件化、运动相机补偿、ReID 插件化结构。
  - 参考：<https://github.com/tryolabs/norfair>

### 2.2 视频分割 / 流式记忆
- SAM 2（Meta）
  - 可模仿模块：streaming memory、`VideoPredictor` 状态机、多目标增量跟踪。
  - 参考：<https://github.com/facebookresearch/sam2>

### 2.3 光学 / 波前 / AO
- HCIPy
  - 可模仿模块：波前与传播的物理约束项、AO + 传感反馈闭环。
  - 参考：<https://github.com/ehpor/hcipy>
- AOtools
  - 可模仿模块：AO 建模与分析算子库（鲁棒指标与仿真基线）。
  - 参考：<https://github.com/AOtools/aotools>
- OpenWFS
  - 可模仿模块：检测器/执行器/算法统一抽象、自动同步、噪声/漂移仿真注入。
  - 参考：<https://github.com/IvoVellekoop/openwfs>，<https://openwfs.readthedocs.io/en/latest/core.html>
- slmsuite（最新 release：v0.4.1，2026-03-30）
  - 可模仿模块：SLM 控制与标定链路、硬件线程/触发时序工程化、测试分层。
  - 参考：<https://github.com/holodyne/slmsuite>，<https://github.com/holodyne/slmsuite/releases>

## 3) 自动检测结果

### 3.1 构建与启动
- `python -m py_compile SpotZoom.py SpotZoom_Machine_Learning/innovation_frontier_v46.py ..\XPS\xps_admin_console\server.py`：通过。
- `python SpotZoom.py --check-env --frontier-v46-assoc-gate`：失败。
  - 错误：缺少 `pylablib`（电机控制依赖）。

### 3.2 SpotZoom 核心流程（本轮主产物）
- 基线（v42+v43+v44+v45，不启用 v46）
  - `artifacts/spotzoom_run_report_v46_baseline.json`
  - `status=success`，`elapsed_s=0.792`，`detect_success=5/5`
- 启用 v46
  - `artifacts/spotzoom_run_report_v46_enabled.json`
  - `status=success`，`elapsed_s=0.801`，`detect_success=5/5`
  - v46 指标：`calls=5`, `refines=0`, `rejects=0`, `reacquires=0`, `failures=0`
- 抖动+噪声（jitter=11, noise=20）启用 v46
  - `artifacts/spotzoom_run_report_v46_noisy.json`
  - `status=success`，`elapsed_s=3.458`，`detect_success=12/12`
  - v46 指标：`calls=12`, `refines=3`, `rejects=8`, `reacquires=2`, `failures=0`
- 缺失输入图像异常路径
  - `artifacts/spotzoom_run_report_v46_missing_asset.json`
  - `status=failed`
  - 错误：`Simulation image not found: artifacts\not_exists_v46.png`

### 3.3 A/B 性能抽样（3 次，随机扰动未固定种子）
- 汇总：`artifacts/ab_v46_summary.json`
- clean：baseline `avg=0.783667s`，enabled `avg=0.800667s`，`delta=+0.017s`
- noisy：baseline `avg=3.532333s`，enabled `avg=3.716333s`，`delta=+0.184s`
- 结论：v46 当前版本在本轮采样中总体偏慢（轻度）。

### 3.4 页面访问 / API / 静态资源 / 网络异常（XPS 管理台）
- 报告：`artifacts/xps_http_check_v46.json`
- 启动：`..\XPS\xps_admin_console\server.py --host 127.0.0.1 --port 8934`
- 核心结果：
  - `GET /` -> 200
  - `GET /styles.css` -> 200
  - `GET /app.js` -> 200
  - `GET /api/features` -> 200
  - `GET /api/session` -> 200
  - `GET /api/system/stages-db` -> 200（未连接时返回 error payload）
  - `GET /static/styles.css` -> 404
  - `POST /api/connect`（非法 JSON）-> 400
  - `POST /api/files/write`（未连接）-> 400
  - `POST /api/controller/raw`（超大 payload）-> 413
  - `GET /api/files/read?path=../server.py` -> 400
  - `GET unused-port` -> timeout（网络异常路径有效）

### 3.5 数据库读写检查
- 扫描目标：`SpotZoom.py`、`xps_admin_console/server.py`、`xps_admin_console/static/app.js` 及 `8821L` 下 py/js。
- 未发现 sqlite/sqlalchemy/mysql/postgres/redis/mongodb 读写调用。
- 当前“数据库读写”风险判定：N/A（主要是 FTP 文件读写与会话连接状态）。

### 3.6 前端控制台日志
- `node --check ..\XPS\xps_admin_console\static\app.js`：通过（语法层）。
- 浏览器 runtime console 自动采集：未完成（环境缺少 `playwright` / `selenium`）。

### 3.7 代码质量与模块结构
- 报告：`artifacts/code_quality_v46.json`
- 关键统计：
  - `SpotZoom.py`：16124 LOC，22 class，218 function，平均函数长度 64.326 行
  - `innovation_frontier_v46.py`：694 LOC，4 class，19 function
- 高风险热点：
  - `parse_args`：2250 行
  - `SpotZoomController.__init__`：1707 行（branch_nodes=285）
  - `check_environment`：826 行（branch_nodes=555）
- 重复窗口（5 行归一化）≥3 次：`441` 处（重复逻辑和参数样板显著）。

## 4) 问题优先级（问题 / 影响模块 / 修复建议）

### P1
- 问题：`SpotZoom.py` 单体过大且职责集中，`parse_args`/`__init__`/`check_environment` 超长。
- 影响模块：参数系统、初始化链路、frontier v34-v46 全部回归面。
- 建议：拆分 `frontier_registry.py`、`args_registry.py`、`env_validator.py`，将 v34-v46 gate 接口统一成策略注册表。

### P1
- 问题：`--check-env` 失败（缺少 `pylablib`），影响真实硬件启动验收。
- 影响模块：电机控制链路、部署可用性。
- 建议：补 `requirements-hw.txt` 或在 check-env 中区分“硬依赖/可选依赖”等级并输出安装指引。

### P1
- 问题：前端 runtime console 自动采集缺失（`playwright` / `selenium` 不可用）。
- 影响模块：页面运行时异常与 failed request 覆盖率。
- 建议：补浏览器自动化依赖并将 `console.error`/`requestfailed` 落盘 `artifacts/`。

### P2
- 问题：`/static/styles.css` 仍为 404。
- 影响模块：静态资源兼容路径、历史引用兼容。
- 建议：为 `/static/*` 增加映射或重定向到根静态资源目录。

### P2
- 问题：v46 噪声场景拒绝偏多（`rejects=8/12`）且 A/B 延迟略增。
- 影响模块：高噪声稳定性与时延。
- 建议：调低 `memory_min_similarity` 或加入 reject-streak 自适应阈值，按 `noise level` 做参数分层。

### P2
- 问题：A/B 结果方差高（随机扰动未固定种子）。
- 影响模块：性能回归判断可靠性。
- 建议：固定扰动种子并输出 P95/拒绝率/偏移分布。

### P3
- 问题：未执行 flake8/pylint 等 lint（环境缺失）。
- 影响模块：静态质量门禁。
- 建议：补 dev 依赖与 CI 静态门禁阈值。

## 5) 下一步优化方案
1. 将 v46 变为“按噪声等级动态参数组”（clean/noisy 两套阈值），并增加 CLI profile 切换。
2. 增加 `frontier_v46` 分段耗时上报（embedding/memory_match/gating/update），定位热点函数。
3. 把 v42-v46 统一到一个 `AssociationGateAdapter`，减少复制粘贴逻辑与回归风险。
4. 增加浏览器级自动化巡检（console/network/资源加载）并产出结构化报告。
5. 对 XPS `/static/*` 做兼容路径修复并补回归测试。

## 6) 本轮关键产物
- `SpotZoom_Machine_Learning/innovation_frontier_v46.py`
- `SpotZoom.py`
- `artifacts/help_snapshot_v46.txt`
- `artifacts/spotzoom_run_report_v46_baseline.json`
- `artifacts/spotzoom_run_report_v46_enabled.json`
- `artifacts/spotzoom_run_report_v46_noisy.json`
- `artifacts/spotzoom_run_report_v46_missing_asset.json`
- `artifacts/ab_v46_summary.json`
- `artifacts/xps_http_check_v46.json`
- `artifacts/code_quality_v46.json`
- `SpotZoom_v46_综合检测报告_20260517.md`
