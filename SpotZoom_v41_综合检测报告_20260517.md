# SpotZoom v41 综合检测报告（2026-05-17）

## 1) 本轮改动
- 新增 `SpotZoom_Machine_Learning/innovation_frontier_v41.py`：Temporal Continuity Association Gate（时序连续门控）。
- 在 `SpotZoom.py` 接入 v41：
  - 动态导入与类型绑定
  - 默认配置、`AlignmentConfig` 字段、CLI 参数、参数校验
  - 运行时初始化、平滑链路调用、reset 生命周期
  - 运行指标上报（calls/refines/rejects/bridge_hits/failures）

## 2) 前沿项目对标与可模仿模块
- CoTracker（Meta）：在线流式轨迹连续建模，可模仿“时序状态记忆 + 连续性评分”。
- ByteTrack：低分框仍参与关联，可模仿“低置信桥接（bridge association）”。
- Norfair：distance gate + hit counter，可模仿“接受/拒绝滞回机制”。
- OpenCV motion：`phaseCorrelate` 对位移估计和窗函数处理，可模仿“运动一致性校验”。
- laserbeamsize：ISO 11146 光束尺寸评估，可模仿“形态质量门控指标”。

## 3) 自动检测结果

### 3.1 构建/启动
- `python -m py_compile SpotZoom.py SpotZoom_Machine_Learning/innovation_frontier_v41.py`：通过。
- `run_spotzoom.bat --check-env ... --frontier-v41-assoc-gate`：通过。

### 3.2 SpotZoom 核心流程（模拟图 + dryrun）
- 命令：启用 v31~v41 门控链进行 1 轮收敛测试。
- 结果：`status=success`，`elapsed_s=1.217`，`detect_success=5/5`。
- v41 指标：`calls=5`, `refines=0`, `rejects=0`, `bridge_hits=0`, `failures=0`。

### 3.3 异常路径
- 缺失图像输入（`--frame-source-image artifacts/not_exist.png`）：
  - 结果 `status=failed`
  - 错误被正确上报：`Simulation image not found: artifacts\\not_exist.png`

### 3.4 页面/API/静态资源/网络异常（XPS 管理台）
- 本地启动 `XPS/xps_admin_console/server.py` 后检查：
  - `GET /` 200
  - `GET /api/features` 200
  - `GET /api/session` 200
  - `GET /api/system/stages-db` 200（返回“未连接”语义错误，接口可用）
  - `GET /styles.css` 200
  - `GET /app.js` 200
  - `GET /static/styles.css` 404
  - `POST /api/connect` 非法端口 -> 400
  - `POST /api/files/write` 未连接 -> 400
  - 网络异常探测（未监听端口）-> 超时
- 前端脚本语法检查：`node --check static/app.js` 通过。

### 3.5 质量与结构
- `flake8 SpotZoom.py innovation_frontier_v41.py --count --statistics`：2051 项（历史 E501 长行为主，非本轮引入）。
- `pylint -E SpotZoom.py ...`：主要为 `cv2` no-member 与可选依赖缺失告警；`innovation_frontier_v41.py` 单独 `pylint -E` 通过。
- 结构规模：
  - `SpotZoom.py` 14753 行，`def` 213，`class` 22
  - `parse_args` 1992 行
  - `SpotZoomController.__init__` 1523 行

## 4) 问题分级（优先级 / 影响模块 / 建议）

### P1
- 问题：`SpotZoom.py` 单体过大，`parse_args` / `__init__` 过长，维护耦合高。
- 影响模块：全局参数系统、初始化流程、回归稳定性。
- 建议：拆分为 `args_*`、`frontier_registry`、`controller_bootstrap` 子模块；先从 v34-v41 门控链提取统一 adapter。

### P1
- 问题：全链路启用 v31~v41 后，模拟流程耗时由历史 0.778s 升至 1.217s（+56%，同类规模工况）。
- 影响模块：实时检测主循环。
- 建议：增加 `--frontier-profile`（阶段耗时计时）并按置信度短路后段门控。

### P2
- 问题：静态检查历史债务高（长行/格式类问题占主导）。
- 影响模块：可读性与CI门禁落地。
- 建议：采用“新增改动先达标”策略，对新增文件开启更严格 lint；主文件分批治理。

### P2
- 问题：XPS API 对未连接场景统一返回 400，语义可进一步细化（例如 409/503）。
- 影响模块：`XPS/xps_admin_console/server.py` API 语义一致性。
- 建议：按错误类型细分状态码并补充前端提示映射。

## 5) 产物
- `8821L/SpotZoom_Machine_Learning/innovation_frontier_v41.py`
- `8821L/SpotZoom.py`
- `8821L/artifacts/spotzoom_run_report_v41_20260517.json`
- `8821L/artifacts/spotzoom_events_v41_20260517.jsonl`
- `8821L/artifacts/spotzoom_run_report_v41_missing_asset.json`
- `8821L/artifacts/xps_http_check_v41_current.json`
- `8821L/artifacts/xps_server_v41_stdout.log`
- `8821L/artifacts/xps_server_v41_stderr.log`
