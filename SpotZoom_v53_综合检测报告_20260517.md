# SpotZoom v53 综合检测报告（2026-05-17）

## 1. 本轮改动摘要
- 新增模块：`8821L/SpotZoom_Machine_Learning/innovation_frontier_v53.py`
- 集成改动：`8821L/SpotZoom.py`
  - 新增 `frontier_v53` 的导入、配置、CLI、环境校验、运行指标、清理钩子、主流程调用。
  - 新增 `_apply_frontier_v53_assoc`，记录 `occlusion_hits/tree_switch_hits/observation_reupdates` 等指标。
- 设计目标：在 v52（层次记忆+CMC）基础上，补充“遮挡感知 + 记忆树路径切换 + 观测重更新”关联策略。

## 2. 互联网前沿对标与可模仿模块

### 2.1 本轮参考项目（已核验）
1. ByteTrack（ECCV 2022）
- 关键点：不丢弃低分框，改为纳入关联提升遮挡场景召回。

2. OC-SORT（CVPR 2023）
- 关键点：Observation-Centric Re-Update（观测中心重更新），降低遮挡后轨迹漂移。

3. BoT-SORT（2022）
- 关键点：在关联中引入 Camera Motion Compensation（CMC）。

4. SAM 2（2024）与 SAM2Long（2024/2025）
- 关键点：流式记忆与长时序分层记忆/多路径记忆树。

5. CoTracker3（ICLR 2025）与 Norfair
- 关键点：在线、低开销、实时友好的工程设计。

6. OA-SORT（arXiv 2026）
- 关键点：显式遮挡系数建模与自适应关联。

### 2.2 映射到 SpotZoom v53 的补充模块
- 已落地：
  - `OcclusionTreeAssociationGate`
  - occlusion-aware 自适应门控（`occlusion_gate_scale`）
  - short/long memory tree path 选择与切换打点（`tree_path/tree_switched`）
  - 观测重更新（`observation_reupdated`）
- 未充分触发（待数据集覆盖）：
  - 高遮挡分支（`occlusion_hits`）
  - 记忆树切换分支（`tree_switch_hits`）

## 3. 自动检测执行结果

### 3.1 构建与启动
- `python -m py_compile`：通过
  - `8821L/SpotZoom.py`
  - `8821L/SpotZoom_Machine_Learning/innovation_frontier_v53.py`
  - `XPS/xps_admin_console/server.py`
- `SpotZoom --help`：通过（`artifacts/help_snapshot_v53.txt`）
- `--check-env`：通过（有提示：`--frame-source-image` 场景建议 `--skip-roi`）

### 3.2 核心流程（SpotZoom）
- 基础 smoke：`artifacts/spotzoom_run_report_v53_smoke_clean.json`
  - `status=success`，`elapsed_s=1.016`
  - `v53: calls=5, rejects=0, refines=4, failures=0`

- 偏移+噪声：`artifacts/spotzoom_run_report_v53_offcenter_noisy_clean.json`
  - `status=success`，`elapsed_s=1.154`
  - `v53: calls=5, rejects=0, refines=4, occlusion_hits=0, tree_switch_hits=0, observation_reupdates=0`

- 失败路径（XPS 不可达）：`artifacts/spotzoom_run_report_v53_xps_fail_clean.json`
  - `status=failed`（符合预期）
  - `error=XPS controller connection failed`

### 3.3 A/B（v52-only vs v53-enabled）
- 文件：`artifacts/ab_v53_summary.json`
- 结果：
  - baseline(v52-only) `elapsed_s=1.062`
  - enabled(v53) `elapsed_s=1.154`
  - 额外耗时 `+0.092s`

### 3.4 页面访问 / 接口请求 / 静态资源 / 网络异常
- 文件：`artifacts/xps_http_check_v53.json`
- 关键状态码：
  - 页面与静态：`/` 200，`/styles.css` 200，`/app.js` 200，`/static/styles.css` 200
  - API：`/api/session` 200，`/api/features` 200，`/api/system/config` 200
  - 异常请求：
    - 非法 JSON（`POST /api/connect`）=> 400
    - 超大 JSON（`POST /api/connect`）=> 413
    - 路径穿越（`/static/../server.py`）=> 403
    - 缺失静态（`/does-not-exist.js`）=> 404
  - 网络异常：关闭端口请求捕获 `URLError timed out`

### 3.5 前端控制台与静态语法
- 文件：`artifacts/frontend_console_check_v53.json`
- 结果：
  - `node --check app.js` 通过
  - `console.log/warn/error` 调用数均为 0
  - 浏览器运行时 console/pageerror 采集仍未闭环（缺 Playwright/Selenium）

### 3.6 数据库读写
- 文件：`artifacts/db_check_v53.json`
- 结果：未发现运行链路中的 DB 驱动调用与 DB 文件工件。

### 3.7 代码质量与模块结构
- 文件：`artifacts/code_quality_v53.json`
- 指标：
  - `SpotZoom.py` 行数：`17624`
  - 函数数：`230`
  - `frontier_apply`：`30`
  - `except Exception`：`119`
  - 热点：
    - `parse_args` 2514 LOC
    - `check_environment` 914 LOC
    - `SpotZoomController.__init__` 1897 LOC

## 4. 问题清单（优先级 / 影响模块 / 建议）

### P1
1. 参数层与初始化层继续膨胀（高耦合）
- 影响模块：`SpotZoom.py`（`parse_args` / `check_environment` / `SpotZoomController.__init__`）
- 风险：每次 frontier 增量都扩大回归面，定位和验证成本持续上升。
- 建议：
  - 拆出 `frontier_registry.py`（注册/构造）
  - 拆出 `config_schema.py` 与 `config_validate.py`
  - 统一 `frontier apply` 模板，减少重复 reject/accept/event 逻辑。

2. 运行时前端控制台日志仍未自动闭环采集
- 影响模块：`XPS/xps_admin_console/static/*`
- 风险：Promise 异常、资源失败链路可能被漏检。
- 建议：引入 Playwright 采集 `console/pageerror/requestfailed`。

### P2
3. v53 关键增强分支尚未在本轮样本触发
- 影响模块：`innovation_frontier_v53.py`（occlusion/tree-switch/observation-reupdate）
- 现象：`occlusion_hits=0`, `tree_switch_hits=0`, `observation_reupdates=0`。
- 建议：补充遮挡与跨场景回放样本，建立 v53 专项回归集。

4. v53 增益引入额外时延
- 影响模块：`innovation_frontier_v53.py` + `_apply_frontier_v53_assoc`
- 现象：A/B 单样本 `+0.092s`。
- 建议：
  - 将 occlusion/path 决策降频（例如每 N 帧更新）
  - 仅在低置信或高残差时启用 v53 全路径。

### P3
5. 宽异常捕获仍然较多
- 影响模块：`SpotZoom.py` 多处恢复链路
- 风险：真实故障易被旁路吞并。
- 建议：分批替换为显式异常类型与统一错误码。

## 5. 下一步优化方案
1. 做 `frontier gate` 工厂化重构（先覆盖 v50-v53），压缩重复代码。
2. 建立 v53 专项回放集：遮挡、突发亮度变化、场景切换。
3. 引入无头浏览器巡检作业，补齐前端运行时日志闭环。
4. 做 v53 参数灵敏度扫描（`occlusion_gate_scale`/`reupdate_threshold`/memory budgets）。

## 6. 本轮产物索引
- `8821L/SpotZoom_Machine_Learning/innovation_frontier_v53.py`
- `8821L/SpotZoom.py`
- `8821L/artifacts/help_snapshot_v53.txt`
- `8821L/artifacts/spotzoom_run_report_v53_smoke_clean.json`
- `8821L/artifacts/spotzoom_run_report_v53_offcenter_noisy_clean.json`
- `8821L/artifacts/spotzoom_run_report_v53_xps_fail_clean.json`
- `8821L/artifacts/spotzoom_run_report_v53_ab_baseline_v52only.json`
- `8821L/artifacts/ab_v53_summary.json`
- `8821L/artifacts/xps_http_check_v53.json`
- `8821L/artifacts/frontend_console_check_v53.json`
- `8821L/artifacts/db_check_v53.json`
- `8821L/artifacts/code_quality_v53.json`
- `8821L/artifacts/v53_summary_snapshot.json`

## 7. 参考来源
- ByteTrack: https://arxiv.org/abs/2110.06864
- OC-SORT: https://arxiv.org/abs/2203.14360
- BoT-SORT: https://arxiv.org/abs/2206.14651
- SAM2: https://arxiv.org/abs/2408.00714
- SAM2Long: https://arxiv.org/abs/2410.16268
- CoTracker3: https://arxiv.org/abs/2410.11831
- OA-SORT: https://arxiv.org/abs/2603.06034
- Norfair: https://github.com/tryolabs/norfair
