# SpotZoom v35 综合检测报告（2026-05-16）

## 1. 本轮变更概览
- 新增模块：`SpotZoom_Machine_Learning/innovation_frontier_v34.py`
  - 引入 `AssociationBridgeGate`（双阈值低置信关联桥接）。
  - 设计灵感：ByteTrack（高/低置信关联）、Norfair（距离门控 + hit counter）、CoTracker（轨迹连续性）。
- 接入主文件：`SpotZoom.py`
  - 新增 v34 导入回退、配置项、CLI 参数、环境校验、运行时初始化、平滑链路调用、指标上报、事件记录、生命周期 reset。

## 2. 前沿项目对标与可模仿能力
- ByteTrack（https://arxiv.org/abs/2110.06864）
  - 可模仿点：低分检测不立即丢弃，先走二阶段关联。
- Ultralytics Track 文档（https://docs.ultralytics.com/modes/track/）
  - 可模仿点：`track_high_thresh` / `track_low_thresh` / `track_buffer` 这类阈值分层与缓存策略。
- Norfair Tracker 参考（https://tryolabs.github.io/norfair/dev/reference/tracker/）
  - 可模仿点：基于距离阈值的匹配与 `hit_counter_max` 惯性控制。
- CoTracker 项目（https://github.com/facebookresearch/co-tracker）
  - 可模仿点：在可见/遮挡变化下维持点轨迹连续性的保守更新思路。

## 3. 自动检查执行结果

### 3.1 构建与启动
- `py_compile`：通过（`SpotZoom.py` + `innovation_frontier_v34.py`）。
- `--check-env`（默认硬件模式）：失败（缺少 `pylablib`，与历史一致）。
- `--check-env`（`dryrun + classic + simulated frame + v29~v34`）：通过。

### 3.2 核心功能流程（dryrun 仿真）
- 运行组合：`classic + dryrun XY/Z + v29/v30/v31/v32/v33/v34`。
- 场景结果：
  - `center`：success，`detect_success=5`，v34 `calls=5/refines=0/rejects=0/failures=0`
  - `offcenter`：success，`detect_success=5`，v34 `calls=5/refines=0/rejects=0/failures=0`
  - `black`：failed（预期，初始检测失败），`detect_miss=2`，v34 `calls=0`
  - `offcenter+jitter`：success，v34 `calls=5/refines=2/rejects=2/failures=0`

### 3.3 页面/接口/数据库/静态资源
- 页面访问：未发现可启动 Web 前端（`package.json` 仅 `docx` 依赖，无 `scripts`）。
- 接口请求：未发现业务 HTTP API 调用链（未检出 FastAPI/Flask/requests/httpx 主链）。
- 数据库读写：未发现 `.db/.sqlite/.sql` 业务库文件与 ORM 主链。
- 前端控制台日志、网络异常、静态资源加载：当前项目形态不适用（无可运行前端站点）。

### 3.4 代码质量与结构
- `flake8`：不可执行（环境未安装）。
- `pylint -E`：不可执行（环境未安装）。
- 结构扫描（AST）：
  - `SpotZoom.py` 约 12666 行；
  - 类数量 22，函数（含方法）约 206；
  - 超长函数：`parse_args` / `SpotZoomController.__init__` / `check_environment` / `_detect_center_with_retry` / `main`。

## 4. 问题分级与影响

### P1
1. 硬件模式环境缺失 `pylablib` 导致默认启动失败  
   - 影响模块：设备连接与真实执行路径（XY/Z 驱动初始化）。
   - 修复建议：补齐运行环境依赖清单并提供一键安装脚本（含版本锁定）。

2. `SpotZoom.py` 单体规模过大、耦合高，回归风险高  
   - 影响模块：参数解析、环境校验、控制器初始化、检测重试链路。
   - 修复建议：按“配置层/驱动层/检测平滑层/报告层”拆分；先把 v29~v34 平滑门控抽成独立 orchestrator。

### P2
1. v34 在抖动场景出现拒绝（`association_distance_too_large`）  
   - 影响模块：`frontier_v34` 低置信关联桥接，边界场景稳定性。
   - 修复建议：小幅放宽 `frontier_v34_base_gate_px` 或引入 gate warmup（前 N 帧更宽门控）。

2. 静态质量门缺失（flake8/pylint 未安装）  
   - 影响模块：全局代码质量可见性与 CI 兜底能力。
   - 修复建议：将 `flake8/pylint` 纳入固定虚拟环境，添加最小 CI 检查。

### P3
1. 可选模块告警噪声（例如 v17 的 PyTorch warning）  
   - 影响模块：日志可读性与告警噪声比。
   - 修复建议：对可选依赖告警做降频/分级输出（首次提示 + 汇总）。

## 5. 下一步优化方案
1. 做 v34 参数小步调优（先 `base_gate_px`、`residual_gate_scale`），目标是 jitter 场景减少误拒绝而不增加漂移。
2. 建立最小回归集（center/offcenter/black/jitter）并固定为自动 smoke pipeline。
3. 拆分 `SpotZoom.py` 的超长函数与单体初始化，优先降低回归耦合面。
4. 增加依赖探针与分层启动模式（`hardware/full`、`dryrun/minimal`）的显式 profile。

