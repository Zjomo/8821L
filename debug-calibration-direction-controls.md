# debug-calibration-direction-controls

状态: [OPEN]

## 问题描述
需要将“标定步长”和“标定间隔”做成 UI 可调控件，并修复闭环准直过程中可能出现的方向反向/发散问题。

## 假设
1. 闭环纠偏的方向符号与实际电机/光斑运动方向相反，导致误差越修越大。
2. 标定时固定步长与固定间隔写死在代码中，导致无法根据系统响应速度调整，容易超调或采样不足。
3. 标定使用的镜组/轴映射与闭环执行时的轴映射不一致，导致纠偏施加到错误轴。
4. 闭环使用的 Kp 或纠偏周期过激，叠加相机/电机延迟后形成正反馈发散。

## 证据摘要
- 标定代码写死步长 `CALIB_STEP = 200`，间隔 `_calib_timer.setInterval(2000)`。
- 闭环纠偏使用 `steps_x = int(dx * steps_per_px * kp)`，`steps_y = int(dy * steps_per_px * kp)`，方向完全依赖轴符号配置。
- 标定日志显示 `+200` 步会让 X/Y 质心负向移动，说明系统存在反号关系。
- 闭环刷新周期为 `100ms`，可能在电机/相机响应未稳定时再次纠偏。

## 目标
- 将标定步长、标定间隔暴露为 UI 可调参数并接入配置流程。
- 修正闭环方向问题，避免误差越修越大。
- 保持修改最小化，避免影响其他功能。
