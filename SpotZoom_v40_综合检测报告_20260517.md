# SpotZoom v40 综合检测报告（2026-05-17）

## 1) 本轮目标与变更
- 在 `SpotZoom_Machine_Learning/innovation_frontier_v40.py` 新增 **Iterative Phase Bridge** 模块（v40）。
- 在 `SpotZoom.py` 完整接入 v40：
  - 动态导入/回退加载
  - 类型绑定（Config/Result/Gate）
  - 默认配置、`AlignmentConfig` 字段、CLI 参数、参数校验
  - 运行时初始化、平滑链路调用、reset 生命周期
  - 运行指标上报（calls/refines/rejects/bridge_hits/iterative_hits/failures）

## 2) 互联网前沿对标（可模仿模块）
- CoTracker（Meta）：长时序点轨迹连续性建模，可模仿“轨迹记忆 + 轨迹置信门控”。
- ByteTrack：高低置信双阈值关联，可模仿“低置信桥接恢复策略”。
- Norfair：distance gate + hit_counter 惯性机制，可模仿“接受/拒绝滞回逻辑”。
- OpenCV motion（4.14 文档）：`phaseCorrelate` / `phaseCorrelateIterative`，可模仿“迭代相位相关微位移估计”。
- laserbeamsize：光斑形态参数化（宽度/椭圆度）可模仿“形态质量门控增强”。

## 3) 自动化检查结果

### 3.1 构建与启动
- `python -m py_compile SpotZoom.py SpotZoom_Machine_Learning/innovation_frontier_v40.py`：通过。
- `run_spotzoom.bat --check-env --frontier-v40-assoc-gate`：通过。
  - 仍有 `pylablib/pkg_resources` 弃用告警。

### 3.2 核心功能流程（SpotZoom）
- 运行命令（模拟图 + dryrun stage）在初始化后长时间阻塞，45~180 秒窗口内未退出。
- 关键对照：
  - 开启 v40：超时
  - 关闭 v40（baseline）：同样超时
- 结论：该阻塞是既有流程问题，非本次 v40 回归。

### 3.3 v40 模块单体回归（合成帧）
- steady 场景：accepted=5, refined=4, rejected=3, iterative=7
- jitter 场景：accepted=9, refined=8, rejected=3, iterative=11
- black 场景：accepted=1, refined=0, rejected=3, iterative=0
- 说明：v40 在抖动场景能稳定触发迭代相位路径并完成细化；黑图低纹理下会走拒绝保护。

### 3.4 页面访问 / API 请求 / 静态资源 / 网络异常（XPS 管理台）
- 使用 `XPS/xps_admin_console/server.py` 在本地端口自动拉起后进行 HTTP 探测。
- 根路径与核心 API：
  - `GET /` 200
  - `GET /api/features` 200
  - `GET /api/session` 200
  - `GET /api/system/stages-db` 200（DB 读取路径可用）
- 静态资源：
  - `GET /styles.css` 200
  - `GET /app.js` 200
  - `GET /static/styles.css` 404（路由设计如此，非必然故障）
- 异常请求：
  - `POST /api/connect` 非法端口 -> 400（错误处理生效）
  - `POST /api/files/write` 未连接状态 -> 400（错误处理生效）
  - 超大 payload -> 400（当前实现返回 400，未返回 413）
  - `GET` 到未监听端口 -> 超时（网络异常路径可复现）

### 3.5 代码质量与结构
- `flake8 SpotZoom.py innovation_frontier_v40.py --count --statistics`
  - 总计 1988 项，主体为 `E501` 长行历史债务。
- `pylint -E SpotZoom.py innovation_frontier_v40.py`
  - 以 `cv2` no-member / 可选依赖导入误报为主，仍存在真实环境依赖问题（如 `pylablib`/硬件模块）。
- `pytest`：当前 Python 运行时未安装（`No module named pytest`）。
- 结构复杂度扫描：
  - `SpotZoom.py` 14454 行，`def` 212
  - `SpotZoomController.__init__` 约 1483 行
  - `parse_args` 约 1928 行
  - `_apply_frontier_v34..v40_assoc` 逻辑相似度最高约 0.9687（高重复）

## 4) 问题分级（优先级 / 影响模块 / 建议）

### P1
- 问题：SpotZoom 模拟 dryrun 主流程长时间阻塞（v40 前后均复现）
- 影响模块：`SpotZoom.py` 主流程（main -> controller 初始化/运行）
- 风险：自动回归不可稳定完成，影响持续交付与回归置信度
- 建议：
  1. 在 main 流程关键节点加阶段日志（build_xy_stage/build_z_stage/controller init/run entry）与 watchdog 超时退出。
  2. 增加 `--smoke-once` 或 `--detect-once` 模式，支持 CI 快速完成单轮检测。
  3. 将模拟流程与硬件流程分离为可独立测试的子命令。

### P1
- 问题：主文件单体过大、关联函数重复度高（v34-v40）
- 影响模块：`SpotZoom.py` 可维护性、回归风险
- 风险：新增功能改动面过大，缺陷定位成本高
- 建议：
  1. 提取统一 `AssociationGateAdapter`，将 v34-v40 通用事件/reject/metric 逻辑模板化。
  2. 将 parse_args 拆分为参数组构建函数（recovery/frontier/control/io）。

### P2
- 问题：静态检查历史债务高（E501 为主），pytest 缺失
- 影响模块：全仓可读性与质量门禁
- 风险：CI 质量闸难落地，隐性回归难提前发现
- 建议：
  1. 先在新增文件和新增改动范围启用分阶段 lint 门禁。
  2. 补充最小 `pytest` 冒烟（模拟帧 + dryrun stage + 一次检测）。

### P2
- 问题：XPS 页面文本出现编码异常（中文显示为乱码）
- 影响模块：`XPS/xps_admin_console/static/index.html`
- 风险：可用性与可维护性下降
- 建议：统一文件编码为 UTF-8（无 BOM）并在编辑器层面固定编码策略。

## 5) 本轮产物
- `SpotZoom_Machine_Learning/innovation_frontier_v40.py`
- `SpotZoom.py`（v40 全链路接入）
- `artifacts/spotzoom_help_v40.txt`
- `artifacts/xps_http_check_v40.json`
- `artifacts/xps_http_check_v40_static.json`
- `artifacts/xps_server_v40_stdout.log`
- `artifacts/xps_server_v40_stderr.log`
- `artifacts/tmp_v40_debug.log`
- `artifacts/tmp_baseline_no_v40.log`
