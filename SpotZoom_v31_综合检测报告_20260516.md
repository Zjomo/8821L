# SpotZoom v31 综合检测报告（2026-05-16）

## 1) 本轮目标与范围
- 目标：检索前沿同类项目，提炼可模仿功能模块，并补充 `SpotZoom.py`。
- 代码改动：
  - 新增 `SpotZoom_Machine_Learning/innovation_frontier_v30.py`
  - 集成 `SpotZoom.py` 的 v30 配置、CLI、运行链、指标、校验与事件上报。
- 自动化检查范围：构建/启动、核心流程、接口请求、数据库读写、前端控制台与页面访问、网络异常、静态资源、代码质量、模块结构。

## 2) 前沿同类项目调研与可借鉴模块
基于官方文档/仓库调研（见文末链接），建议重点借鉴：

1. 时序一致性与遮挡恢复
- 来源：CoTracker3、Norfair、ByteTrack
- 可模仿：长时序一致性、低置信度关联恢复、轻量跟踪状态维护。
- 本轮落地：v30 相位锁定精修与 v29 串联，增强时序稳态。

2. 亚像素定位与轨迹平滑
- 来源：TrackMate、Trackpy、OpenCV phaseCorrelate
- 可模仿：亚像素位移估计、轨迹一致性约束、窗口化频域匹配。
- 本轮落地：`frontier_v30_phase_lock`（局部 patch 相位相关 + 置信度加权融合）。

3. 漏检回退与鲁棒门控
- 来源：ThunderSTORM（多尺度思路）、ByteTrack（低分检测利用）
- 可模仿：漏检时退化策略组合、可信度门控。
- 本轮建议：将 v27/v28/v29/v30 整合为可学习策略路由（后续计划）。

## 3) 代码补充说明（v30）
- 新增模块：
  - `PhaseLockConfig`
  - `PhaseLockResult`
  - `PhaseLockRefiner`
- 核心逻辑：
  - 从当前帧和上一帧局部 patch 做 `phaseCorrelate` 位移估计。
  - 基于 response/置信度/最大位移阈值进行拒绝或融合。
  - 输出 refined center，并上报事件与统计指标。
- 新增 CLI：
  - `--frontier-v30-phase-lock`
  - `--frontier-v30-patch-radius`
  - `--frontier-v30-response-threshold`
  - `--frontier-v30-max-shift-px`
  - `--frontier-v30-blend`
  - `--frontier-v30-min-patch-std`
  - `--frontier-v30-confidence-gain`

## 4) 自动化检测执行结果

### 4.1 构建与启动
- `py_compile`：通过。
- `--check-env`：通过。
- 观察：存在 `pkg_resources` 弃用告警（setuptools 未来版本风险）。

### 4.2 核心功能流程（模拟帧 + dryrun stage）
- `spotzoom_run_report_v31_center_v30.json`：成功，v30 调用 5 次，0 失败。
- `spotzoom_run_report_v31_offcenter_v30.json`：成功，v30 调用 5 次，0 失败。
- `spotzoom_run_report_v31_black_v30.json`：失败（预期），初始目标检测 6 次均 miss。
- `spotzoom_run_report_v31_offcenter_jitter_v30.json`：成功，v29 refine 2 次，v30 refine 2 次，事件流可见 `frontier_v30_phase_lock_refined`。

### 4.3 页面访问 / 前端控制台 / 静态资源
- 未发现 Web 前端工程（无 html/react/vue 构建入口）。
- 因此页面访问、前端控制台、静态资源加载检查：当前项目形态不适用（N/A）。

### 4.4 接口请求 / 网络异常
- 未发现 HTTP API 服务端/客户端调用链（无 requests/httpx/fetch 主流程依赖）。
- 发现硬件通信 socket（XPS stage），属于设备控制通道而非 Web API。

### 4.5 数据库读写
- 未发现 sqlite/sqlalchemy/mysql/redis 等数据库读写路径。

### 4.6 代码质量与模块结构
- `flake8` / `pylint`：当前环境未安装，无法直接执行。
- 结构扫描结论：
  - `SpotZoom.py` 约 11611 行，函数数 202。
  - 超长函数明显（如 `parse_args` 1320 行、`__init__` 1090 行、`check_environment` 491 行）。
  - 恢复链路函数 8 个，存在高重复结构。
  - `except Exception` 计数 94 处，异常吞噬风险偏高。

## 5) 问题优先级（本轮）

### P0（立即处理）
- 无阻断性故障。

### P1（高优先）
1. 高耦合单体风险
- 影响模块：`SpotZoom.py`
- 现象：超大文件 + 超长函数 + 多策略堆叠。
- 风险：回归面大、调参困难、故障定位慢。
- 建议：按“检测/恢复/控制/配置/报告”拆分为子模块，先抽 `parse_args` 与 recovery pipeline。

2. 质量门禁缺失
- 影响模块：全项目
- 现象：`flake8`/`pylint` 不可用，缺少持续静态检查。
- 风险：语义退化与风格漂移难以及时发现。
- 建议：补 `requirements-dev.txt` + pre-commit + CI 最小门禁（py_compile + ruff/flake8 + pylint E/F）。

### P2（中优先）
3. 异常粒度偏粗
- 影响模块：`SpotZoom.py`
- 现象：大量 `except Exception`。
- 风险：真实错误被降级为 bypass，导致隐性功能退化。
- 建议：按错误类型细化捕获，关键链路记录 error_code 与 context。

4. 黑图场景仅“失败退出”，缺少主动降级策略
- 影响模块：检测重试链路
- 现象：黑图 6 次 miss 后退出。
- 建议：加入“低纹理/全黑”快速判别与提前终止码，减少无效重试。

## 6) 下一步优化方案
1. 模块化重构第一阶段（建议 1-2 天）
- 拆分 `parse_args`、`check_environment`、`run` orchestration。
- 抽象统一 recovery strategy 接口，减少 8 条恢复链重复。

2. 质量体系补齐（建议半天）
- 安装并固定 `ruff/flake8/pylint` 版本。
- 新增最小 CI：编译 + 冒烟 + 静态检查。

3. v30 强化验证（建议半天）
- 增加 jitter/noise 参数网格回归。
- 统计 v30 refine 收益（误差下降、收敛轮次、误拒率）。

## 7) 本轮产物
- 代码：
  - `SpotZoom.py`（v30 全链路接入）
  - `SpotZoom_Machine_Learning/innovation_frontier_v30.py`
- 报告：
  - `spotzoom_run_report_v31_center_v30.json`
  - `spotzoom_run_report_v31_offcenter_v30.json`
  - `spotzoom_run_report_v31_black_v30.json`
  - `spotzoom_run_report_v31_offcenter_jitter_v30.json`
- 事件流：
  - `spotzoom_events_v31_center_v30.jsonl`
  - `spotzoom_events_v31_offcenter_v30.jsonl`
  - `spotzoom_events_v31_black_v30.jsonl`
  - `spotzoom_events_v31_offcenter_jitter_v30.jsonl`

## 8) 调研来源（官方）
- CoTracker: https://github.com/facebookresearch/co-tracker
- Norfair: https://github.com/tryolabs/norfair
- ByteTrack: https://github.com/ifzhang/ByteTrack
- TrackMate: https://imagej.net/plugins/trackmate/
- ThunderSTORM: https://github.com/zitmen/thunderstorm
- OpenCV motion/phase correlation docs: https://docs.opencv.org/3.4/d7/df3/group__imgproc__motion.html
- Trackpy docs: https://soft-matter.github.io/trackpy/dev/generated/trackpy.locate.html
