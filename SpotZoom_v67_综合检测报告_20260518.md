# SpotZoom v67 综合检测报告（2026-05-18）

## 1. 前沿同类项目调研（本轮）

1. CoTracker3（ICCV 2025）  
- 论文: https://openaccess.thecvf.com/content/ICCV2025/html/Karaev_CoTracker3_Simpler_and_Better_Point_Tracking_by_Pseudo-Labelling_Real_Videos_ICCV_2025_paper.html  
- 仓库: https://github.com/facebookresearch/co-tracker  
- 可模仿模块: 可见性/置信度联合轨迹过滤、遮挡阶段的稳态回退。

2. Norfair Tracker  
- 文档: https://tryolabs.github.io/norfair/dev/reference/tracker/  
- 可模仿模块: `hit_counter_max`、`initialization_delay`、`pointwise_hit_counter_max` 的命中/拒绝平衡机制。

3. trackpy Adaptive Search  
- 文档: https://soft-matter.github.io/trackpy/dev/tutorial/adaptive-search.html  
- 可模仿模块: `adaptive_step` + `adaptive_stop` 的动态门限收缩策略（避免一次性硬拒绝）。

4. DeepTrack2  
- 仓库: https://github.com/DeepTrackAI/DeepTrack2  
- 可模仿模块: 模块化数据处理/检测链路的可组合接口（便于后续从主文件拆链）。

## 2. SpotZoom.py 本轮补充

目标文件: `E:\jupyter file\2_Optics\8821L\SpotZoom.py`

新增 `frontier v67 adaptive-search guard`（在 v66 后执行）：
- 核心思路: trackpy 自适应门限收缩 + Norfair hit-counter warmup + CoTracker3 可见性驱动松弛。
- 新增配置（默认配置 / `AlignmentConfig` / CLI / 参数校验 / open-source bundle / 运行指标全链路接入）：
  - `frontier_v67_adaptive_search_guard_enabled`
  - `frontier_v67_history_size`
  - `frontier_v67_base_gate_px`
  - `frontier_v67_adaptive_step`
  - `frontier_v67_adaptive_stop_px`
  - `frontier_v67_max_iters`
  - `frontier_v67_hit_counter_max`
  - `frontier_v67_initialization_delay`
  - `frontier_v67_pointwise_hit_counter_max`
  - `frontier_v67_visibility_relax_threshold`
  - `frontier_v67_visibility_relax_gain`
  - `frontier_v67_confidence_penalty`
  - `frontier_v67_confidence_boost`
- 新增运行指标:
  - `frontier_v67_guard_checks`
  - `frontier_v67_guard_refines`
  - `frontier_v67_guard_rejects`
  - `frontier_v67_guard_soft_accepts`
  - `frontier_v67_guard_failures`
- 新增控制器状态:
  - `self._frontier_v67_center_history`
  - `self._frontier_v67_reject_streak`
  - `self._frontier_v67_hit_counter`
- 新增方法:
  - `_apply_frontier_v67_adaptive_search_guard(...)`
- 主链路更新:
  - `v57 -> v58 -> v59 -> v63 -> v64 -> v65 -> v66 -> v67`
- Open-source bundle 联动:
  - `microscopy_tracking`
  - `resilient_association`
  - `frontier_2026_tracking_stack`
  以上 preset 已默认启用 v67。

## 3. 自动化检测结果

### 3.1 构建 / 启动 / 语法

- `python -m py_compile 8821L/SpotZoom.py`: PASS
- `python -m py_compile XPS/xps_admin_console/server.py`: PASS
- `node --check XPS/xps_admin_console/static/app.js`: PASS
- `python SpotZoom.py --check-env ... --frontier-v67-adaptive-search-guard --skip-roi`: PASS（含 YOLO 缺失告警，自动 fallback classic）

### 3.2 SpotZoom 核心流程回放

1) `spotzoom_run_report_v67_smoke.json`  
- 状态: success  
- 指标: `detect_success=5/5`  
- v65/v66/v67: 全部 soft_accept，0 reject。

2) `spotzoom_run_report_v67_offcenter_noisy.json`  
- 状态: success  
- 指标: `detect_success=5/5`  
- v66: `refines=1`  
- v67: `checks=5, refines=0, rejects=0, soft_accepts=5`。

3) `spotzoom_run_report_v67_probe.json`（高扰动+紧门控）  
- 状态: success  
- 指标: `detect_success=5/5`  
- v66: `refines=1, rejects=1`  
- v67: `refines=1, rejects=0, soft_accepts=4`（触发了 v67 refine 分支）。

4) `spotzoom_run_report_v67_reject_probe.json`（极限拒绝压测）  
- 状态: success  
- 指标: `detect_success=5/5`  
- v65: `rejects=2`；v66: `rejects=2`；v67: `refines=1, rejects=1`  
- 说明: v67 reject 分支已被真实触发，同时仍保持整体流程可收敛。

5) 故障注入
- `spotzoom_run_report_v67_missing_image.json`: failed（符合预期，缺失图像）
- `spotzoom_run_report_v67_xps_fail.json`: failed（符合预期，XPS 连接失败）

### 3.3 页面访问 / 静态资源 / 接口请求 / 网络异常 / 前端控制台

- 文件: `8821L/artifacts/xps_http_check_v67.json`
- 服务日志: `8821L/artifacts/xps_server_v67_stdout.log`
- GET:
  - `/`, `/static/app.js`, `/static/styles.css`, `/api/features`, `/api/session` -> 200
  - `/api/unknown`, `/static/not_found.js` -> 404（预期）
- POST:
  - `/api/connect`（127.0.0.1:1 超时）-> 400
  - `/api/controller/raw`（未连接）-> 400
  - `/api/files/upload`（非法 base64）-> 400
  - `/api/files/upload`（超 2MB）-> 413
  - `/api/disconnect` -> 200
- Playwright runtime:
  - `console.error`: `Failed to load resource: net::ERR_FAILED`
  - `requestFailed`: Google Fonts CSS 请求失败（外网字体依赖导致）
  - `pageErrors`: 0

### 3.4 数据库读写

- 本轮未在 `SpotZoom.py` 和 `XPS/xps_admin_console/server.py` 检出 sqlite/mysql/postgres/redis/mongodb 等读写路径。
- 当前系统仍以图像处理、设备控制和 HTTP 接口交互为主。

### 3.5 代码质量与模块结构

- `SpotZoom.py`: 19996 行，`except Exception` 131 处，`_apply_frontier_v*` 方法 39 个。
- `SpotZoomController.__init__`: 2203 行（高耦合）。
- `XPS/xps_admin_console/server.py`: 916 行，`except Exception` 13 处。

## 4. 问题分级（优先级 / 影响模块 / 修复建议）

### P1

1) 主文件耦合持续过高  
- 影响模块: `8821L/SpotZoom.py`  
- 现象: `SpotZoomController.__init__` 2203 行，frontier 链持续增长。  
- 建议: 抽离 `v58~v67` 到独立 post-association pipeline 模块，主控只保留编排。

2) 前端外部字体依赖导致 runtime 网络异常  
- 影响模块: `XPS/xps_admin_console/static/index.html`（Google Fonts）  
- 现象: 浏览器控制台出现 `net::ERR_FAILED`，失败请求可复现。  
- 建议: 改为本地字体资源或增加降级字体策略，避免运行环境外网依赖。

3) YOLO 后端依赖缺失（自动 fallback classic）  
- 影响模块: `SpotZoom` 检测后端能力上限  
- 现象: `--check-env` 告警 ultralytics 缺失。  
- 建议: 固化独立 Python 3.10~3.12 YOLO 运行时，避免线上环境能力漂移。

### P2

4) 接口异常语义仍偏粗  
- 影响模块: `XPS/xps_admin_console/server.py`  
- 现象: timeout/未连接/参数错误主要映射 400。  
- 建议: 细化 408/409/422/5xx，并统一 `error_code` 字段。

5) 广谱异常捕获较多  
- 影响模块: `SpotZoom.py`, `server.py`  
- 现象: 依赖日志文本定位根因，静态可分析性弱。  
- 建议: 分阶段改 typed exceptions + 统一错误事件 schema。

### P3

6) 文案编码存在乱码风险  
- 影响模块: `XPS/xps_admin_console/server.py`（features 中文文案）  
- 现象: API 返回摘要字段出现乱码。  
- 建议: 统一 UTF-8 源文件编码并做接口层输出回归检查。

## 5. 下一步优化方案

1. 抽离 `frontier_post_assoc_chain_v58_v67.py`，降低主文件冲突和回归风险。  
2. 为 v67 增加可观测看板（hit_counter/reject_streak 搜索门限轨迹），便于线上调参。  
3. 去除前端外部字体强依赖，补“无外网”场景的 console/network 回归测试。  
4. 在 `server.py` 推行结构化错误码与状态码语义化映射。  
5. 固化 YOLO 运行时（单独 venv + 版本锁定），避免 classic fallback 带来的能力损失。
