# SpotZoom v48 综合检测报告（2026-05-17）

## 1) 本轮代码补充（已落地）
- 文件：`SpotZoom.py`
- 新增能力：`frontier_v48` 风格的推理延迟遥测（tail latency）
  - 新指标：`detector_inference_calls / failures / over_50ms / over_100ms / over_200ms / peak_ms / frontier_v48_latency_alerts`
  - 新汇总：run report 新增 `detector_latency_ms`（`count/mean/p50/p95/p99`）
  - 接入位置：主检测与 recovery scan 主检测入口
- 工程性改进：`RunReporter.flush()` 从“超长手写字段映射”改为自动序列化 `RuntimeMetrics`，降低维护耦合和遗漏风险。

## 2) 自动检测矩阵结果

### 2.1 构建/语法
- `python -m py_compile SpotZoom.py`：通过
- `python -m py_compile XPS/xps_admin_console/server.py`：通过

### 2.2 启动与核心流程
- `SpotZoom --check-env --detector-backend classic`：通过
- 冒烟运行（模拟图像 + dryrun XY/Z）：通过，1 次迭代收敛
  - 报告：`artifacts/spotzoom_run_report_v48_smoke.json`
  - 关键：`detect_attempts=5`, `detect_success=5`, `detector_latency_ms.p99=1.03364`
- 网络异常场景（`--z-driver xps` 指向不可达地址 `203.0.113.11`）：按预期失败
  - 报告：`artifacts/spotzoom_run_report_v48_xps_fail.json`
  - 错误：`XPS controller connection failed`

### 2.3 页面访问 / 接口请求 / 静态资源
- 启动：`XPS/xps_admin_console/server.py --host 127.0.0.1 --port 8788`
- 页面访问：`/` -> 200
- API：`/api/features` -> 200；`/api/session` -> 200
- 静态资源：`/styles.css` -> 200；`/app.js` -> 200
- 网络异常：`POST /api/connect` 到不可达主机，服务端日志记录 `timed out`
- 检查产物：`artifacts/xps_http_check_v48.json`

### 2.4 数据库读写
- SpotZoom 与 xps_admin_console 本轮未发现数据库组件调用（未检测到 sqlite/mysql/postgres 访问路径）。

### 2.5 前端控制台/脚本健康
- `node --check XPS/xps_admin_console/static/app.js`：通过
- `index.html` 引用资源检查：`/styles.css`、`/app.js` 均存在。

### 2.6 代码质量与模块结构
- `SpotZoom.py` 总行数：`15345`
- ML 动态导入检查：`_import_ml_module(...)` 引用 `76` 个模块，`missing=0`
- 复杂度热点（AST 行数）：
  - `parse_args`：2292 行
  - `SpotZoomController.__init__`：1740 行
  - `check_environment`：840 行
  - `main`：555 行
  - `_detect_center_with_retry`：457 行
- 分支热点（近似分支计数）：
  - `check_environment`：564
  - `SpotZoomController.__init__`：290
  - `_detect_center_with_retry`：87
- 异常处理统计：
  - `except` 块：140
  - `except Exception:` 形式：32
  - `pass` 语句：18

## 3) 问题与优先级

### P1（高）
1. 超大函数与高分支复杂度，回归风险高
   - 影响模块：`parse_args`、`check_environment`、`SpotZoomController.__init__`、`_detect_center_with_retry`
   - 风险：小改动引发跨模块副作用，覆盖不足时难定位回归。
   - 建议：按“参数分组/能力分组”拆分为子注册函数与子 pipeline；对关键路径加最小单元测试。

2. 代码耦合偏高（控制器构造承担过多职责）
   - 影响模块：`SpotZoomController.__init__`
   - 风险：扩展新 frontier 模块时初始化和资源清理链持续膨胀。
   - 建议：引入模块注册表 + 生命周期管理器（init/reset/close 统一接口）。

### P2（中）
3. 异常处理中存在 `pass` 吞异常点
   - 影响模块：导入 fallback、清理流程等多个位置。
   - 风险：故障被静默掩盖，诊断信号不足。
   - 建议：最少记录 debug 级日志（含模块名/上下文），禁止无注释空吞。

4. 并发运行锁冲突
   - 影响模块：run lock 机制（`artifacts/spotzoom.lock.json`）
   - 风险：自动化并发触发时出现“假失败”。
   - 建议：自动化任务统一带 `--disable-run-lock` 或按任务分配独立 lock 文件。

5. 依赖可用性告警（PyTorch 缺失）
   - 影响模块：部分 frontier 能力（v17 等）
   - 风险：能力静默降级。
   - 建议：将可选依赖缺失写入 run report 的 `warnings` 字段并分级提示。

### P3（低）
6. 前端外部字体依赖
   - 影响模块：`xps_admin_console/static/index.html`
   - 风险：离线环境下首屏字体回退。
   - 建议：提供本地字体降级策略或可选离线包。

## 4) 对标项目与可借鉴模块（互联网）
1. `pyRTC (pyrtcao)`：强调 `p99` 延迟/吞吐指标与实时闭环基准。
   - 可借鉴：持续 tail-latency 观测、性能回归门禁、分场景 benchmark。
2. `laserbeamsize`：ISO 11146 的 D4σ 光斑分析。
   - 可借鉴：将 D4σ/椭圆参数纳入稳定性门控与质量报告（SpotZoom 已有 profile gate，可继续增强统计口径）。
3. `pypogs`：GUI + API 双通道、闭环跟踪与参数化配置。
   - 可借鉴：将关键闭环状态通过统一 API 结构输出，提升可观测性和外部编排能力。
4. `trackpy adaptive search`：遇到复杂候选时局部收缩 search_range，避免全局性能坍塌。
   - 可借鉴：对恢复链路引入“局部降级/自适应搜索半径”策略，降低极端噪声场景的算力抖动。

## 5) 下一步优化方案
1. 把 `parse_args`、`check_environment`、`__init__` 拆为可测试子模块（先拆配置与依赖检查）。
2. 为 `_detect_center_with_retry` 建立分层恢复策略开关（轻量优先，重型兜底），并记录阶段耗时。
3. 将 `frontier_v48` 延迟告警阈值参数化（预算、窗口、告警比率），纳入 CLI 与配置文件。
4. 增加自动化回归集：
   - 正常收敛（smoke）
   - 网络不可达（XPS）
   - 资源缺失（模型/图像）
   - 高噪声场景（验证恢复链与延迟分布）
