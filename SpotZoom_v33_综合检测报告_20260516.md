# SpotZoom v33 综合检测报告（2026-05-16）

## 1) 本轮代码补充
- 新增模块：`SpotZoom_Machine_Learning/innovation_frontier_v32.py`
  - 轻量轨迹一致性门控（LK 光流 + 前后向一致性）
  - 关键输出：`predicted_center`、`fb_error_px`、`track_error`、`innovation_px`、`blend_weight`
- 接入主流程：`SpotZoom.py`
  - 新增 v32 入口：模块导入、默认配置、`AlignmentConfig`、CLI 参数、参数校验、`--check-env` 告警、运行指标、事件上报、生命周期 reset
  - 新增平滑链路阶段：`_apply_frontier_v32_coherence`

## 2) 互联网前沿类似项目与可模仿模块（本轮调研）
1. CoTracker（Meta）
   - 链接：[github.com/facebookresearch/co-tracker](https://github.com/facebookresearch/co-tracker)
   - 可模仿模块：长时序点轨迹一致性约束、低置信度时的时序先验融合
2. OpenCV Video Tracking
   - 链接：[calcOpticalFlowPyrLK 文档](https://docs.opencv.org/master/dc/d6b/group__video__track.html)
   - 可模仿模块：前后向光流一致性校验（forward-backward check）、异常轨迹拒绝
3. OpenCV Optical Flow Tutorial
   - 链接：[tutorial_optical_flow](https://docs.opencv.org/4.x/d4/dee/tutorial_optical_flow.html)
   - 可模仿模块：稀疏点跟踪稳定性条件与金字塔参数化
4. laserbeamsize（ISO 11146）
   - 链接：[laserbeamsize 文档](https://laserbeamsize.readthedocs.io/en/latest/01-Definitions.html)
   - 可模仿模块：二阶矩束质门控（已在 v31 使用，可继续做动态阈值）
5. pycro-manager（Micro-Manager 生态）
   - 链接：[github.com/micro-manager/pycro-manager](https://github.com/micro-manager/pycro-manager)
   - 可模仿模块：事件/钩子式流程编排，解耦检测与执行动作

## 3) 自动化检查执行结果
| 维度 | 结果 | 说明 |
|---|---|---|
| 构建/语法 | 通过 | `python -m py_compile SpotZoom.py innovation_frontier_v32.py` 通过 |
| 启动/环境 | 条件通过 | `--check-env` 在默认硬件模式下报 `pylablib` 缺失；`--xy-driver dryrun --z-driver dryrun` 通过 |
| 页面访问 | 不适用 | `html_count=0`，未发现 Web 页面入口 |
| 核心功能流程 | 通过 | center/offcenter/jitter 三组成功；black 场景按预期失败 |
| 接口请求 | 不适用 | 未发现 Web API 服务主链路 |
| 数据库读写 | 不适用 | `python_db_import_hits=0` |
| 前端控制台日志 | 不适用 | 无前端页面 |
| 网络异常 | 低风险 | 本轮 dryrun + 本地图像输入，无远程网络依赖 |
| 静态资源加载 | 通过 | `__tmp_spot_center.jpg` / `__tmp_spot_offcenter.jpg` / `__tmp_black.jpg` 正常加载 |
| 代码质量 | 受限 | `flake8` / `pylint` 在当前环境未安装（无法执行） |
| 模块结构 | 风险存在 | `SpotZoom.py` 12155 LOC，frontier apply 方法 9 个，耦合高 |

## 4) 运行产物与关键结果
- `spotzoom_run_report_v33_center_v32.json`：success，v32 calls=5/refines=0/rejects=0
- `spotzoom_run_report_v33_offcenter_v32.json`：success，v32 calls=5/refines=0/rejects=0
- `spotzoom_run_report_v33_black_v32.json`：failed（预期，detect_miss=6）
- `spotzoom_run_report_v33_offcenter_jitter_v32.json`：success，v29_refines=6，v30_refines=7，v31_refines=3，v32_refines=3
- 事件流：`frontier_v32_coherence_accepted` 在 jitter 场景触发 8 次，无 v32 failure

## 5) 问题清单（优先级 / 影响模块 / 修复建议）
1. P1 高：主文件单体过大（`SpotZoom.py` 12155 行）
   - 影响模块：初始化、检测链、恢复链、CLI 参数编排
   - 风险：回归面大、评审困难、修改成本高
   - 修复建议：拆分 `frontier_pipeline.py`、`runtime_bootstrap.py`、`io_drivers.py`
2. P1 高：环境依赖脆弱（默认模式需 `pylablib`）
   - 影响模块：硬件驱动启动与 `--check-env`
   - 风险：CI/自动化常态失败
   - 修复建议：增加 `--check-env --dryrun-only` 快检路径，并输出“硬件依赖缺失但 dryrun 可运行”分级状态
3. P2 中：前沿链路重复逻辑仍高（v27-v32 多个 `_apply_*` 模式同构）
   - 影响模块：指标统计、reject/accept 事件字段维护
   - 风险：新增链路时易漏字段、易引入行为不一致
   - 修复建议：抽象统一 `RefineStage` 接口与通用事件模板
4. P2 中：异常吞噬仍存在（`except Exception: pass` 多处）
   - 影响模块：导入回退、设备收尾、兼容分支
   - 风险：故障根因不可见
   - 修复建议：统一降级到 `LOGGER.debug/warning` 并附模块上下文键
5. P3 低：v32 参数尚未做工况预设
   - 影响模块：不同噪声与倍率场景的可迁移性
   - 风险：跨设备时需要人工调参
   - 修复建议：提供 `v32 preset`（stable / noisy / high-magnification）

## 6) 下一步优化方案
1. 将 v27-v32 合并为可注册 stage pipeline（统一入参、统一 reject/accept payload）。
2. 增加自动回归脚本：center/offcenter/black/jitter 四场景强制比对关键 metrics。
3. 增加 v32 自适应门限：依据最近 N 帧 `fb_error` 与 `innovation` 分位数动态调节阈值。
4. 在 CI 中拆分两套检查：`dryrun-ci`（必过）与 `hardware-ci`（可选，带依赖标签）。
