# SpotZoom v49 综合检测报告（2026-05-17）

## 1) 前沿同类项目调研与可模仿模块

### A. 推理服务工程化（Triton / TorchServe）
- 参考项目：
  - NVIDIA Triton Inference Protocols 与 Quickstart
  - TorchServe Inference API
- 可模仿模块：
  1. 健康检查与就绪探针（health/ready/ping）
  2. 运行时统计接口（请求量、错误率、延迟分位）
  3. 预热机制（warmup）降低首帧抖动
- 本轮已落地到 SpotZoom.py：worker 新增 `health`/`stats`/`warmup` 指令，并支持延迟 p50/p95/p99 统计。

### B. 光学闭环仿真（HCIPy / AOtools）
- 参考项目：
  - HCIPy（波前传感、AO 闭环、光学传播）
  - AOtools（AO 常用工具与方程库）
- 可模仿模块（本轮仅给出下一步规划，未直接改动）：
  1. 可插拔波前传感器链路（Shack-Hartmann / Pyramid）
  2. 闭环校准-运行双态流程（interaction matrix + 控制器）
  3. 统一光学传播与扰动仿真接口（便于离线回放与在线对比）

## 2) SpotZoom.py 本轮补充内容

### 已实现改动
1. 修复 `--worker-detect` 强制 YOLO 的缺陷：
   - 改为 `worker_main()` 走 `build_detector(args)`，遵循 `--detector-backend`。
2. 新增 worker 统计基础设施：
   - `_percentile_linear()`
   - `_worker_stats_payload()`
3. 新增 worker 协议命令：
   - `health` / `ping`：健康状态与当前统计
   - `stats`：分位延迟与错误计数
   - `warmup`：预热 N 次（1~64）并回传耗时与统计
4. 增强 worker 入参容错：
   - 支持 `image_path` 或 `image_jpg_b64`
   - 缺图、解码失败、推理异常分别计数并回包

## 3) 自动化检测执行与结果

### 执行命令（摘要）
- 语法构建：`python -m py_compile SpotZoom.py`
- 环境校验：`python SpotZoom.py --check-env --frame-source-image __tmp_spot_center.jpg --skip-roi --detector-backend classic --xy-driver dryrun --z-driver dryrun`
- 主流程烟测：`python SpotZoom.py --frame-source-image __tmp_spot_center.jpg --skip-roi --no-preview --detector-backend classic --xy-driver dryrun --z-driver dryrun ...`
- worker 协议：`--worker-detect` 下执行 `stats/warmup/detect` 请求

### 检测矩阵
- 构建（语法）：通过
- 启动（check-env）：通过（有可选依赖告警）
- 页面访问：通过（`XPS/xps_admin_console` 根页面 200）
- 核心流程（仿真闭环）：通过
- 接口请求（worker JSON IPC）：通过（已支持 health/stats/warmup/detect）
- 数据库读写：N/A（未发现 sqlite/sqlalchemy/redis 等依赖）
- 前端控制台日志：受限（Playwright/Selenium 缺失，未完成浏览器级 console 抓取）
- 网络异常：通过（`/api/connect` 到不可达主机返回 400，可控失败）
- 静态资源加载：通过（`/styles.css` 与 `/app.js` 均 200，缺失资源返回 404）
- 代码质量/结构：存在高耦合与重复逻辑风险

## 4) 关键问题、优先级、影响模块、修复建议

### P0（已修复）
- 问题：worker 模式忽略 `--detector-backend`，在无 ultralytics 环境直接失败。
- 影响模块：`worker_main()`、检测服务链路。
- 处理：已改为统一走 `build_detector(args)` + `resolved_backend`。

### P1
- 问题：单文件规模过大导致耦合高、回归风险高。
- 证据：`SpotZoom.py` 约 16428 行；`SpotZoomController` 单类约 8447 行；`parse_args` 约 2292 行。
- 影响模块：参数系统、主控制闭环、frontier 扩展。
- 建议：按“检测后端 / 恢复策略 / 设备驱动 / worker 协议 / 配置层”拆分子模块并建立契约测试。

### P1
- 问题：YOLO 运行时与 Python 3.14 环境兼容性/依赖缺失。
- 证据：ultralytics 不可用，提示建议 Python 3.10-3.12。
- 影响模块：YOLO 检测路径、线上切换能力。
- 建议：固定单独推理环境（venv/conda）并在 CI 中显式校验 `--detector-backend yolo`。

### P2
- 问题：大量 frontier 模块在当前环境降级（PyTorch 不可用告警）。
- 影响模块：frontier v17+ 相关增强能力。
- 建议：将“可选模块可用性”写入启动摘要，并在报告中区分“核心路径可用/增强路径降级”。

## 5) 性能与稳定性观察
- smoke 样本中经典检测延迟 p95 约 18~19ms（warmup 前后有波动）。
- 新增 `warmup` 可用于上线前预热，减少首批请求冷启动不确定性。
- 通过 `stats` 可持续观察 parse/decode/inference 错误比，便于定位 IPC 输入质量问题。

## 6) 下一步优化方案（建议执行顺序）
1. 将 worker 协议升级为“明确版本号 + OpenAPI/JSON Schema 描述”，便于外部编排。
2. 拆分 `SpotZoomController`（先抽离 recovery 与 frontier gate），每步加回归用例。
3. 建立三套基线集：center / offcenter / jitter，自动输出 p50/p95 + 收敛轮次。
4. 为 YOLO 与 classic 分别建立健康探针与自动回退策略（含超时熔断）。
5. 引入 HCIPy/AOtools 离线闭环仿真接口，作为 frontier 参数调优沙箱。

## 7) 本轮产物
- 主流程报告：`artifacts/spotzoom_run_report_v49_smoke_after_patch.json`
- 主流程事件流：`artifacts/spotzoom_events_v49_smoke_after_patch.jsonl`
- 日志：`artifacts/spotzoom_v49_after_patch.log`
- XPS Web/API 检测：`artifacts/xps_http_check_v49.json`

## 8) 参考链接
- Triton inference protocols: https://docs.nvidia.com/deeplearning/triton-inference-server/user-guide/docs/customization_guide/inference_protocols.html
- Triton quickstart health/ready: https://docs.nvidia.com/deeplearning/triton-inference-server/archives/triton-inference-server-2320/user-guide/docs/getting_started/quickstart.html
- TorchServe inference API (ping/health): https://docs.pytorch.org/serve/inference_api.html
- ONNX Runtime profiling tools: https://onnxruntime.ai/docs/performance/tune-performance/profiling-tools.html
- HCIPy about: https://hcipy.org/about.html
- AOtools docs: https://aotools.readthedocs.io/_/downloads/en/v1.0.1/pdf/
